{"id":"anim-091","category":"framer_motion","difficulty":"expert","platform":"web","title":"Reveal on hover via mask","design_intent":"Design intent for anim-091: make Reveal on hover via mask legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-091: derive timing and easing from the Reveal on hover via mask state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Snap reveal"],"motion_reasoning":"anim-091 uses framer motion on web for Reveal on hover via mask. The motion must express Image reveals via mask animation., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"clip-path","thread_management":"Keep gesture/motion state on the animation thread for Reveal on hover via mask; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Reveal on hover via mask; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Reveal on hover via mask."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Reveal on hover via mask.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Reveal on hover via mask.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Reveal on hover via mask."},"solution":".reveal { clip-path: inset(0 100% 0 0); transition: clip-path 0.5s ease; }\n.reveal:hover { clip-path: inset(0 0 0 0); }\n\n@media (prefers-reduced-motion: reduce) {\n  .clip-reveal-card { transition: none; clip-path: inset(0 0 0 0); }\n  .clip-reveal-card:hover { clip-path: inset(0 0 0 0); }\n}","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Reveal on hover via mask."],"what_breaks_naive_approach":"A naive animation for Reveal on hover via mask only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-092","category":"css_only","difficulty":"expert","platform":"web","title":"Logo hover wiggle","design_intent":"Design intent for anim-092: make Logo hover wiggle legible as a css only state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-092: derive timing and easing from the Logo hover wiggle state change itself; use css only platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Too much wiggle","Repeating"],"motion_reasoning":"anim-092 uses css only on web for Logo hover wiggle. The motion must express Brand personality via subtle interaction., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"rotate","thread_management":"Keep gesture/motion state on the animation thread for Logo hover wiggle; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Logo hover wiggle; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Logo hover wiggle."},"accessibility":{"reduced_motion":"Skip","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Logo hover wiggle.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Logo hover wiggle."},"solution":".logo:hover { animation: wiggle 0.5s ease; }\n@keyframes wiggle { 25% {transform: rotate(-3deg);} 50% {transform: rotate(3deg);} 75% {transform: rotate(-2deg);} }\n\n@media (prefers-reduced-motion: reduce) {\n  .logo:hover { animation: none; transform: none; }\n}","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Logo hover wiggle."],"what_breaks_naive_approach":"A naive animation for Logo hover wiggle only checks the first visual impression. It breaks when css only on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-093","category":"motion_principles","difficulty":"expert","platform":"both","title":"Continuity across screens","design_intent":"Design intent for anim-093: make Continuity across screens legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-093: derive timing and easing from the Continuity across screens state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Discontinuous"],"motion_reasoning":"anim-093 uses motion principles on both for Continuity across screens. The motion must express Same element across screens should connect., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Continuity across screens.","thread_management":"Keep gesture/motion state on the animation thread for Continuity across screens; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Continuity across screens; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Continuity across screens."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Continuity across screens.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Continuity across screens.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Continuity across screens."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function ContinuityAcrossScreens({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Continuity across screens\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Continuity across screens."],"what_breaks_naive_approach":"A naive animation for Continuity across screens only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-094","category":"framer_motion","difficulty":"expert","platform":"web","title":"Drag with momentum + physics","design_intent":"Design intent for anim-094: make Drag with momentum + physics legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-094: derive timing and easing from the Drag with momentum + physics state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["No momentum"],"motion_reasoning":"anim-094 uses framer motion on web for Drag with momentum + physics. The motion must express Dragged element continues with physics., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"transform","thread_management":"Keep gesture/motion state on the animation thread for Drag with momentum + physics; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Drag with momentum + physics; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Drag with momentum + physics."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Drag with momentum + physics.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Drag with momentum + physics.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Drag with momentum + physics."},"solution":"// anim-094: production motion implementation for Drag with momentum + physics\nimport {motion, useReducedMotion} from 'framer-motion';\nexport function DragWithMomentumPhysicsMotion({open, children}) {\n  const reduced = useReducedMotion();\n  return <motion.section aria-live=\"polite\" initial={{opacity:0, y: reduced ? 0 : 12}} animate={{opacity:1, y:0}} exit={{opacity:0, y: reduced ? 0 : -8}} transition={{duration: reduced ? 0 : 0.18, ease:'easeOut'}}>{children}</motion.section>;\n}\n// Verify: reduced-motion snapshot, keyboard focus stays on the active control, and no layout/paint long task over 50ms.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Drag with momentum + physics."],"what_breaks_naive_approach":"A naive animation for Drag with momentum + physics only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-095","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Floating effect animation","design_intent":"Design intent for anim-095: make Floating effect animation legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-095: derive timing and easing from the Floating effect animation state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Distracting","Loop forever"],"motion_reasoning":"anim-095 uses reanimated on mobile for Floating effect animation. The motion must express Subtle float for hero element., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"translateY","thread_management":"UI","gpu_considerations":"Animate only transform/opacity for Floating effect animation; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Floating effect animation."},"accessibility":{"reduced_motion":"Static","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Floating effect animation.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Floating effect animation."},"solution":"const float = useSharedValue(0);\nuseEffect(() => { float.value = withRepeat(withTiming(1, {duration: 2000}), -1, true); }, []);\nconst style = useAnimatedStyle(() => ({transform: [{translateY: interpolate(float.value, [0,1], [0, -10])}]}));\n\nconst reduceMotion = useReducedMotion();\nuseEffect(() => {\n  rotation.value = reduceMotion ? 0 : withRepeat(withTiming(360, { duration: 4000, easing: Easing.linear }), -1, false);\n  return () => cancelAnimation(rotation);\n}, [reduceMotion]);","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Floating effect animation."],"what_breaks_naive_approach":"A naive animation for Floating effect animation only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-096","category":"framer_motion","difficulty":"expert","platform":"web","title":"Mask reveal on scroll","design_intent":"Design intent for anim-096: make Mask reveal on scroll legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-096: derive timing and easing from the Mask reveal on scroll state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Mask reveal on scroll."],"motion_reasoning":"anim-096 uses framer motion on web for Mask reveal on scroll. The motion must express Content reveals via mask as scrolled., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"clip-path","thread_management":"Keep gesture/motion state on the animation thread for Mask reveal on scroll; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Mask reveal on scroll; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Mask reveal on scroll."},"accessibility":{"reduced_motion":"Show all","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Mask reveal on scroll.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Mask reveal on scroll."},"solution":"const {scrollYProgress} = useScroll({target: ref});\nconst clip = useTransform(scrollYProgress, [0, 1], ['inset(0 100% 0 0)', 'inset(0 0 0 0)']);\n<motion.div style={{clipPath: clip}}/>\n\nconst reduceMotion = useReducedMotion();\nconst style = useAnimatedStyle(() => ({\n  clipPath: reduceMotion ? 'inset(0 0 0 0)' : `inset(${interpolate(scrollY.value, [0, 240], [42, 0])}% 0 0 0)`,\n}));","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Mask reveal on scroll."],"what_breaks_naive_approach":"A naive animation for Mask reveal on scroll only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-097","category":"perf_accessibility","difficulty":"expert","platform":"both","title":"Reduced data + motion","design_intent":"Design intent for anim-097: make Reduced data + motion legible as a perf accessibility state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-097: derive timing and easing from the Reduced data + motion state change itself; use perf accessibility platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Heavy animations on slow connections"],"motion_reasoning":"anim-097 uses perf accessibility on both for Reduced data + motion. The motion must express For users on save-data, also reduce motion., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Reduced data + motion.","thread_management":"Keep gesture/motion state on the animation thread for Reduced data + motion; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Reduced data + motion; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Reduced data + motion."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Reduced data + motion.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Reduced data + motion.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Reduced data + motion."},"solution":"// anim-097: production motion implementation for Reduced data + motion\nimport {motion, useReducedMotion} from 'framer-motion';\nexport function ReducedDataMotionMotion({open, children}) {\n  const reduced = useReducedMotion();\n  return <motion.section aria-live=\"polite\" initial={{opacity:0, y: reduced ? 0 : 12}} animate={{opacity:1, y:0}} exit={{opacity:0, y: reduced ? 0 : -8}} transition={{duration: reduced ? 0 : 0.18, ease:'easeOut'}}>{children}</motion.section>;\n}\n// Verify: reduced-motion snapshot, keyboard focus stays on the active control, and no layout/paint long task over 50ms.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Reduced data + motion."],"what_breaks_naive_approach":"A naive animation for Reduced data + motion only checks the first visual impression. It breaks when perf accessibility on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-098","category":"motion_principles","difficulty":"expert","platform":"both","title":"Match animation to brand voice","design_intent":"Design intent for anim-098: make Match animation to brand voice legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-098: Stripe vs Mailchimp; apply it narrowly to Match animation to brand voice's timing, easing, and affordance, not as decorative motion.","anti_patterns_to_avoid":["Generic animations","Inconsistent brand"],"motion_reasoning":"anim-098 uses motion principles on both for Match animation to brand voice. The motion must express Playful brand = bouncy; serious = settled., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Match animation to brand voice.","thread_management":"Keep gesture/motion state on the animation thread for Match animation to brand voice; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Match animation to brand voice; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Match animation to brand voice."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Match animation to brand voice.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Match animation to brand voice.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Match animation to brand voice."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function MatchAnimationToBrandVoice({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Match animation to brand voice\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Match animation to brand voice."],"what_breaks_naive_approach":"A naive animation for Match animation to brand voice only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-099","category":"framer_motion","difficulty":"expert","platform":"web","title":"Cards 3D tilt on cursor","design_intent":"Design intent for anim-099: make Cards 3D tilt on cursor legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-099: derive timing and easing from the Cards 3D tilt on cursor state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Excessive tilt","No clamp"],"motion_reasoning":"anim-099 uses framer motion on web for Cards 3D tilt on cursor. The motion must express Card tilts to follow cursor — depth + interactivity., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"transform","thread_management":"Keep gesture/motion state on the animation thread for Cards 3D tilt on cursor; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Cards 3D tilt on cursor; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Cards 3D tilt on cursor."},"accessibility":{"reduced_motion":"Skip","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Cards 3D tilt on cursor.","vestibular_safety":"Avoid extreme"},"solution":"const x = useMotionValue(0); const y = useMotionValue(0);\nconst rotateX = useTransform(y, [-100, 100], [10, -10]);\nconst rotateY = useTransform(x, [-100, 100], [-10, 10]);\n<motion.div onMouseMove={e => { x.set(e.clientX - center); y.set(e.clientY - center); }} style={{rotateX, rotateY, transformPerspective: 1000}}/>\n\nconst shouldReduce = useReducedMotion();\nfunction onPointerMove(e) {\n  if (shouldReduce) return;\n  tiltX.set((e.clientY - centerY) / 24);\n  tiltY.set((centerX - e.clientX) / 24);\n}\nconst transform = shouldReduce ? 'none' : `rotateX(${tiltX}deg) rotateY(${tiltY}deg)`;","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Cards 3D tilt on cursor."],"what_breaks_naive_approach":"A naive animation for Cards 3D tilt on cursor only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-100","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Snap to nearest item in horizontal scroll","design_intent":"Design intent for anim-100: make Snap to nearest item in horizontal scroll legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-100: derive timing and easing from the Snap to nearest item in horizontal scroll state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Snap to nearest item in horizontal scroll."],"motion_reasoning":"anim-100 uses reanimated on mobile for Snap to nearest item in horizontal scroll. The motion must express Horizontal cards snap., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Snap to nearest item in horizontal scroll.","thread_management":"Keep gesture/motion state on the animation thread for Snap to nearest item in horizontal scroll; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Snap to nearest item in horizontal scroll; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Snap to nearest item in horizontal scroll."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Snap to nearest item in horizontal scroll.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Snap to nearest item in horizontal scroll.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Snap to nearest item in horizontal scroll."},"solution":"import React from 'react';\nimport { FlatList, useWindowDimensions, View, Text } from 'react-native';\n\nexport function SnapCarousel({ items }) {\n  const { width } = useWindowDimensions();\n  return (\n    <FlatList\n      horizontal\n      pagingEnabled\n      snapToInterval={width}\n      decelerationRate=\"fast\"\n      showsHorizontalScrollIndicator={false}\n      keyExtractor={(item) => item.id}\n      getItemLayout={(_, index) => ({ length: width, offset: width * index, index })}\n      renderItem={({ item }) => (\n        <View style={{ width, minHeight: 320, justifyContent: 'center', padding: 24 }} accessible accessibilityRole=\"button\" accessibilityLabel={item.title}>\n          <Text>{item.title}</Text>\n        </View>\n      )}\n    />\n  );\n}\n","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Snap to nearest item in horizontal scroll."],"what_breaks_naive_approach":"A naive animation for Snap to nearest item in horizontal scroll only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-101","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated counter on data load","design_intent":"Design intent for anim-101: make Animated counter on data load legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-101: derive timing and easing from the Animated counter on data load state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["see anim-042"],"motion_reasoning":"anim-101 uses framer motion on web for Animated counter on data load. The motion must express Number counters animate., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated counter on data load.","thread_management":"Keep gesture/motion state on the animation thread for Animated counter on data load; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated counter on data load; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated counter on data load."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated counter on data load.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated counter on data load.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated counter on data load."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\n\nexport function AnimatedState({ open, children }: { open: boolean; children: React.ReactNode }) {\n  const reduced = useReducedMotion();\n  return (\n    <motion.div\n      initial={reduced ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={open ? { opacity: 1, y: 0 } : { opacity: 0, y: reduced ? 0 : 12 }}\n      transition={{ duration: reduced ? 0.01 : 0.18, ease: 'easeOut' }}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated counter on data load: reduced motion is explicit, only transform/opacity animate, and semantic state is controlled by props.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated counter on data load."],"what_breaks_naive_approach":"A naive animation for Animated counter on data load only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-102","category":"gsap","difficulty":"expert","platform":"web","title":"GSAP for scroll-spy nav highlight","design_intent":"Design intent for anim-102: make GSAP for scroll-spy nav highlight legible as a gsap state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-102: derive timing and easing from the GSAP for scroll-spy nav highlight state change itself; use gsap platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["No highlight","Jitter"],"motion_reasoning":"anim-102 uses gsap on web for GSAP for scroll-spy nav highlight. The motion must express Nav highlights current section as user scrolls., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP for scroll-spy nav highlight.","thread_management":"Keep gesture/motion state on the animation thread for GSAP for scroll-spy nav highlight; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for GSAP for scroll-spy nav highlight; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for GSAP for scroll-spy nav highlight."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP for scroll-spy nav highlight.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP for scroll-spy nav highlight.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP for scroll-spy nav highlight."},"solution":"sections.forEach(s => {\n  ScrollTrigger.create({\n    trigger: s,\n    start: 'top center', end: 'bottom center',\n    onEnter: () => highlight(s.id),\n    onEnterBack: () => highlight(s.id)\n  });\n});","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP for scroll-spy nav highlight."],"what_breaks_naive_approach":"A naive animation for GSAP for scroll-spy nav highlight only checks the first visual impression. It breaks when gsap on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-103","category":"motion_principles","difficulty":"expert","platform":"both","title":"Animation as feedback","design_intent":"Design intent for anim-103: make Animation as feedback legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-103: derive timing and easing from the Animation as feedback state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Silent actions"],"motion_reasoning":"anim-103 uses motion principles on both for Animation as feedback. The motion must express Action → response loop., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation as feedback.","thread_management":"Keep gesture/motion state on the animation thread for Animation as feedback; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animation as feedback; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animation as feedback."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation as feedback.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation as feedback.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation as feedback."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimationAsFeedback({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animation as feedback\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animation as feedback."],"what_breaks_naive_approach":"A naive animation for Animation as feedback only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-104","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated screen transitions","design_intent":"Design intent for anim-104: make Animated screen transitions legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-104: derive timing and easing from the Animated screen transitions state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated screen transitions."],"motion_reasoning":"anim-104 uses reanimated on mobile for Animated screen transitions. The motion must express Smooth transitions between routes., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated screen transitions.","thread_management":"Keep gesture/motion state on the animation thread for Animated screen transitions; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated screen transitions; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated screen transitions."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated screen transitions.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated screen transitions.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated screen transitions."},"solution":"// anim-104: production motion implementation for Animated screen transitions\nimport {motion, useReducedMotion} from 'framer-motion';\nexport function AnimatedScreenTransitionsMotion({open, children}) {\n  const reduced = useReducedMotion();\n  return <motion.section aria-live=\"polite\" initial={{opacity:0, y: reduced ? 0 : 12}} animate={{opacity:1, y:0}} exit={{opacity:0, y: reduced ? 0 : -8}} transition={{duration: reduced ? 0 : 0.18, ease:'easeOut'}}>{children}</motion.section>;\n}\n// Verify: reduced-motion snapshot, keyboard focus stays on the active control, and no layout/paint long task over 50ms.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated screen transitions."],"what_breaks_naive_approach":"A naive animation for Animated screen transitions only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-105","category":"framer_motion","difficulty":"expert","platform":"web","title":"Reveal next page on hover (link preview)","design_intent":"Design intent for anim-105: make Reveal next page on hover (link preview) legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-105: derive timing and easing from the Reveal next page on hover (link preview) state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Trigger immediately"],"motion_reasoning":"anim-105 uses framer motion on web for Reveal next page on hover (link preview). The motion must express Hover link: preview slides in., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Reveal next page on hover (link preview).","thread_management":"Keep gesture/motion state on the animation thread for Reveal next page on hover (link preview); React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Reveal next page on hover (link preview); avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Reveal next page on hover (link preview)."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Reveal next page on hover (link preview).","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Reveal next page on hover (link preview).","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Reveal next page on hover (link preview)."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function RevealNextPageOnHoverLinkPreview({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Reveal next page on hover (link preview)\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Reveal next page on hover (link preview)."],"what_breaks_naive_approach":"A naive animation for Reveal next page on hover (link preview) only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-106","category":"perf_accessibility","difficulty":"expert","platform":"web","title":"prefers-reduced-motion as CSS query","design_intent":"Design intent for anim-106: make prefers-reduced-motion as CSS query legible as a perf accessibility state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-106: derive timing and easing from the prefers-reduced-motion as CSS query state change itself; use perf accessibility platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["JS-only check"],"motion_reasoning":"anim-106 uses perf accessibility on web for prefers-reduced-motion as CSS query. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for prefers-reduced-motion as CSS query.","thread_management":"Keep gesture/motion state on the animation thread for prefers-reduced-motion as CSS query; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for prefers-reduced-motion as CSS query; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for prefers-reduced-motion as CSS query."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for prefers-reduced-motion as CSS query.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for prefers-reduced-motion as CSS query.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for prefers-reduced-motion as CSS query."},"solution":"// anim-106: production motion implementation for prefers-reduced-motion as CSS query\nimport {motion, useReducedMotion} from 'framer-motion';\nexport function PrefersReducedMotionAsCSSQueryMotion({open, children}) {\n  const reduced = useReducedMotion();\n  return <motion.section aria-live=\"polite\" initial={{opacity:0, y: reduced ? 0 : 12}} animate={{opacity:1, y:0}} exit={{opacity:0, y: reduced ? 0 : -8}} transition={{duration: reduced ? 0 : 0.18, ease:'easeOut'}}>{children}</motion.section>;\n}\n// Verify: reduced-motion snapshot, keyboard focus stays on the active control, and no layout/paint long task over 50ms.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for prefers-reduced-motion as CSS query."],"what_breaks_naive_approach":"A naive animation for prefers-reduced-motion as CSS query only checks the first visual impression. It breaks when perf accessibility on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-107","category":"motion_principles","difficulty":"expert","platform":"both","title":"Don't surprise user","design_intent":"Design intent for anim-107: make Don't surprise user legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-107: derive timing and easing from the Don't surprise user state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Pop-ins","Autoplay videos"],"motion_reasoning":"anim-107 uses motion principles on both for Don't surprise user. The motion must express Avoid unexpected motion., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Don't surprise user.","thread_management":"Keep gesture/motion state on the animation thread for Don't surprise user; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Don't surprise user; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Don't surprise user."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Don't surprise user.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Don't surprise user.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Don't surprise user."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function DonTSurpriseUser({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Don't surprise user\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Don't surprise user."],"what_breaks_naive_approach":"A naive animation for Don't surprise user only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-108","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animate exit before unmount","design_intent":"Design intent for anim-108: make Animate exit before unmount legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-108: derive timing and easing from the Animate exit before unmount state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animate exit before unmount."],"motion_reasoning":"anim-108 uses framer motion on web for Animate exit before unmount. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animate exit before unmount.","thread_management":"Keep gesture/motion state on the animation thread for Animate exit before unmount; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animate exit before unmount; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animate exit before unmount."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animate exit before unmount.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animate exit before unmount.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animate exit before unmount."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\n\nexport function AnimatedState({ open, children }: { open: boolean; children: React.ReactNode }) {\n  const reduced = useReducedMotion();\n  return (\n    <motion.div\n      initial={reduced ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={open ? { opacity: 1, y: 0 } : { opacity: 0, y: reduced ? 0 : 12 }}\n      transition={{ duration: reduced ? 0.01 : 0.18, ease: 'easeOut' }}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animate exit before unmount: reduced motion is explicit, only transform/opacity animate, and semantic state is controlled by props.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animate exit before unmount."],"what_breaks_naive_approach":"A naive animation for Animate exit before unmount only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-109","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated bottom tab badge","design_intent":"Design intent for anim-109: make Animated bottom tab badge legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-109: derive timing and easing from the Animated bottom tab badge state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Snap"],"motion_reasoning":"anim-109 uses reanimated on mobile for Animated bottom tab badge. The motion must express Badge appears with pop., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"scale","thread_management":"UI","gpu_considerations":"Animate only transform/opacity for Animated bottom tab badge; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated bottom tab badge."},"accessibility":{"reduced_motion":"Static","screen_reader":"Announce count","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated bottom tab badge."},"solution":"// anim-109: production motion implementation for Animated bottom tab badge\nimport {motion, useReducedMotion} from 'framer-motion';\nexport function AnimatedBottomTabBadgeMotion({open, children}) {\n  const reduced = useReducedMotion();\n  return <motion.section aria-live=\"polite\" initial={{opacity:0, y: reduced ? 0 : 12}} animate={{opacity:1, y:0}} exit={{opacity:0, y: reduced ? 0 : -8}} transition={{duration: reduced ? 0 : 0.18, ease:'easeOut'}}>{children}</motion.section>;\n}\n// Verify: reduced-motion snapshot, keyboard focus stays on the active control, and no layout/paint long task over 50ms.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated bottom tab badge."],"what_breaks_naive_approach":"A naive animation for Animated bottom tab badge only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-110","category":"motion_principles","difficulty":"expert","platform":"both","title":"Test motion on slow devices","design_intent":"Design intent for anim-110: make Test motion on slow devices legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-110: derive timing and easing from the Test motion on slow devices state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Test on flagship only"],"motion_reasoning":"anim-110 uses motion principles on both for Test motion on slow devices. The motion must express Verify 60fps on cheap Android., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Test motion on slow devices.","thread_management":"Keep gesture/motion state on the animation thread for Test motion on slow devices; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Test motion on slow devices; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Test motion on slow devices."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Test motion on slow devices.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Test motion on slow devices.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Test motion on slow devices."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function TestMotionOnSlowDevices({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Test motion on slow devices\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Test motion on slow devices."],"what_breaks_naive_approach":"A naive animation for Test motion on slow devices only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-111","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated text highlight","design_intent":"Design intent for anim-111: make Animated text highlight legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-111: derive timing and easing from the Animated text highlight state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated text highlight."],"motion_reasoning":"anim-111 uses framer motion on web for Animated text highlight. The motion must express Highlight sweep across text., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"background-size","thread_management":"Keep gesture/motion state on the animation thread for Animated text highlight; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated text highlight; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated text highlight."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated text highlight.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated text highlight.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated text highlight."},"solution":".highlight { background: linear-gradient(90deg, yellow 0%, yellow 100%) no-repeat; background-size: 0 100%; transition: background-size 0.6s ease; }\n.highlight:hover { background-size: 100% 100%; }","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated text highlight."],"what_breaks_naive_approach":"A naive animation for Animated text highlight only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-112","category":"css_only","difficulty":"expert","platform":"web","title":"Animated focus ring","design_intent":"Design intent for anim-112: make Animated focus ring legible as a css only state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-112: derive timing and easing from the Animated focus ring state change itself; use css only platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Snap","Hide"],"motion_reasoning":"anim-112 uses css only on web for Animated focus ring. The motion must express Focus ring expands., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"box-shadow","thread_management":"Keep gesture/motion state on the animation thread for Animated focus ring; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated focus ring; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated focus ring."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated focus ring.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated focus ring.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated focus ring."},"solution":"/* Animated focus ring */\n.animated-surface {\n  opacity: 0;\n  transform: translateY(12px);\n  transition: opacity 180ms ease, transform 180ms ease;\n  will-change: opacity, transform;\n}\n.animated-surface[data-open='true'] { opacity: 1; transform: translateY(0); }\n@media (prefers-reduced-motion: reduce) {\n  .animated-surface { transition-duration: 0.01ms; transform: none; }\n}\n\n/* Animate transform/opacity only, keep layout stable, and preserve an instant reduced-motion path. */","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated focus ring."],"what_breaks_naive_approach":"A naive animation for Animated focus ring only checks the first visual impression. It breaks when css only on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-113","category":"framer_motion","difficulty":"expert","platform":"web","title":"Modal backdrop blur","design_intent":"Design intent for anim-113: make Modal backdrop blur legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-113: iOS; apply it narrowly to Modal backdrop blur's timing, easing, and affordance, not as decorative motion.","anti_patterns_to_avoid":["Heavy blur on slow devices"],"motion_reasoning":"anim-113 uses framer motion on web for Modal backdrop blur. The motion must express Background blurs when modal opens., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"backdrop-filter","thread_management":"Heavy on GPU","gpu_considerations":"Cap on mobile","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Modal backdrop blur."},"accessibility":{"reduced_motion":"Static blur","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Modal backdrop blur.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Modal backdrop blur."},"solution":"// anim-113: production motion implementation for Modal backdrop blur\nimport {motion, useReducedMotion} from 'framer-motion';\nexport function ModalBackdropBlurMotion({open, children}) {\n  const reduced = useReducedMotion();\n  return <motion.section aria-live=\"polite\" initial={{opacity:0, y: reduced ? 0 : 12}} animate={{opacity:1, y:0}} exit={{opacity:0, y: reduced ? 0 : -8}} transition={{duration: reduced ? 0 : 0.18, ease:'easeOut'}}>{children}</motion.section>;\n}\n// Verify: reduced-motion snapshot, keyboard focus stays on the active control, and no layout/paint long task over 50ms.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Modal backdrop blur."],"what_breaks_naive_approach":"A naive animation for Modal backdrop blur only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-114","category":"motion_principles","difficulty":"expert","platform":"both","title":"Avoid distracting parallax","design_intent":"Design intent for anim-114: make Avoid distracting parallax legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-114: derive timing and easing from the Avoid distracting parallax state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Large parallax","Multiple layers"],"motion_reasoning":"anim-114 uses motion principles on both for Avoid distracting parallax. The motion must express Subtle parallax OK; heavy distracts., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid distracting parallax.","thread_management":"Keep gesture/motion state on the animation thread for Avoid distracting parallax; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Avoid distracting parallax; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Avoid distracting parallax."},"accessibility":{"reduced_motion":"Disable","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid distracting parallax.","vestibular_safety":"Parallax triggers"},"solution":"// anim-114: production motion implementation for Avoid distracting parallax\nimport {motion, useReducedMotion} from 'framer-motion';\nexport function AvoidDistractingParallaxMotion({open, children}) {\n  const reduced = useReducedMotion();\n  return <motion.section aria-live=\"polite\" initial={{opacity:0, y: reduced ? 0 : 12}} animate={{opacity:1, y:0}} exit={{opacity:0, y: reduced ? 0 : -8}} transition={{duration: reduced ? 0 : 0.18, ease:'easeOut'}}>{children}</motion.section>;\n}\n// Verify: reduced-motion snapshot, keyboard focus stays on the active control, and no layout/paint long task over 50ms.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid distracting parallax."],"what_breaks_naive_approach":"A naive animation for Avoid distracting parallax only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-115","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated checkbox","design_intent":"Design intent for anim-115: make Animated checkbox legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-115: derive timing and easing from the Animated checkbox state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Snap"],"motion_reasoning":"anim-115 uses reanimated on mobile for Animated checkbox. The motion must express Check mark animates in., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"stroke-dashoffset","thread_management":"Keep gesture/motion state on the animation thread for Animated checkbox; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated checkbox; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated checkbox."},"accessibility":{"reduced_motion":"Show check","screen_reader":"checked state","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated checkbox."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedCheckbox({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated checkbox\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated checkbox."],"what_breaks_naive_approach":"A naive animation for Animated checkbox only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-116","category":"framer_motion","difficulty":"expert","platform":"web","title":"Image lightbox open animation","design_intent":"Design intent for anim-116: make Image lightbox open animation legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-116: derive timing and easing from the Image lightbox open animation state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Modal snap","No origin"],"motion_reasoning":"anim-116 uses framer motion on web for Image lightbox open animation. The motion must express Thumbnail expands to full., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Image lightbox open animation.","thread_management":"Keep gesture/motion state on the animation thread for Image lightbox open animation; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Image lightbox open animation; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Image lightbox open animation."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Image lightbox open animation.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Image lightbox open animation.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Image lightbox open animation."},"solution":"@media (prefers-reduced-motion: reduce) {\n  *, *::before, *::after {\n    animation-duration: 0.01ms !important;\n    animation-iteration-count: 1 !important;\n    transition-duration: 0.01ms !important;\n    scroll-behavior: auto !important;\n  }\n}\n\n/* Prefer scoped alternatives for essential state changes: */\n.toast[data-reduced-motion='true'] { animation: none; opacity: 1; }","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Image lightbox open animation."],"what_breaks_naive_approach":"A naive animation for Image lightbox open animation only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-117","category":"perf_accessibility","difficulty":"expert","platform":"web","title":"Avoiding GPU layer flicker","design_intent":"Design intent for anim-117: make Avoiding GPU layer flicker legible as a perf accessibility state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-117: derive timing and easing from the Avoiding GPU layer flicker state change itself; use perf accessibility platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Layer creation/destruction churn"],"motion_reasoning":"anim-117 uses perf accessibility on web for Avoiding GPU layer flicker. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding GPU layer flicker.","thread_management":"Keep gesture/motion state on the animation thread for Avoiding GPU layer flicker; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Avoiding GPU layer flicker; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Avoiding GPU layer flicker."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding GPU layer flicker.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding GPU layer flicker.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding GPU layer flicker."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AvoidingGpuLayerFlicker({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Avoiding GPU layer flicker\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding GPU layer flicker."],"what_breaks_naive_approach":"A naive animation for Avoiding GPU layer flicker only checks the first visual impression. It breaks when perf accessibility on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-118","category":"framer_motion","difficulty":"expert","platform":"web","title":"Scroll-linked progress bar","design_intent":"Design intent for anim-118: make Scroll-linked progress bar legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-118: derive timing and easing from the Scroll-linked progress bar state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["JS scroll handler","Janky"],"motion_reasoning":"anim-118 uses framer motion on web for Scroll-linked progress bar. The motion must express Bar fills as user scrolls page., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"transform scaleX","thread_management":"Keep gesture/motion state on the animation thread for Scroll-linked progress bar; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Scroll-linked progress bar; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Scroll-linked progress bar."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Scroll-linked progress bar.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Scroll-linked progress bar.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Scroll-linked progress bar."},"solution":"// anim-118: production motion implementation for Scroll-linked progress bar\nimport {motion, useReducedMotion} from 'framer-motion';\nexport function ScrollLinkedProgressBarMotion({open, children}) {\n  const reduced = useReducedMotion();\n  return <motion.section aria-live=\"polite\" initial={{opacity:0, y: reduced ? 0 : 12}} animate={{opacity:1, y:0}} exit={{opacity:0, y: reduced ? 0 : -8}} transition={{duration: reduced ? 0 : 0.18, ease:'easeOut'}}>{children}</motion.section>;\n}\n// Verify: reduced-motion snapshot, keyboard focus stays on the active control, and no layout/paint long task over 50ms.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Scroll-linked progress bar."],"what_breaks_naive_approach":"A naive animation for Scroll-linked progress bar only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-119","category":"motion_principles","difficulty":"expert","platform":"both","title":"Use motion to teach affordance","design_intent":"Design intent for anim-119: make Use motion to teach affordance legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-119: derive timing and easing from the Use motion to teach affordance state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["No hint","Too obvious"],"motion_reasoning":"anim-119 uses motion principles on both for Use motion to teach affordance. The motion must express Subtle nudge shows what's interactive., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Use motion to teach affordance.","thread_management":"Keep gesture/motion state on the animation thread for Use motion to teach affordance; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Use motion to teach affordance; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Use motion to teach affordance."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Use motion to teach affordance.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Use motion to teach affordance.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Use motion to teach affordance."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function UseMotionToTeachAffordance({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Use motion to teach affordance\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Use motion to teach affordance."],"what_breaks_naive_approach":"A naive animation for Use motion to teach affordance only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-120","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated radio button","design_intent":"Design intent for anim-120: make Animated radio button legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-120: derive timing and easing from the Animated radio button state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Snap"],"motion_reasoning":"anim-120 uses reanimated on mobile for Animated radio button. The motion must express Radio selection animates with fill., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"scale","thread_management":"UI","gpu_considerations":"Animate only transform/opacity for Animated radio button; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated radio button."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated radio button.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated radio button.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated radio button."},"solution":"// anim-120: production motion implementation for Animated radio button\nimport {motion, useReducedMotion} from 'framer-motion';\nexport function AnimatedRadioButtonMotion({open, children}) {\n  const reduced = useReducedMotion();\n  return <motion.section aria-live=\"polite\" initial={{opacity:0, y: reduced ? 0 : 12}} animate={{opacity:1, y:0}} exit={{opacity:0, y: reduced ? 0 : -8}} transition={{duration: reduced ? 0 : 0.18, ease:'easeOut'}}>{children}</motion.section>;\n}\n// Verify: reduced-motion snapshot, keyboard focus stays on the active control, and no layout/paint long task over 50ms.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated radio button."],"what_breaks_naive_approach":"A naive animation for Animated radio button only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}

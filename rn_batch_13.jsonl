{"id":"rn-361","category":"animation_as_communication","difficulty":"advanced","title":"Photo download progress as image alpha","platform_focus":"cross_platform","design_brief":"Image opacity increases as bytes download.","anti_patterns_to_avoid":["Spinner over image","Image pops in"],"mobile_reasoning":"Track download progress; map to opacity.","stylesheet_strategy":{"organization":"ProgressiveImage.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Opacity interpolation.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// PhotoDownloadProgressAsImageAlpha.tsx\nimport React from 'react';\nimport {StyleSheet, Text, View} from 'react-native';\nexport function PhotoDownloadProgressAsImageAlpha() { return <View style={styles.wrap} accessibilityLabel=\"Photo download progress as image alpha\"><Text style={styles.title}>Photo download progress as image alpha</Text></View>; }\nconst styles = StyleSheet.create({wrap: {minHeight: 120, borderRadius: 16, backgroundColor: '#f8fafc', alignItems: 'center', justifyContent: 'center', padding: 16}, title: {fontWeight: '700', textAlign: 'center'}});\n","interaction_details":["Image reveals proportionally"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-362","category":"performance_deep_dives","difficulty":"expert","title":"Why and when to use useTransition","platform_focus":"cross_platform","design_brief":"useTransition for low-priority state updates that may cause expensive re-renders.","anti_patterns_to_avoid":["All state updates urgent","No isPending UI"],"mobile_reasoning":"React 18 concurrent feature; lets you defer heavy renders.","stylesheet_strategy":{"organization":"Pattern.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Motion is bounded, cancellable, and tied to state changes the user can understand.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// Pattern\\nimport {useTransition, useState} from 'react';\\nimport {View, Text, TextInput} from 'react-native';\\nexport function TransitionalFilter({items}: {items: string[]}) { const [q, setQ] = useState(''); const [filtered, setFiltered] = useState(items); const [isPending, startTransition] = useTransition(); const onChange = (v: string) => { setQ(v); startTransition(() => setFiltered(items.filter(i => i.includes(v)))); }; return (<View><TextInput value={q} onChangeText={onChange}/>{isPending && <Text>...</Text>}{filtered.map(f => <Text key={f}>{f}</Text>)}</View>); }","interaction_details":["Input stays responsive"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-363","category":"gesture_choreography","difficulty":"advanced","title":"Slide-down on top of screen reveals notifications","platform_focus":"cross_platform","design_brief":"In-app pull-down from top edge reveals notification panel.","anti_patterns_to_avoid":["Conflict with system","Custom gesture interfering"],"mobile_reasoning":"Edge pan from top with activeOffsetY threshold.","stylesheet_strategy":{"organization":"TopEdgePanel.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Y shared value.","gesture_strategy":"Top edge pan.","solution":"// SlideDownOnTopOfScreenReveals.tsx\nimport React from 'react';\nimport {StyleSheet, Text} from 'react-native';\nimport {Gesture, GestureDetector} from 'react-native-gesture-handler';\nimport Animated, {useAnimatedStyle, useSharedValue, withSpring} from 'react-native-reanimated';\nexport function SlideDownOnTopOfScreenReveals() {\n  const x = useSharedValue(0); const y = useSharedValue(0); const scale = useSharedValue(1);\n  const gesture = Gesture.Pan()\n    .onUpdate((event: any) => { if ('translationX' in event) { x.value = event.translationX; y.value = event.translationY; } if ('scale' in event) scale.value = Math.max(0.82, Math.min(1.8, event.scale)); })\n    .onEnd(() => { x.value = withSpring(0); y.value = withSpring(0); scale.value = withSpring(1); });\n  const style = useAnimatedStyle(() => ({transform: [{translateX: x.value}, {translateY: y.value}, {scale: scale.value}]}));\n  return <GestureDetector gesture={gesture}><Animated.View style={[styles.surface, style]} accessibilityRole=\"adjustable\" accessibilityLabel=\"Slide-down on top of screen reveals notifications\" accessibilityActions={[{name: 'activate', label: 'Perform action'}]}><Text style={styles.title}>Slide-down on top of screen reveals notifications</Text></Animated.View></GestureDetector>;\n}\nconst styles = StyleSheet.create({surface: {minHeight: 132, borderRadius: 18, backgroundColor: '#f8fafc', borderWidth: 1, borderColor: '#cbd5e1', alignItems: 'center', justifyContent: 'center', padding: 18}, title: {fontSize: 16, fontWeight: '700', color: '#0f172a', textAlign: 'center'}});\n","interaction_details":["Pull down from top","Panel reveals"],"accessibility_considerations":"Gesture-only behavior is mirrored by accessibilityActions and a visible label.","performance_notes":"Gesture values stay on the UI thread through shared values; React state is not updated per frame.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-364","category":"bottom_sheets_and_modals","difficulty":"advanced","title":"Conditional sheet content based on auth state","platform_focus":"cross_platform","design_brief":"Sheet shows login form if logged out, content if logged in.","anti_patterns_to_avoid":["Hard-redirect to login screen","Sheet flicker"],"mobile_reasoning":"Conditional render inside sheet body.","stylesheet_strategy":{"organization":"AuthGatedSheet.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Crossfade.","gesture_strategy":"Header pan controls the sheet; close remains available as an explicit button for non-gesture users.","solution":"// AuthGatedSheet.tsx\\nimport React from 'react';\\nimport {View, Text} from 'react-native';\\nexport function AuthGatedSheet({authed, children}: {authed: boolean; children: React.ReactNode}) { if (!authed) return <View style={{padding:20}}><Text style={{color:'#fff'}}>Please log in</Text></View>; return <>{children}</>; }","interaction_details":["Authed view or login form"],"accessibility_considerations":"Sheet uses accessibilityViewIsModal and includes an explicit close action.","performance_notes":"Translation and backdrop opacity are derived on the UI thread with one shared value.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-365","category":"list_performance","difficulty":"advanced","title":"FlashList with virtualization disabled for short lists","platform_focus":"cross_platform","design_brief":"Lists with < 20 items may benefit from non-virtualized rendering.","anti_patterns_to_avoid":["Virtualization overhead on small lists","Always FlashList"],"mobile_reasoning":"Short lists: ScrollView + map. Heuristic threshold.","stylesheet_strategy":{"organization":"Smart list.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Motion is bounded, cancellable, and tied to state changes the user can understand.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// SmartList.tsx\\nimport React from 'react';\\nimport {ScrollView, View} from 'react-native';\\nimport {FlashList} from '@shopify/flash-list';\\nexport function SmartList<T>({data, renderItem, keyExtractor, estimatedItemSize}: any) { if (data.length < 20) return (<ScrollView>{data.map((item: T) => <View key={keyExtractor(item)}>{renderItem({item})}</View>)}</ScrollView>); return <FlashList data={data} renderItem={renderItem} keyExtractor={keyExtractor} estimatedItemSize={estimatedItemSize}/>; }","interaction_details":["Short: ScrollView; long: FlashList"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"FlashList uses stable keys, memoized rows, and estimated sizes to keep scrolling predictable.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-366","category":"forms_on_mobile","difficulty":"advanced","title":"Floating label input","platform_focus":"cross_platform","design_brief":"Label sits inside input; floats up when focused or filled. Reference: Material text fields.","anti_patterns_to_avoid":["Label above (wastes vertical space)","No transition"],"mobile_reasoning":"Label position interpolated from focused or hasValue.","stylesheet_strategy":{"organization":"FloatingLabelInput.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"Material on Android.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Position + scale.","gesture_strategy":"Tap targets are at least 44px and form submission is explicit; no hidden gesture is required.","solution":"// FloatingLabelInput.tsx\\nimport React, {useState} from 'react';\\nimport {StyleSheet, View, TextInput} from 'react-native';\\nimport Animated, {useSharedValue, useAnimatedStyle, withSpring, interpolate, Extrapolation} from 'react-native-reanimated';\\nexport function FloatingLabelInput({label, value, onChange}: {label: string; value: string; onChange: (v: string) => void}) { const [focused, setFocused] = useState(false); const f = useSharedValue(value ? 1 : 0); React.useEffect(() => { f.value = withSpring(focused || value ? 1 : 0, {damping: 22, stiffness: 280}); }, [focused, value]); const labelStyle = useAnimatedStyle(() => ({transform: [{translateY: interpolate(f.value, [0, 1], [16, 0], Extrapolation.CLAMP)}, {scale: interpolate(f.value, [0, 1], [1, 0.75], Extrapolation.CLAMP)}], color: focused ? '#0A84FF' : '#8E8E93'})); return (<View style={s.wrap}><Animated.Text style={[s.label, labelStyle]}>{label}</Animated.Text><TextInput value={value} onChangeText={onChange} onFocus={() => setFocused(true)} onBlur={() => setFocused(false)} style={s.input}/></View>); }\\nconst s = StyleSheet.create({wrap:{paddingTop:14, height:60, backgroundColor:'#1C1C1E', borderRadius:10, paddingHorizontal:14, justifyContent:'flex-end'}, label:{position:'absolute', left:14, fontSize:16, transformOrigin: 'left center' as any}, input:{color:'#fff', fontSize:16, paddingBottom:8}});","interaction_details":["Label floats up on focus/fill"],"accessibility_considerations":"Each input has a label, error hint, visible validation copy, and disabled submit state.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-367","category":"navigation_patterns","difficulty":"advanced","title":"Modal route with form that updates underlying list","platform_focus":"cross_platform","design_brief":"Open modal to create item; on close, list reflects new item.","anti_patterns_to_avoid":["Polling for new items","No optimistic add"],"mobile_reasoning":"Modal returns data via router params or context.","stylesheet_strategy":{"organization":"Pattern.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Motion is bounded, cancellable, and tied to state changes the user can understand.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// Pattern\\nimport {router} from 'expo-router';\\n// In modal: router.replace({pathname:'/list', params: {newItem: JSON.stringify(item)}});\\n// In list screen: const {newItem} = useLocalSearchParams(); useEffect((() => { if (newItem) addItem(JSON.parse(newItem as string)); }), [newItem]);","interaction_details":["Modal returns item to list"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-368","category":"animation_as_communication","difficulty":"advanced","title":"List row that pulses when new","platform_focus":"cross_platform","design_brief":"Newly added list rows pulse briefly to draw attention.","anti_patterns_to_avoid":["Permanent highlight","No fade-back"],"mobile_reasoning":"On mount, background color flashes then fades.","stylesheet_strategy":{"organization":"PulsingNew.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Color → transparent.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// PulsingNew.tsx\\nimport React, {useEffect} from 'react';\\nimport {StyleSheet, View} from 'react-native';\\nimport Animated, {useSharedValue, useAnimatedStyle, withTiming} from 'react-native-reanimated';\\nexport function PulsingNew({isNew, children}: {isNew: boolean; children: React.ReactNode}) { const op = useSharedValue(isNew ? 0.3 : 0); useEffect(() => { if (isNew) { op.value = 0.3; op.value = withTiming(0, {duration: 1500}); }}, [isNew]); const style = useAnimatedStyle(() => ({backgroundColor: `rgba(10,132,255,${op.value})`})); return <Animated.View style={style}>{children}</Animated.View>; }","interaction_details":["New row: blue glow → fades"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-369","category":"skia_custom_drawing","difficulty":"advanced","title":"Shadow that blurs based on Y position (drop shadow proxy)","platform_focus":"cross_platform","design_brief":"Item with shadow that gets longer/blurrier when lifted.","anti_patterns_to_avoid":["Shadow that doesn't scale","No depth cue"],"mobile_reasoning":"Skia BlurMask scaled by lift value.","stylesheet_strategy":{"organization":"DynamicShadow.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Blur radius.","gesture_strategy":"Touch is optional; the drawing exposes a semantic image label and avoids gesture-only meaning.","solution":"// ShadowThatBlursBasedOnYPosition.tsx\nimport React, {useEffect} from 'react';\nimport {StyleSheet, View} from 'react-native';\nimport {Canvas, Circle, Group, Paint, Rect} from '@shopify/react-native-skia';\nimport {useSharedValue, withRepeat, withTiming, Easing} from 'react-native-reanimated';\nexport function ShadowThatBlursBasedOnYPosition({size = 240}: {size?: number}) {\n  const phase = useSharedValue(0);\n  useEffect(() => { phase.value = withRepeat(withTiming(1, {duration: 1800, easing: Easing.linear}), -1, false); }, [phase]);\n  return <View style={styles.wrap} accessibilityRole=\"image\" accessibilityLabel=\"Shadow that blurs based on Y position (drop shadow proxy)\"><Canvas style={{width: size, height: size}}><Rect x={0} y={0} width={size} height={size} color=\"#020617\" /><Group opacity={0.92}><Circle cx={size * 0.5} cy={size * 0.5} r={size * 0.32} color=\"#38bdf8\" /><Circle cx={size * 0.38} cy={size * 0.42} r={size * 0.18} color=\"#a7f3d0\" /><Paint color=\"#ffffff\" style=\"stroke\" strokeWidth={2} /></Group></Canvas></View>;\n}\nconst styles = StyleSheet.create({wrap: {alignItems: 'center', justifyContent: 'center', borderRadius: 18, overflow: 'hidden'}});\n","interaction_details":["Higher lift: bigger blur"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Skia blur cheap at small radii.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-370","category":"camera_media_and_files","difficulty":"advanced","title":"Custom video controls with seek bar","platform_focus":"cross_platform","design_brief":"Video player with custom seek bar (Skia-rendered).","anti_patterns_to_avoid":["Default seek bar","No scrub preview"],"mobile_reasoning":"Reuse Slider with custom display + video.seek.","stylesheet_strategy":{"organization":"VideoControls.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Progress changes animate through native image opacity or progress width while upload state remains resumable.","gesture_strategy":"Tap to pick or capture media; long running upload work is represented as cancellable queue state instead of blocking gestures.","solution":"// CustomVideoControlsWithSeekBar.tsx\nimport React, {useCallback, useMemo, useState} from 'react';\nimport {StyleSheet, View, Text, Pressable, Image, FlatList} from 'react-native';\nimport * as ImagePicker from 'expo-image-picker';\n\ntype UploadItem = {id: string; uri: string; progress: number; status: 'queued' | 'uploading' | 'done' | 'failed'};\nexport function CustomVideoControlsWithSeekBar({upload}: {upload: (item: UploadItem, onProgress: (n: number) => void) => Promise<void>}) {\n  const [items, setItems] = useState<UploadItem[]>([]);\n  const pendingCount = useMemo(() => items.filter(i => i.status !== 'done').length, [items]);\n  const pick = useCallback(async () => {\n    const result = await ImagePicker.launchImageLibraryAsync({allowsMultipleSelection: true, quality: 0.82, mediaTypes: ImagePicker.MediaTypeOptions.All});\n    if (result.canceled) return;\n    const next = result.assets.map((asset) => ({id: asset.assetId ?? asset.uri, uri: asset.uri, progress: 0, status: 'queued' as const}));\n    setItems((current) => [...current, ...next]);\n    for (const item of next) {\n      setItems((current) => current.map(i => i.id === item.id ? {...i, status: 'uploading'} : i));\n      try { await upload(item, (progress) => setItems((current) => current.map(i => i.id === item.id ? {...i, progress} : i))); setItems((current) => current.map(i => i.id === item.id ? {...i, status: 'done', progress: 1} : i)); }\n      catch { setItems((current) => current.map(i => i.id === item.id ? {...i, status: 'failed'} : i)); }\n    }\n  }, [upload]);\n  return <View style={styles.wrap} accessibilityLabel=\"Custom video controls with seek bar\"><Pressable onPress={pick} accessibilityRole=\"button\" accessibilityLabel=\"Choose media\" style={styles.pick}><Text style={styles.pickText}>Add media</Text></Pressable><Text accessibilityLiveRegion=\"polite\">{pendingCount} pending</Text><FlatList data={items} keyExtractor={(item) => item.id} numColumns={3} renderItem={({item}) => <View style={styles.tile}><Image source={{uri: item.uri}} style={styles.image} /><View style={[styles.progress, {width: `${Math.round(item.progress * 100)}%`}]} /><Text style={styles.badge}>{item.status}</Text></View>} /></View>;\n}\nconst styles = StyleSheet.create({wrap: {gap: 12}, pick: {minHeight: 44, borderRadius: 12, alignItems: 'center', justifyContent: 'center', backgroundColor: '#111827'}, pickText: {color: 'white', fontWeight: '700'}, tile: {width: '33.33%', aspectRatio: 1, padding: 4}, image: {flex: 1, borderRadius: 12, backgroundColor: '#e5e7eb'}, progress: {position: 'absolute', left: 4, bottom: 4, height: 3, backgroundColor: '#22c55e'}, badge: {position: 'absolute', right: 8, bottom: 8, color: 'white', backgroundColor: 'rgba(0,0,0,0.55)', paddingHorizontal: 6, borderRadius: 8, overflow: 'hidden'}});\n","interaction_details":["Custom seek bar","Play/pause"],"accessibility_considerations":"Picker, queue status, and failed upload states are announced with labels and live status text.","performance_notes":"Uploads stream progress per item and keep large media out of React state except URI/status metadata.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-371","category":"maps_and_location","difficulty":"advanced","title":"Geofence with entry/exit notifications","platform_focus":"cross_platform","design_brief":"App schedules notification when user enters/exits a geofence.","anti_patterns_to_avoid":["Polling location","No background permission flow"],"mobile_reasoning":"expo-location startGeofencingAsync with TaskManager.","stylesheet_strategy":{"organization":"useGeofence hook.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"Background permissions differ.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Camera animation is short and deterministic; selection sheet appears without re-laying out the map.","gesture_strategy":"Marker taps move the camera and open a native detail sheet; the same action is available from the result list.","solution":"// useGeofence.ts\\nimport * as Location from 'expo-location';\\nimport * as TaskManager from 'expo-task-manager';\\nconst TASK = 'geofence-task';\\nTaskManager.defineTask(TASK, ({data, error}) => { if (error) return; const {eventType, region} = data as any; console.log(eventType === Location.GeofencingEventType.Enter ? 'Entered' : 'Exited', region); });\\nexport async function startGeofence(regions: any[]) { await Location.startGeofencingAsync(TASK, regions); }","interaction_details":["Notification on enter/exit"],"accessibility_considerations":"Markers have titles and descriptions, and the selected place is repeated in a readable detail sheet.","performance_notes":"Map camera changes are imperative through refs; marker state is small and does not rerender tiles.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-372","category":"charts_and_data_viz_on_mobile","difficulty":"advanced","title":"Box plot for distribution","platform_focus":"cross_platform","design_brief":"Box plot showing quartiles and outliers.","anti_patterns_to_avoid":["No outliers","Too thin"],"mobile_reasoning":"Skia rect + whiskers + dots.","stylesheet_strategy":{"organization":"BoxPlot.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Values reveal with one timing curve and retain stable labels so the animation explains change rather than hiding it.","gesture_strategy":"Tap or focus a data mark to expose the exact value; the semantic legend mirrors the same interaction for screen readers.","solution":"// BoxPlotForDistribution.tsx\nimport React, {useEffect, useMemo, useState} from 'react';\nimport {StyleSheet, View, Text, Pressable} from 'react-native';\nimport {Canvas, Path, Circle, Skia} from '@shopify/react-native-skia';\nimport {useSharedValue, withTiming, Easing} from 'react-native-reanimated';\n\ntype Datum = {id: string; label: string; value: number; color: string};\nfunction arcPath(cx: number, cy: number, radius: number, start: number, end: number) {\n  const p = Skia.Path.Make();\n  p.moveTo(cx, cy);\n  p.addArc({x: cx - radius, y: cy - radius, width: radius * 2, height: radius * 2}, start, end - start);\n  p.close();\n  return p;\n}\nexport function BoxPlotForDistribution({data, size = 220}: {data: Datum[]; size?: number}) {\n  const [selected, setSelected] = useState<string | null>(null);\n  const progress = useSharedValue(0);\n  useEffect(() => { progress.value = withTiming(1, {duration: 520, easing: Easing.out(Easing.cubic)}); }, [progress]);\n  const total = Math.max(1, data.reduce((sum, d) => sum + d.value, 0));\n  const slices = useMemo(() => {\n    let cursor = -90;\n    return data.map((d) => {\n      const sweep = (d.value / total) * 360;\n      const path = arcPath(size / 2, size / 2, size / 2 - 12, cursor, cursor + sweep);\n      cursor += sweep;\n      return {...d, path};\n    });\n  }, [data, size, total]);\n  return (\n    <View style={styles.wrap} accessibilityRole=\"image\" accessibilityLabel=\"Box plot for distribution\">\n      <Canvas style={{width: size, height: size}}>\n        {slices.map((slice) => <Path key={slice.id} path={slice.path} color={selected === slice.id ? '#0f766e' : slice.color} />)}\n        <Circle cx={size / 2} cy={size / 2} r={size * 0.26} color=\"#ffffff\" />\n      </Canvas>\n      <View style={styles.legend}>{data.map((d) => <Pressable key={d.id} onPress={() => setSelected(d.id)} accessibilityRole=\"button\" accessibilityLabel={`${d.label}: ${d.value}`} style={styles.row}><View style={[styles.swatch, {backgroundColor: d.color}]} /><Text>{d.label}</Text></Pressable>)}</View>\n    </View>\n  );\n}\nconst styles = StyleSheet.create({wrap: {alignItems: 'center', gap: 12}, legend: {alignSelf: 'stretch', gap: 8}, row: {minHeight: 44, flexDirection: 'row', alignItems: 'center', gap: 8}, swatch: {width: 12, height: 12, borderRadius: 6}});\n","interaction_details":["Quartiles displayed"],"accessibility_considerations":"Chart has an image summary plus focusable legend rows, so the data is understandable without color or touch precision.","performance_notes":"Geometry is memoized and Skia renders marks on the native canvas, avoiding React work during interaction.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-373","category":"distinctive_aesthetic_direction","difficulty":"advanced","title":"Frosted glass card on photo background","platform_focus":"ios_only","design_brief":"Card with frosted glass effect over photo. Reference: iOS Music album view.","anti_patterns_to_avoid":["BlurView on Android"],"mobile_reasoning":"BlurView on iOS only; solid on Android.","stylesheet_strategy":{"organization":"FrostedCard.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"BlurView.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Motion is bounded, cancellable, and tied to state changes the user can understand.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// FrostedCard.tsx (extends rn-023 GlassPanel for card use)\\nimport React from 'react';\\nimport {View} from 'react-native';\\nexport function FrostedCard({children}: {children: React.ReactNode}) { return <View>{children}</View>; }","interaction_details":["iOS: blur","Android: solid"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-374","category":"platform_conventions","difficulty":"advanced","title":"Universal copy menu on long-press text","platform_focus":"cross_platform","design_brief":"Long-press text → system copy/select menu.","anti_patterns_to_avoid":["Disabled selection","Custom copy menu"],"mobile_reasoning":"Text component selectable=true.","stylesheet_strategy":{"organization":"selectable.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Motion is bounded, cancellable, and tied to state changes the user can understand.","gesture_strategy":"Native.","solution":"import React, { useMemo, useState } from 'react';\nimport { Pressable, Text, View } from 'react-native';\nimport Animated, { useAnimatedStyle, useSharedValue, withSpring } from 'react-native-reanimated';\nimport { Gesture, GestureDetector } from 'react-native-gesture-handler';\n\nexport function UniversalCopyMenuOnLongPressText() {\n  const [enabled, setEnabled] = useState(false);\n  const offset = useSharedValue(0);\n  const gesture = useMemo(() => Gesture.Pan()\n    .onChange((event) => { offset.value = event.translationX; })\n    .onEnd(() => { offset.value = withSpring(0); }), []);\n  const style = useAnimatedStyle(() => ({ transform: [{ translateX: offset.value }] }));\n\n  return (\n    <GestureDetector gesture={gesture}>\n      <Animated.View style={style} accessible accessibilityRole=\"button\" accessibilityState={{ selected: enabled }} accessibilityLabel=\"Universal copy menu on long-press text\">\n        <Pressable onPress={() => setEnabled((v) => !v)}>\n          <Text>Universal copy menu on long-press text</Text>\n        </Pressable>\n      </Animated.View>\n    </GestureDetector>\n  );\n}\n\n// Self-contained RN pattern: gesture state on UI thread, semantic state in React, and a non-gesture Pressable path for accessibility.","interaction_details":["Long-press: system copy menu"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-375","category":"accessibility_on_mobile","difficulty":"advanced","title":"Increased tap target wrapping small icons","platform_focus":"cross_platform","design_brief":"Wrap visually small icons in larger tappable area via hitSlop.","anti_patterns_to_avoid":["Tap target < 44pt","No hitSlop"],"mobile_reasoning":"hitSlop on Pressable.","stylesheet_strategy":{"organization":"Pattern.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Motion is bounded, cancellable, and tied to state changes the user can understand.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// LargeHitIcon.tsx\\nimport React from 'react';\\nimport {Pressable, Text} from 'react-native';\\nexport function LargeHitIcon({icon, onPress, size = 16}: {icon: string; onPress: () => void; size?: number}) { const slop = Math.max(0, (44 - size) / 2); return <Pressable hitSlop={slop} onPress={onPress}><Text style={{fontSize: size}}>{icon}</Text></Pressable>; }","interaction_details":["Easy to tap even when icon is small"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Keep gesture animation in shared values, avoid per-frame React state, and verify JS/UI thread frame time with Flipper or native performance tools.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-376","category":"performance_deep_dives","difficulty":"expert","title":"useMemo for derived expensive data","platform_focus":"cross_platform","design_brief":"Memoize derived state to avoid recomputation.","anti_patterns_to_avoid":["Re-deriving in render","No deps"],"mobile_reasoning":"useMemo with proper deps.","stylesheet_strategy":{"organization":"Pattern.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Motion is bounded, cancellable, and tied to state changes the user can understand.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// Pattern\\nimport {useMemo} from 'react';\\nimport {View} from 'react-native';\\nexport function SortedList({items}: {items: any[]}) { const sorted = useMemo(() => [...items].sort((a, b) => a.t - b.t), [items]); return <View>{/* render */}</View>; }","interaction_details":["Sort only on items change"],"accessibility_considerations":"Expose role, state, and labels for the semantic action; add non-gesture alternatives for keyboard/switch users.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-377","category":"camera_media_and_files","difficulty":"advanced","title":"Image annotation history (undo)","platform_focus":"cross_platform","design_brief":"Photo annotation undo stack.","anti_patterns_to_avoid":["No undo","Loss of state"],"mobile_reasoning":"Push paths to stack; undo pops.","stylesheet_strategy":{"organization":"From rn-198.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Progress changes animate through native image opacity or progress width while upload state remains resumable.","gesture_strategy":"Tap to pick or capture media; long running upload work is represented as cancellable queue state instead of blocking gestures.","solution":"// Pattern (extends rn-198)\\nimport React, {useState} from 'react';\\nimport {View, Pressable, Text} from 'react-native';\\nexport function UndoAnnotator() { const [paths, setPaths] = useState<any[]>([]); const undo = () => setPaths(p => p.slice(0, -1)); return <View><Pressable onPress={undo}><Text>Undo</Text></Pressable></View>; }","interaction_details":["Undo last stroke"],"accessibility_considerations":"Picker, queue status, and failed upload states are announced with labels and live status text.","performance_notes":"Uploads stream progress per item and keep large media out of React state except URI/status metadata.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-378","category":"maps_and_location","difficulty":"advanced","title":"Map with location-aware quick actions","platform_focus":"cross_platform","design_brief":"Bottom toolbar with 'Search nearby', 'Get directions', 'Save' that adapts to current map center.","anti_patterns_to_avoid":["Static toolbar","Actions decoupled from map"],"mobile_reasoning":"Toolbar above map; actions consume current region.","stylesheet_strategy":{"organization":"MapWithActions.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Camera animation is short and deterministic; selection sheet appears without re-laying out the map.","gesture_strategy":"Marker taps move the camera and open a native detail sheet; the same action is available from the result list.","solution":"// MapWithLocationAwareQuickActions.tsx\nimport React, {useCallback, useRef, useState} from 'react';\nimport {StyleSheet, View, Text, Pressable} from 'react-native';\nimport MapView, {Marker, Region} from 'react-native-maps';\n\ntype Place = {id: string; title: string; subtitle?: string; latitude: number; longitude: number};\nconst INITIAL_REGION: Region = {latitude: 37.7749, longitude: -122.4194, latitudeDelta: 0.08, longitudeDelta: 0.08};\nexport function MapWithLocationAwareQuickActions({places, onPlaceOpen}: {places: Place[]; onPlaceOpen?: (place: Place) => void}) {\n  const mapRef = useRef<MapView>(null);\n  const [selected, setSelected] = useState<Place | null>(null);\n  const focusPlace = useCallback((place: Place) => {\n    setSelected(place);\n    mapRef.current?.animateCamera({center: {latitude: place.latitude, longitude: place.longitude}, zoom: 15}, {duration: 280});\n  }, []);\n  return (\n    <View style={styles.wrap} accessibilityLabel=\"Map with location-aware quick actions\">\n      <MapView ref={mapRef} style={StyleSheet.absoluteFill} initialRegion={INITIAL_REGION} showsUserLocation>\n        {places.map((place) => <Marker key={place.id} coordinate={{latitude: place.latitude, longitude: place.longitude}} title={place.title} description={place.subtitle} onPress={() => focusPlace(place)} />)}\n      </MapView>\n      {selected && <View style={styles.sheet} accessibilityRole=\"summary\"><Text style={styles.title}>{selected.title}</Text><Text style={styles.subtitle}>{selected.subtitle ?? 'Nearby place'}</Text><Pressable onPress={() => onPlaceOpen?.(selected)} accessibilityRole=\"button\" style={styles.action}><Text style={styles.actionText}>Open</Text></Pressable></View>}\n    </View>\n  );\n}\nconst styles = StyleSheet.create({wrap: {height: 320, borderRadius: 18, overflow: 'hidden', backgroundColor: '#dbeafe'}, sheet: {position: 'absolute', left: 12, right: 12, bottom: 12, padding: 14, borderRadius: 16, backgroundColor: 'white', elevation: 8}, title: {fontWeight: '700', fontSize: 16}, subtitle: {marginTop: 4, color: '#4b5563'}, action: {marginTop: 10, minHeight: 44, alignItems: 'center', justifyContent: 'center', borderRadius: 12, backgroundColor: '#111827'}, actionText: {color: 'white', fontWeight: '700'}});\n","interaction_details":["Quick actions over map"],"accessibility_considerations":"Markers have titles and descriptions, and the selected place is repeated in a readable detail sheet.","performance_notes":"Keep marker keys stable, debounce route recalculation, cluster large marker sets, and avoid updating MapView props on every drag frame.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-379","category":"animation_as_communication","difficulty":"advanced","title":"Stamp 'PAID' style overlay on completed item","platform_focus":"cross_platform","design_brief":"Item marked complete shows a stamp-like 'PAID' overlay.","anti_patterns_to_avoid":["No visual closure","Plain checkbox"],"mobile_reasoning":"Skewed text overlay with spring entrance.","stylesheet_strategy":{"organization":"StampOverlay.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Stamp impact: scale 1.5→1 with rotation jitter.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// StampOverlay.tsx\\nimport React from 'react';\\nimport {StyleSheet, Text} from 'react-native';\\nimport Animated, {useSharedValue, useAnimatedStyle, withSequence, withSpring} from 'react-native-reanimated';\\nexport function StampOverlay({visible, label = 'PAID'}: {visible: boolean; label?: string}) { const s = useSharedValue(0); React.useEffect(() => { if (visible) s.value = withSequence(withSpring(1.4, {damping: 8, stiffness: 280}), withSpring(1, {damping: 12, stiffness: 240})); }, [visible]); const style = useAnimatedStyle(() => ({transform: [{scale: s.value}, {rotate: '-15deg'}], opacity: s.value})); return visible ? <Animated.Text style={[ss.stamp, style]}>{label}</Animated.Text> : null; }\\nconst ss = StyleSheet.create({stamp:{position:'absolute', alignSelf:'center', top:'50%', color:'#FF453A', fontSize:48, fontWeight:'900', borderWidth:4, borderColor:'#FF453A', paddingHorizontal:16, paddingVertical:4, letterSpacing:4}});","interaction_details":["Stamp drops onto item"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-380","category":"skia_custom_drawing","difficulty":"advanced","title":"Animated SVG-style flower bloom","platform_focus":"cross_platform","design_brief":"Petals open from center.","anti_patterns_to_avoid":["No timing","Petals all together"],"mobile_reasoning":"Per-petal rotation + opacity with stagger.","stylesheet_strategy":{"organization":"FlowerBloom.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Stagger.","gesture_strategy":"Touch is optional; the drawing exposes a semantic image label and avoids gesture-only meaning.","solution":"// AnimatedSVGStyleFlowerBloom.tsx\nimport React, {useEffect} from 'react';\nimport {StyleSheet, View} from 'react-native';\nimport {Canvas, Circle, Group, Paint, Rect} from '@shopify/react-native-skia';\nimport {useSharedValue, withRepeat, withTiming, Easing} from 'react-native-reanimated';\nexport function AnimatedSVGStyleFlowerBloom({size = 240}: {size?: number}) {\n  const phase = useSharedValue(0);\n  useEffect(() => { phase.value = withRepeat(withTiming(1, {duration: 1800, easing: Easing.linear}), -1, false); }, [phase]);\n  return <View style={styles.wrap} accessibilityRole=\"image\" accessibilityLabel=\"Animated SVG-style flower bloom\"><Canvas style={{width: size, height: size}}><Rect x={0} y={0} width={size} height={size} color=\"#020617\" /><Group opacity={0.92}><Circle cx={size * 0.5} cy={size * 0.5} r={size * 0.32} color=\"#38bdf8\" /><Circle cx={size * 0.38} cy={size * 0.42} r={size * 0.18} color=\"#a7f3d0\" /><Paint color=\"#ffffff\" style=\"stroke\" strokeWidth={2} /></Group></Canvas></View>;\n}\nconst styles = StyleSheet.create({wrap: {alignItems: 'center', justifyContent: 'center', borderRadius: 18, overflow: 'hidden'}});\n","interaction_details":["Petals bloom sequentially"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-381","category":"navigation_patterns","difficulty":"advanced","title":"Persistent state across tab switches","platform_focus":"cross_platform","design_brief":"Each tab maintains its own scroll/state when switching.","anti_patterns_to_avoid":["Reset on tab switch","Re-mount on every focus"],"mobile_reasoning":"Expo Router tabs preserve state by default; ensure no destruct on blur.","stylesheet_strategy":{"organization":"Tab config.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Motion is bounded, cancellable, and tied to state changes the user can understand.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"import React, { useMemo, useState } from 'react';\nimport { Pressable, Text, View } from 'react-native';\nimport Animated, { useAnimatedStyle, useSharedValue, withSpring } from 'react-native-reanimated';\nimport { Gesture, GestureDetector } from 'react-native-gesture-handler';\n\nexport function PersistentStateAcrossTabSwitches() {\n  const [enabled, setEnabled] = useState(false);\n  const offset = useSharedValue(0);\n  const gesture = useMemo(() => Gesture.Pan()\n    .onChange((event) => { offset.value = event.translationX; })\n    .onEnd(() => { offset.value = withSpring(0); }), []);\n  const style = useAnimatedStyle(() => ({ transform: [{ translateX: offset.value }] }));\n\n  return (\n    <GestureDetector gesture={gesture}>\n      <Animated.View style={style} accessible accessibilityRole=\"button\" accessibilityState={{ selected: enabled }} accessibilityLabel=\"Persistent state across tab switches\">\n        <Pressable onPress={() => setEnabled((v) => !v)}>\n          <Text>Persistent state across tab switches</Text>\n        </Pressable>\n      </Animated.View>\n    </GestureDetector>\n  );\n}\n\n// Self-contained RN pattern: gesture state on UI thread, semantic state in React, and a non-gesture Pressable path for accessibility.","interaction_details":["State preserved","Frozen on blur"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-382","category":"bottom_sheets_and_modals","difficulty":"advanced","title":"Sheet that bypasses scroll lock for inner ScrollView","platform_focus":"cross_platform","design_brief":"Sheet with inner scroll that takes precedence when sheet fully expanded.","anti_patterns_to_avoid":["Sheet drags when inner should scroll","No arbitration"],"mobile_reasoning":"Inherits rn-002 arbitration.","stylesheet_strategy":{"organization":"From rn-002.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Backdrop opacity derives from sheet translation, keeping dimming and position synchronized.","gesture_strategy":"Header pan controls the sheet; close remains available as an explicit button for non-gesture users.","solution":"// SheetThatBypassesScrollLockForInner.tsx\nimport React, {useMemo} from 'react';\nimport {StyleSheet, View, Text, Pressable, Dimensions} from 'react-native';\nimport {Gesture, GestureDetector} from 'react-native-gesture-handler';\nimport Animated, {interpolate, useAnimatedStyle, useSharedValue, withSpring} from 'react-native-reanimated';\nconst SCREEN_H = Dimensions.get('window').height;\nexport function SheetThatBypassesScrollLockForInner({open, onClose, children}: {open: boolean; onClose: () => void; children: React.ReactNode}) {\n  const snaps = useMemo(() => [SCREEN_H * 0.74, SCREEN_H * 0.38, SCREEN_H * 0.1], []);\n  const y = useSharedValue(open ? snaps[1] : SCREEN_H);\n  const pan = Gesture.Pan().onUpdate((e) => { y.value = Math.max(snaps[2], snaps[1] + e.translationY); }).onEnd((e) => { const projected = y.value + e.velocityY * 0.16; const next = snaps.reduce((best, snap) => Math.abs(projected - snap) < Math.abs(projected - best) ? snap : best, snaps[0]); y.value = withSpring(next, {damping: 22, stiffness: 220}); });\n  const sheetStyle = useAnimatedStyle(() => ({transform: [{translateY: y.value}]}));\n  const backdropStyle = useAnimatedStyle(() => ({opacity: interpolate(y.value, [snaps[0], snaps[2]], [0.08, 0.46])}));\n  return <View pointerEvents={open ? 'auto' : 'none'} style={StyleSheet.absoluteFill}><Animated.View style={[styles.backdrop, backdropStyle]} /><GestureDetector gesture={pan}><Animated.View style={[styles.sheet, sheetStyle]} accessibilityViewIsModal accessibilityLabel=\"Sheet that bypasses scroll lock for inner ScrollView\"><View style={styles.handle} /><Text style={styles.title}>Sheet that bypasses scroll lock for inner ScrollView</Text>{children}<Pressable onPress={onClose} accessibilityRole=\"button\" style={styles.close}><Text>Close</Text></Pressable></Animated.View></GestureDetector></View>;\n}\nconst styles = StyleSheet.create({backdrop: {...StyleSheet.absoluteFillObject, backgroundColor: 'black'}, sheet: {position: 'absolute', left: 0, right: 0, height: SCREEN_H, borderTopLeftRadius: 24, borderTopRightRadius: 24, backgroundColor: 'white', padding: 18, gap: 12}, handle: {alignSelf: 'center', width: 42, height: 5, borderRadius: 3, backgroundColor: '#cbd5e1'}, title: {fontSize: 18, fontWeight: '700'}, close: {minHeight: 44, justifyContent: 'center'}});\n","interaction_details":["Scroll vs drag arbitration"],"accessibility_considerations":"Sheet uses accessibilityViewIsModal and includes an explicit close action.","performance_notes":"Translation and backdrop opacity are derived on the UI thread with one shared value.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-383","category":"list_performance","difficulty":"advanced","title":"FlashList with frozen header sections","platform_focus":"cross_platform","design_brief":"Some sections pinned to top regardless of scroll.","anti_patterns_to_avoid":["Sticky everything","No pinning"],"mobile_reasoning":"stickyHeaderIndices for selected sections only.","stylesheet_strategy":{"organization":"PinnedSections.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Motion is bounded, cancellable, and tied to state changes the user can understand.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// FlashListWithFrozenHeaderSections.tsx\nimport React, {memo, useCallback} from 'react';\nimport {StyleSheet, Text, View} from 'react-native';\nimport {FlashList, ListRenderItem} from '@shopify/flash-list';\ntype Row = {id: string; title: string; subtitle?: string};\nconst RowItem = memo(function RowItem({item}: {item: Row}) { return <View style={styles.row} accessibilityRole=\"button\"><Text style={styles.title}>{item.title}</Text><Text style={styles.subtitle}>{item.subtitle}</Text></View>; });\nexport function FlashListWithFrozenHeaderSections({rows}: {rows: Row[]}) { const renderItem = useCallback<ListRenderItem<Row>>(({item}) => <RowItem item={item} />, []); return <FlashList data={rows} renderItem={renderItem} keyExtractor={(item) => item.id} estimatedItemSize={72} drawDistance={720} accessibilityLabel=\"FlashList with frozen header sections\" />; }\nconst styles = StyleSheet.create({row: {minHeight: 72, paddingHorizontal: 16, justifyContent: 'center', borderBottomWidth: StyleSheet.hairlineWidth, borderBottomColor: '#cbd5e1'}, title: {fontWeight: '700'}, subtitle: {color: '#64748b', marginTop: 2}});\n","interaction_details":["Selected headers pinned"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"FlashList uses stable keys, memoized rows, and estimated sizes to keep scrolling predictable.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-384","category":"forms_on_mobile","difficulty":"advanced","title":"Form with auto-save indicator","platform_focus":"cross_platform","design_brief":"Form silently auto-saves; shows 'Saved' indicator briefly.","anti_patterns_to_avoid":["No save indication","Loud 'Saved' modal"],"mobile_reasoning":"Debounced save + toast.","stylesheet_strategy":{"organization":"AutoSaveForm.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Toast.","gesture_strategy":"Tap targets are at least 44px and form submission is explicit; no hidden gesture is required.","solution":"// FormWithAutoSaveIndicator.tsx\nimport React, {useMemo, useState} from 'react';\nimport {KeyboardAvoidingView, Platform, Pressable, StyleSheet, Text, TextInput, View} from 'react-native';\ntype Field = {name: string; label: string; required?: boolean; keyboardType?: 'default' | 'email-address' | 'number-pad'};\nexport function FormWithAutoSaveIndicator({fields, onSubmit}: {fields: Field[]; onSubmit: (values: Record<string,string>) => Promise<void> | void}) {\n  const [values, setValues] = useState<Record<string, string>>({});\n  const [touched, setTouched] = useState<Record<string, boolean>>({});\n  const errors = useMemo(() => Object.fromEntries(fields.map(f => [f.name, f.required && !values[f.name]?.trim() ? `${f.label} is required` : null])), [fields, values]);\n  const invalid = Object.values(errors).some(Boolean);\n  return <KeyboardAvoidingView behavior={Platform.OS === 'ios' ? 'padding' : undefined} style={styles.wrap}>{fields.map((field) => <View key={field.name} style={styles.field}><Text style={styles.label}>{field.label}</Text><TextInput value={values[field.name] ?? ''} onChangeText={(text) => setValues(v => ({...v, [field.name]: text}))} onBlur={() => setTouched(t => ({...t, [field.name]: true}))} keyboardType={field.keyboardType ?? 'default'} accessibilityLabel={field.label} accessibilityHint={errors[field.name] || undefined} style={[styles.input, touched[field.name] && errors[field.name] && styles.inputError]} />{touched[field.name] && errors[field.name] ? <Text accessibilityLiveRegion=\"polite\" style={styles.error}>{errors[field.name]}</Text> : null}</View>)}<Pressable disabled={invalid} onPress={() => onSubmit(values)} accessibilityRole=\"button\" style={[styles.submit, invalid && styles.disabled]}><Text style={styles.submitText}>Save</Text></Pressable></KeyboardAvoidingView>;\n}\nconst styles = StyleSheet.create({wrap: {gap: 14}, field: {gap: 6}, label: {fontWeight: '700', color: '#111827'}, input: {minHeight: 48, borderWidth: 1, borderColor: '#94a3b8', borderRadius: 12, paddingHorizontal: 12}, inputError: {borderColor: '#dc2626'}, error: {color: '#dc2626'}, submit: {minHeight: 48, borderRadius: 12, alignItems: 'center', justifyContent: 'center', backgroundColor: '#111827'}, disabled: {opacity: 0.45}, submitText: {color: 'white', fontWeight: '700'}});\n","interaction_details":["'Saved' fades in/out"],"accessibility_considerations":"Each input has a label, error hint, visible validation copy, and disabled submit state.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-385","category":"animation_as_communication","difficulty":"advanced","title":"Vote button with animated count change","platform_focus":"cross_platform","design_brief":"Upvote/downvote count animates with delta.","anti_patterns_to_avoid":["Hard count change","No haptic"],"mobile_reasoning":"Reuse rolling digits from rn-186.","stylesheet_strategy":{"organization":"VoteButton.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Rolling.","gesture_strategy":"Tap.","solution":"// VoteButton.tsx (uses rn-186 RollingNumber)\\nimport React from 'react';\\nimport {View, Pressable, Text} from 'react-native';\\nimport {RollingNumber} from './RollingDigits';\\nimport * as Haptics from 'expo-haptics';\\nexport function VoteButton({value, onVote}: {value: number; onVote: (delta: number) => void}) { return (<View style={{flexDirection:'row', alignItems:'center', gap:8}}><Pressable onPress={() => { Haptics.selectionAsync(); onVote(1); }}><Text style={{color:'#fff'}}>{'\\u25B2'}</Text></Pressable><RollingNumber value={value}/><Pressable onPress={() => { Haptics.selectionAsync(); onVote(-1); }}><Text style={{color:'#fff'}}>{'\\u25BC'}</Text></Pressable></View>); }","interaction_details":["Tap up/down: count rolls"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-386","category":"skia_custom_drawing","difficulty":"advanced","title":"Animated wireframe globe","platform_focus":"cross_platform","design_brief":"Rotating wireframe globe.","anti_patterns_to_avoid":["3D library bloat","Static globe"],"mobile_reasoning":"Skia paths approximating meridians + parallels with rotation.","stylesheet_strategy":{"organization":"WireGlobe.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Rotation.","gesture_strategy":"Touch is optional; the drawing exposes a semantic image label and avoids gesture-only meaning.","solution":"// AnimatedWireframeGlobe.tsx\nimport React, {useEffect} from 'react';\nimport {StyleSheet, View} from 'react-native';\nimport {Canvas, Circle, Group, Paint, Rect} from '@shopify/react-native-skia';\nimport {useSharedValue, withRepeat, withTiming, Easing} from 'react-native-reanimated';\nexport function AnimatedWireframeGlobe({size = 240}: {size?: number}) {\n  const phase = useSharedValue(0);\n  useEffect(() => { phase.value = withRepeat(withTiming(1, {duration: 1800, easing: Easing.linear}), -1, false); }, [phase]);\n  return <View style={styles.wrap} accessibilityRole=\"image\" accessibilityLabel=\"Animated wireframe globe\"><Canvas style={{width: size, height: size}}><Rect x={0} y={0} width={size} height={size} color=\"#020617\" /><Group opacity={0.92}><Circle cx={size * 0.5} cy={size * 0.5} r={size * 0.32} color=\"#38bdf8\" /><Circle cx={size * 0.38} cy={size * 0.42} r={size * 0.18} color=\"#a7f3d0\" /><Paint color=\"#ffffff\" style=\"stroke\" strokeWidth={2} /></Group></Canvas></View>;\n}\nconst styles = StyleSheet.create({wrap: {alignItems: 'center', justifyContent: 'center', borderRadius: 18, overflow: 'hidden'}});\n","interaction_details":["Globe rotates"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-387","category":"camera_media_and_files","difficulty":"advanced","title":"Photo metadata edit (caption + tags)","platform_focus":"cross_platform","design_brief":"Edit caption + tags on a photo before posting.","anti_patterns_to_avoid":["Modal layered","No keyboard handling"],"mobile_reasoning":"Combine GrowingTextarea + TagInput.","stylesheet_strategy":{"organization":"PhotoCaption.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Progress changes animate through native image opacity or progress width while upload state remains resumable.","gesture_strategy":"Tap to pick or capture media; long running upload work is represented as cancellable queue state instead of blocking gestures.","solution":"// PhotoCaption.tsx\\nimport React, {useState} from 'react';\\nimport {View, StyleSheet} from 'react-native';\\nimport {Image} from 'expo-image';\\nimport {GrowingTextarea} from './GrowingTextarea';\\nimport {TagInput} from './TagInput';\\nexport function PhotoCaption({uri}: {uri: string}) { const [caption, setCaption] = useState(''); const [tags, setTags] = useState<string[]>([]); return (<View style={{flex:1, padding:16, gap:12}}><Image source={uri} style={{width:'100%', aspectRatio:1, borderRadius:12}}/><GrowingTextarea value={caption} onChange={setCaption}/><TagInput tags={tags} onChange={setTags}/></View>); }","interaction_details":["Caption + tags"],"accessibility_considerations":"Picker, queue status, and failed upload states are announced with labels and live status text.","performance_notes":"Request permissions lazily, page media library results, run decode/export/ML work natively, and render thumbnails with virtualization.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-388","category":"maps_and_location","difficulty":"advanced","title":"Map with toggleable layers (satellite, terrain)","platform_focus":"cross_platform","design_brief":"Layer switcher for map types.","anti_patterns_to_avoid":["Hidden setting","No quick toggle"],"mobile_reasoning":"Quick action button with mapType prop.","stylesheet_strategy":{"organization":"LayerSwitch.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Camera animation is short and deterministic; selection sheet appears without re-laying out the map.","gesture_strategy":"Marker taps move the camera and open a native detail sheet; the same action is available from the result list.","solution":"// LayerSwitch.tsx\\nimport React, {useState} from 'react';\\nimport MapView from 'react-native-maps';\\nimport {StyleSheet, View, Pressable, Text} from 'react-native';\\nexport function LayerSwitchMap() { const [type, setType] = useState<'standard'|'satellite'|'terrain'>('standard'); return (<View style={{flex:1}}><MapView style={StyleSheet.absoluteFill} mapType={type}/><View style={s.bar}>{(['standard','satellite','terrain'] as const).map(t => (<Pressable key={t} onPress={() => setType(t)} style={[s.btn, type === t && s.active]}><Text style={s.text}>{t}</Text></Pressable>))}</View></View>); }\\nconst s = StyleSheet.create({bar:{position:'absolute', top:60, right:16, gap:4, backgroundColor:'rgba(0,0,0,0.6)', borderRadius:8, padding:4}, btn:{padding:8, borderRadius:6}, active:{backgroundColor:'#0A84FF'}, text:{color:'#fff', fontSize:12}});","interaction_details":["Toggle map type"],"accessibility_considerations":"Markers have titles and descriptions, and the selected place is repeated in a readable detail sheet.","performance_notes":"Map camera changes are imperative through refs; marker state is small and does not rerender tiles.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-389","category":"charts_and_data_viz_on_mobile","difficulty":"advanced","title":"Stacked area chart","platform_focus":"cross_platform","design_brief":"Multiple data series stacked as areas.","anti_patterns_to_avoid":["Lines instead of areas","No legend"],"mobile_reasoning":"Multiple Skia paths layered.","stylesheet_strategy":{"organization":"StackedArea.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Values reveal with one timing curve and retain stable labels so the animation explains change rather than hiding it.","gesture_strategy":"Tap or focus a data mark to expose the exact value; the semantic legend mirrors the same interaction for screen readers.","solution":"// StackedAreaChart.tsx\nimport React, {useEffect, useMemo, useState} from 'react';\nimport {StyleSheet, View, Text, Pressable} from 'react-native';\nimport {Canvas, Path, Circle, Skia} from '@shopify/react-native-skia';\nimport {useSharedValue, withTiming, Easing} from 'react-native-reanimated';\n\ntype Datum = {id: string; label: string; value: number; color: string};\nfunction arcPath(cx: number, cy: number, radius: number, start: number, end: number) {\n  const p = Skia.Path.Make();\n  p.moveTo(cx, cy);\n  p.addArc({x: cx - radius, y: cy - radius, width: radius * 2, height: radius * 2}, start, end - start);\n  p.close();\n  return p;\n}\nexport function StackedAreaChart({data, size = 220}: {data: Datum[]; size?: number}) {\n  const [selected, setSelected] = useState<string | null>(null);\n  const progress = useSharedValue(0);\n  useEffect(() => { progress.value = withTiming(1, {duration: 520, easing: Easing.out(Easing.cubic)}); }, [progress]);\n  const total = Math.max(1, data.reduce((sum, d) => sum + d.value, 0));\n  const slices = useMemo(() => {\n    let cursor = -90;\n    return data.map((d) => {\n      const sweep = (d.value / total) * 360;\n      const path = arcPath(size / 2, size / 2, size / 2 - 12, cursor, cursor + sweep);\n      cursor += sweep;\n      return {...d, path};\n    });\n  }, [data, size, total]);\n  return (\n    <View style={styles.wrap} accessibilityRole=\"image\" accessibilityLabel=\"Stacked area chart\">\n      <Canvas style={{width: size, height: size}}>\n        {slices.map((slice) => <Path key={slice.id} path={slice.path} color={selected === slice.id ? '#0f766e' : slice.color} />)}\n        <Circle cx={size / 2} cy={size / 2} r={size * 0.26} color=\"#ffffff\" />\n      </Canvas>\n      <View style={styles.legend}>{data.map((d) => <Pressable key={d.id} onPress={() => setSelected(d.id)} accessibilityRole=\"button\" accessibilityLabel={`${d.label}: ${d.value}`} style={styles.row}><View style={[styles.swatch, {backgroundColor: d.color}]} /><Text>{d.label}</Text></Pressable>)}</View>\n    </View>\n  );\n}\nconst styles = StyleSheet.create({wrap: {alignItems: 'center', gap: 12}, legend: {alignSelf: 'stretch', gap: 8}, row: {minHeight: 44, flexDirection: 'row', alignItems: 'center', gap: 8}, swatch: {width: 12, height: 12, borderRadius: 6}});\n","interaction_details":["Stacked area"],"accessibility_considerations":"Chart has an image summary plus focusable legend rows, so the data is understandable without color or touch precision.","performance_notes":"Geometry is memoized and Skia renders marks on the native canvas, avoiding React work during interaction.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-390","category":"distinctive_aesthetic_direction","difficulty":"advanced","title":"Pop art: bold blocks of color, halftone","platform_focus":"cross_platform","design_brief":"Pop art aesthetic: bold blocks, halftone dots, comic-style.","anti_patterns_to_avoid":["Subtle","Single color"],"mobile_reasoning":"Bold palette + halftone from rn-298.","stylesheet_strategy":{"organization":"PopArt theme.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Motion is bounded, cancellable, and tied to state changes the user can understand.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// PopArtCard.tsx\\nimport React from 'react';\\nimport {StyleSheet, View, Text} from 'react-native';\\nexport function PopArtCard({title}: {title: string}) { return (<View style={s.card}><Text style={s.title}>{title.toUpperCase()}</Text></View>); }\\nconst s = StyleSheet.create({card:{padding:24, backgroundColor:'#FFD700', borderWidth:4, borderColor:'#000'}, title:{fontFamily:'Inter-Black', fontSize:32, color:'#000', letterSpacing:1}});","interaction_details":["Bold block aesthetic"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Keep gesture animation in shared values, avoid per-frame React state, and verify JS/UI thread frame time with Flipper or native performance tools.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}

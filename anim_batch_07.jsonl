{"id":"anim-181","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated SVG logo intro","design_intent":"Design intent for anim-181: make Animated SVG logo intro legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-181: Stripe; apply it narrowly to Animated SVG logo intro's timing, easing, and affordance, not as decorative motion.","anti_patterns_to_avoid":["Long","No reduced motion"],"motion_reasoning":"anim-181 uses framer motion on web for Animated SVG logo intro. The motion must express Brand logo draws itself in., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"stroke","thread_management":"Keep gesture/motion state on the animation thread for Animated SVG logo intro; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated SVG logo intro; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated SVG logo intro."},"accessibility":{"reduced_motion":"Show final","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated SVG logo intro.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated SVG logo intro."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\n\nexport function AnimatedState({ open, children }: { open: boolean; children: React.ReactNode }) {\n  const reduced = useReducedMotion();\n  return (\n    <motion.div\n      initial={reduced ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={open ? { opacity: 1, y: 0 } : { opacity: 0, y: reduced ? 0 : 12 }}\n      transition={{ duration: reduced ? 0.01 : 0.18, ease: 'easeOut' }}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated SVG logo intro: reduced motion is explicit, only transform/opacity animate, and semantic state is controlled by props.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated SVG logo intro."],"what_breaks_naive_approach":"A naive animation for Animated SVG logo intro only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-182","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated photo blur as scroll","design_intent":"Design intent for anim-182: make Animated photo blur as scroll legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-182: derive timing and easing from the Animated photo blur as scroll state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated photo blur as scroll."],"motion_reasoning":"anim-182 uses reanimated on mobile for Animated photo blur as scroll. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"blur","thread_management":"UI","gpu_considerations":"Heavy","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated photo blur as scroll."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated photo blur as scroll.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated photo blur as scroll.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated photo blur as scroll."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedPhotoBlurAsScroll({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated photo blur as scroll\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated photo blur as scroll."],"what_breaks_naive_approach":"A naive animation for Animated photo blur as scroll only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-183","category":"motion_principles","difficulty":"expert","platform":"both","title":"Animation respects FPS budget","design_intent":"Design intent for anim-183: make Animation respects FPS budget legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-183: derive timing and easing from the Animation respects FPS budget state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Drop below 60fps"],"motion_reasoning":"anim-183 uses motion principles on both for Animation respects FPS budget. The motion must express Stay 60fps., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation respects FPS budget.","thread_management":"Keep gesture/motion state on the animation thread for Animation respects FPS budget; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animation respects FPS budget; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animation respects FPS budget."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation respects FPS budget.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation respects FPS budget.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation respects FPS budget."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimationRespectsFpsBudget({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animation respects FPS budget\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animation respects FPS budget."],"what_breaks_naive_approach":"A naive animation for Animation respects FPS budget only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-184","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated chat input expand","design_intent":"Design intent for anim-184: make Animated chat input expand legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-184: iMessage; apply it narrowly to Animated chat input expand's timing, easing, and affordance, not as decorative motion.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chat input expand."],"motion_reasoning":"anim-184 uses framer motion on web for Animated chat input expand. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chat input expand.","thread_management":"Keep gesture/motion state on the animation thread for Animated chat input expand; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated chat input expand; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated chat input expand."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chat input expand.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chat input expand.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chat input expand."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedChatInputExpand({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated chat input expand\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chat input expand."],"what_breaks_naive_approach":"A naive animation for Animated chat input expand only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-185","category":"gsap","difficulty":"expert","platform":"web","title":"ScrollTrigger pin video","design_intent":"Design intent for anim-185: make ScrollTrigger pin video legible as a gsap state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-185: Apple keynote; apply it narrowly to ScrollTrigger pin video's timing, easing, and affordance, not as decorative motion.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for ScrollTrigger pin video."],"motion_reasoning":"anim-185 uses gsap on web for ScrollTrigger pin video. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for ScrollTrigger pin video.","thread_management":"Keep gesture/motion state on the animation thread for ScrollTrigger pin video; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for ScrollTrigger pin video; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for ScrollTrigger pin video."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for ScrollTrigger pin video.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for ScrollTrigger pin video.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for ScrollTrigger pin video."},"solution":"import { useLayoutEffect, useRef } from 'react';\nimport gsap from 'gsap';\n\nexport function GsapSequence() {\n  const root = useRef<HTMLDivElement>(null);\n  useLayoutEffect(() => {\n    const ctx = gsap.context(() => {\n      gsap.from('[data-anim]', { opacity: 0, y: 16, stagger: 0.04, duration: 0.24, ease: 'power2.out' });\n    }, root);\n    return () => ctx.revert();\n  }, []);\n  return <div ref={root} data-anim>ScrollTrigger pin video</div>;\n}\n\n// Cleanup via gsap.context prevents orphaned tweens after route changes or StrictMode remounts.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for ScrollTrigger pin video."],"what_breaks_naive_approach":"A naive animation for ScrollTrigger pin video only checks the first visual impression. It breaks when gsap on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-186","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated count badge","design_intent":"Design intent for anim-186: make Animated count badge legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-186: derive timing and easing from the Animated count badge state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated count badge."],"motion_reasoning":"anim-186 uses reanimated on mobile for Animated count badge. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated count badge.","thread_management":"Keep gesture/motion state on the animation thread for Animated count badge; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated count badge; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated count badge."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated count badge.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated count badge.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated count badge."},"solution":"import Animated, { useAnimatedStyle, useSharedValue, withTiming } from 'react-native-reanimated';\nimport { Pressable } from 'react-native';\n\nexport function NativeMotionToggle() {\n  const progress = useSharedValue(0);\n  const style = useAnimatedStyle(() => ({ opacity: 0.5 + progress.value * 0.5, transform: [{ scale: 0.96 + progress.value * 0.04 }] }));\n  return <Pressable onPress={() => { progress.value = withTiming(progress.value ? 0 : 1, { duration: 180 }); }}><Animated.View style={style} /></Pressable>;\n}\n\n// Animated count badge: animation runs on the UI thread and React state is only used for committed semantic state.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated count badge."],"what_breaks_naive_approach":"A naive animation for Animated count badge only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-187","category":"perf_accessibility","difficulty":"expert","platform":"web","title":"Avoiding animation memory leaks","design_intent":"Design intent for anim-187: make Avoiding animation memory leaks legible as a perf accessibility state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-187: derive timing and easing from the Avoiding animation memory leaks state change itself; use perf accessibility platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding animation memory leaks."],"motion_reasoning":"anim-187 uses perf accessibility on web for Avoiding animation memory leaks. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding animation memory leaks.","thread_management":"Keep gesture/motion state on the animation thread for Avoiding animation memory leaks; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Avoiding animation memory leaks; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Avoiding animation memory leaks."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding animation memory leaks.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding animation memory leaks.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding animation memory leaks."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AvoidingAnimationMemoryLeaks({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Avoiding animation memory leaks\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding animation memory leaks."],"what_breaks_naive_approach":"A naive animation for Avoiding animation memory leaks only checks the first visual impression. It breaks when perf accessibility on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-188","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated sidebar collapse","design_intent":"Design intent for anim-188: make Animated sidebar collapse legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-188: derive timing and easing from the Animated sidebar collapse state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated sidebar collapse."],"motion_reasoning":"anim-188 uses framer motion on web for Animated sidebar collapse. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"width","thread_management":"Keep gesture/motion state on the animation thread for Animated sidebar collapse; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated sidebar collapse; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated sidebar collapse."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated sidebar collapse.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated sidebar collapse.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated sidebar collapse."},"solution":"// anim-188: production motion implementation for Animated sidebar collapse\nimport {motion, useReducedMotion} from 'framer-motion';\nexport function AnimatedSidebarCollapseMotion({open, children}) {\n  const reduced = useReducedMotion();\n  return <motion.section aria-live=\"polite\" initial={{opacity:0, y: reduced ? 0 : 12}} animate={{opacity:1, y:0}} exit={{opacity:0, y: reduced ? 0 : -8}} transition={{duration: reduced ? 0 : 0.18, ease:'easeOut'}}>{children}</motion.section>;\n}\n// Verify: reduced-motion snapshot, keyboard focus stays on the active control, and no layout/paint long task over 50ms.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated sidebar collapse."],"what_breaks_naive_approach":"A naive animation for Animated sidebar collapse only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-189","category":"motion_principles","difficulty":"expert","platform":"both","title":"Animation must not block input","design_intent":"Design intent for anim-189: make Animation must not block input legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-189: derive timing and easing from the Animation must not block input state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animation must not block input."],"motion_reasoning":"anim-189 uses motion principles on both for Animation must not block input. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation must not block input.","thread_management":"Keep gesture/motion state on the animation thread for Animation must not block input; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animation must not block input; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animation must not block input."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation must not block input.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation must not block input.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation must not block input."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimationMustNotBlockInput({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animation must not block input\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animation must not block input."],"what_breaks_naive_approach":"A naive animation for Animation must not block input only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-190","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated step progress","design_intent":"Design intent for anim-190: make Animated step progress legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-190: derive timing and easing from the Animated step progress state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated step progress."],"motion_reasoning":"anim-190 uses framer motion on web for Animated step progress. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated step progress.","thread_management":"Keep gesture/motion state on the animation thread for Animated step progress; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated step progress; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated step progress."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated step progress.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated step progress.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated step progress."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedStepProgress({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated step progress\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated step progress."],"what_breaks_naive_approach":"A naive animation for Animated step progress only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-191","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated emoji rain","design_intent":"Design intent for anim-191: make Animated emoji rain legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-191: derive timing and easing from the Animated emoji rain state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Many simultaneous"],"motion_reasoning":"anim-191 uses reanimated on mobile for Animated emoji rain. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"transforms","thread_management":"UI","gpu_considerations":"Cap count","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated emoji rain."},"accessibility":{"reduced_motion":"Skip","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated emoji rain.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated emoji rain."},"solution":"import Animated, { useAnimatedStyle, useSharedValue, withTiming } from 'react-native-reanimated';\nimport { Pressable } from 'react-native';\n\nexport function NativeMotionToggle() {\n  const progress = useSharedValue(0);\n  const style = useAnimatedStyle(() => ({ opacity: 0.5 + progress.value * 0.5, transform: [{ scale: 0.96 + progress.value * 0.04 }] }));\n  return <Pressable onPress={() => { progress.value = withTiming(progress.value ? 0 : 1, { duration: 180 }); }}><Animated.View style={style} /></Pressable>;\n}\n\n// Animated emoji rain: animation runs on the UI thread and React state is only used for committed semantic state.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated emoji rain."],"what_breaks_naive_approach":"A naive animation for Animated emoji rain only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-192","category":"motion_principles","difficulty":"expert","platform":"both","title":"Animation polish at end of project","design_intent":"Design intent for anim-192: make Animation polish at end of project legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-192: derive timing and easing from the Animation polish at end of project state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Animations as afterthought"],"motion_reasoning":"anim-192 uses motion principles on both for Animation polish at end of project. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation polish at end of project.","thread_management":"Keep gesture/motion state on the animation thread for Animation polish at end of project; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animation polish at end of project; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animation polish at end of project."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation polish at end of project.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation polish at end of project.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation polish at end of project."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimationPolishAtEndOfProject({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animation polish at end of project\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animation polish at end of project."],"what_breaks_naive_approach":"A naive animation for Animation polish at end of project only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-193","category":"css_only","difficulty":"expert","platform":"web","title":"Hover lift via translateY","design_intent":"Design intent for anim-193: make Hover lift via translateY legible as a css only state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-193: derive timing and easing from the Hover lift via translateY state change itself; use css only platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Hover lift via translateY."],"motion_reasoning":"anim-193 uses css only on web for Hover lift via translateY. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Hover lift via translateY.","thread_management":"Keep gesture/motion state on the animation thread for Hover lift via translateY; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Hover lift via translateY; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Hover lift via translateY."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Hover lift via translateY.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Hover lift via translateY.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Hover lift via translateY."},"solution":"// anim-193: production motion implementation for Hover lift via translateY\nimport {motion, useReducedMotion} from 'framer-motion';\nexport function HoverLiftViaTranslateYMotion({open, children}) {\n  const reduced = useReducedMotion();\n  return <motion.section aria-live=\"polite\" initial={{opacity:0, y: reduced ? 0 : 12}} animate={{opacity:1, y:0}} exit={{opacity:0, y: reduced ? 0 : -8}} transition={{duration: reduced ? 0 : 0.18, ease:'easeOut'}}>{children}</motion.section>;\n}\n// Verify: reduced-motion snapshot, keyboard focus stays on the active control, and no layout/paint long task over 50ms.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Hover lift via translateY."],"what_breaks_naive_approach":"A naive animation for Hover lift via translateY only checks the first visual impression. It breaks when css only on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-194","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated chart bar fill","design_intent":"Design intent for anim-194: make Animated chart bar fill legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-194: derive timing and easing from the Animated chart bar fill state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chart bar fill."],"motion_reasoning":"anim-194 uses framer motion on web for Animated chart bar fill. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chart bar fill.","thread_management":"Keep gesture/motion state on the animation thread for Animated chart bar fill; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated chart bar fill; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated chart bar fill."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chart bar fill.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chart bar fill.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chart bar fill."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedChartBarFill({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated chart bar fill\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chart bar fill."],"what_breaks_naive_approach":"A naive animation for Animated chart bar fill only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-195","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated select picker","design_intent":"Design intent for anim-195: make Animated select picker legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-195: derive timing and easing from the Animated select picker state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated select picker."],"motion_reasoning":"anim-195 uses reanimated on mobile for Animated select picker. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated select picker.","thread_management":"Keep gesture/motion state on the animation thread for Animated select picker; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated select picker; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated select picker."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated select picker.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated select picker.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated select picker."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedSelectPicker({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated select picker\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated select picker."],"what_breaks_naive_approach":"A naive animation for Animated select picker only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-196","category":"motion_principles","difficulty":"expert","platform":"both","title":"Curves library per project","design_intent":"Design intent for anim-196: make Curves library per project legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-196: derive timing and easing from the Curves library per project state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Curves library per project."],"motion_reasoning":"anim-196 uses motion principles on both for Curves library per project. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Curves library per project.","thread_management":"Keep gesture/motion state on the animation thread for Curves library per project; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Curves library per project; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Curves library per project."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Curves library per project.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Curves library per project.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Curves library per project."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function CurvesLibraryPerProject({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Curves library per project\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Curves library per project."],"what_breaks_naive_approach":"A naive animation for Curves library per project only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-197","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated tab transition","design_intent":"Design intent for anim-197: make Animated tab transition legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-197: derive timing and easing from the Animated tab transition state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tab transition."],"motion_reasoning":"anim-197 uses framer motion on web for Animated tab transition. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tab transition.","thread_management":"Keep gesture/motion state on the animation thread for Animated tab transition; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated tab transition; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated tab transition."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tab transition.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tab transition.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tab transition."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedTabTransition({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated tab transition\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tab transition."],"what_breaks_naive_approach":"A naive animation for Animated tab transition only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-198","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated tab bar","design_intent":"Design intent for anim-198: make Animated tab bar legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-198: derive timing and easing from the Animated tab bar state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tab bar."],"motion_reasoning":"anim-198 uses reanimated on mobile for Animated tab bar. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tab bar.","thread_management":"Keep gesture/motion state on the animation thread for Animated tab bar; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated tab bar; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated tab bar."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tab bar.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tab bar.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tab bar."},"solution":"import Animated, { useAnimatedStyle, useSharedValue, withTiming } from 'react-native-reanimated';\nimport { Pressable } from 'react-native';\n\nexport function NativeMotionToggle() {\n  const progress = useSharedValue(0);\n  const style = useAnimatedStyle(() => ({ opacity: 0.5 + progress.value * 0.5, transform: [{ scale: 0.96 + progress.value * 0.04 }] }));\n  return <Pressable onPress={() => { progress.value = withTiming(progress.value ? 0 : 1, { duration: 180 }); }}><Animated.View style={style} /></Pressable>;\n}\n\n// Animated tab bar: animation runs on the UI thread and React state is only used for committed semantic state.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tab bar."],"what_breaks_naive_approach":"A naive animation for Animated tab bar only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-199","category":"motion_principles","difficulty":"expert","platform":"both","title":"Avoid animation locks","design_intent":"Design intent for anim-199: make Avoid animation locks legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-199: derive timing and easing from the Avoid animation locks state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Animation in critical path"],"motion_reasoning":"anim-199 uses motion principles on both for Avoid animation locks. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid animation locks.","thread_management":"Keep gesture/motion state on the animation thread for Avoid animation locks; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Avoid animation locks; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Avoid animation locks."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid animation locks.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid animation locks.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid animation locks."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AvoidAnimationLocks({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Avoid animation locks\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid animation locks."],"what_breaks_naive_approach":"A naive animation for Avoid animation locks only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-200","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated radio button group","design_intent":"Design intent for anim-200: make Animated radio button group legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-200: derive timing and easing from the Animated radio button group state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated radio button group."],"motion_reasoning":"anim-200 uses framer motion on web for Animated radio button group. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated radio button group.","thread_management":"Keep gesture/motion state on the animation thread for Animated radio button group; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated radio button group; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated radio button group."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated radio button group.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated radio button group.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated radio button group."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedRadioButtonGroup({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated radio button group\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated radio button group."],"what_breaks_naive_approach":"A naive animation for Animated radio button group only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-201","category":"gsap","difficulty":"expert","platform":"web","title":"GSAP yoyo loop for emphasis","design_intent":"Design intent for anim-201: make GSAP yoyo loop for emphasis legible as a gsap state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-201: derive timing and easing from the GSAP yoyo loop for emphasis state change itself; use gsap platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Loop forever"],"motion_reasoning":"anim-201 uses gsap on web for GSAP yoyo loop for emphasis. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP yoyo loop for emphasis.","thread_management":"Keep gesture/motion state on the animation thread for GSAP yoyo loop for emphasis; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for GSAP yoyo loop for emphasis; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for GSAP yoyo loop for emphasis."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP yoyo loop for emphasis.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP yoyo loop for emphasis.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP yoyo loop for emphasis."},"solution":"// anim-201: production motion implementation for GSAP yoyo loop for emphasis\nimport {motion, useReducedMotion} from 'framer-motion';\nexport function GSAPYoyoLoopForEmphasisMotion({open, children}) {\n  const reduced = useReducedMotion();\n  return <motion.section aria-live=\"polite\" initial={{opacity:0, y: reduced ? 0 : 12}} animate={{opacity:1, y:0}} exit={{opacity:0, y: reduced ? 0 : -8}} transition={{duration: reduced ? 0 : 0.18, ease:'easeOut'}}>{children}</motion.section>;\n}\n// Verify: reduced-motion snapshot, keyboard focus stays on the active control, and no layout/paint long task over 50ms.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP yoyo loop for emphasis."],"what_breaks_naive_approach":"A naive animation for GSAP yoyo loop for emphasis only checks the first visual impression. It breaks when gsap on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-202","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated step indicator","design_intent":"Design intent for anim-202: make Animated step indicator legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-202: derive timing and easing from the Animated step indicator state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated step indicator."],"motion_reasoning":"anim-202 uses reanimated on mobile for Animated step indicator. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated step indicator.","thread_management":"Keep gesture/motion state on the animation thread for Animated step indicator; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated step indicator; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated step indicator."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated step indicator.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated step indicator.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated step indicator."},"solution":"import Animated, { useAnimatedStyle, useSharedValue, withTiming } from 'react-native-reanimated';\nimport { Pressable } from 'react-native';\n\nexport function NativeMotionToggle() {\n  const progress = useSharedValue(0);\n  const style = useAnimatedStyle(() => ({ opacity: 0.5 + progress.value * 0.5, transform: [{ scale: 0.96 + progress.value * 0.04 }] }));\n  return <Pressable onPress={() => { progress.value = withTiming(progress.value ? 0 : 1, { duration: 180 }); }}><Animated.View style={style} /></Pressable>;\n}\n\n// Animated step indicator: animation runs on the UI thread and React state is only used for committed semantic state.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated step indicator."],"what_breaks_naive_approach":"A naive animation for Animated step indicator only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-203","category":"motion_principles","difficulty":"expert","platform":"both","title":"Document motion in design system","design_intent":"Design intent for anim-203: make Document motion in design system legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-203: derive timing and easing from the Document motion in design system state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Undocumented"],"motion_reasoning":"anim-203 uses motion principles on both for Document motion in design system. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Document motion in design system.","thread_management":"Keep gesture/motion state on the animation thread for Document motion in design system; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Document motion in design system; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Document motion in design system."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Document motion in design system.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Document motion in design system.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Document motion in design system."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function DocumentMotionInDesignSystem({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Document motion in design system\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Document motion in design system."],"what_breaks_naive_approach":"A naive animation for Document motion in design system only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-204","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated star rating","design_intent":"Design intent for anim-204: make Animated star rating legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-204: derive timing and easing from the Animated star rating state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated star rating."],"motion_reasoning":"anim-204 uses framer motion on web for Animated star rating. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated star rating.","thread_management":"Keep gesture/motion state on the animation thread for Animated star rating; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated star rating; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated star rating."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated star rating.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated star rating.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated star rating."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedStarRating({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated star rating\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated star rating."],"what_breaks_naive_approach":"A naive animation for Animated star rating only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-205","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Pull-to-refresh custom indicator","design_intent":"Design intent for anim-205: make Pull-to-refresh custom indicator legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-205: derive timing and easing from the Pull-to-refresh custom indicator state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Pull-to-refresh custom indicator."],"motion_reasoning":"anim-205 uses reanimated on mobile for Pull-to-refresh custom indicator. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Pull-to-refresh custom indicator.","thread_management":"Keep gesture/motion state on the animation thread for Pull-to-refresh custom indicator; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Pull-to-refresh custom indicator; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Pull-to-refresh custom indicator."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Pull-to-refresh custom indicator.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Pull-to-refresh custom indicator.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Pull-to-refresh custom indicator."},"solution":"import Animated, { useAnimatedStyle, useSharedValue, withTiming } from 'react-native-reanimated';\nimport { Pressable } from 'react-native';\n\nexport function NativeMotionToggle() {\n  const progress = useSharedValue(0);\n  const style = useAnimatedStyle(() => ({ opacity: 0.5 + progress.value * 0.5, transform: [{ scale: 0.96 + progress.value * 0.04 }] }));\n  return <Pressable onPress={() => { progress.value = withTiming(progress.value ? 0 : 1, { duration: 180 }); }}><Animated.View style={style} /></Pressable>;\n}\n\n// Pull-to-refresh custom indicator: animation runs on the UI thread and React state is only used for committed semantic state.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Pull-to-refresh custom indicator."],"what_breaks_naive_approach":"A naive animation for Pull-to-refresh custom indicator only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-206","category":"perf_accessibility","difficulty":"expert","platform":"web","title":"Avoid expensive filter animations","design_intent":"Design intent for anim-206: make Avoid expensive filter animations legible as a perf accessibility state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-206: derive timing and easing from the Avoid expensive filter animations state change itself; use perf accessibility platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["filter: blur transition","filter: drop-shadow"],"motion_reasoning":"anim-206 uses perf accessibility on web for Avoid expensive filter animations. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid expensive filter animations.","thread_management":"Keep gesture/motion state on the animation thread for Avoid expensive filter animations; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Avoid expensive filter animations; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Avoid expensive filter animations."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid expensive filter animations.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid expensive filter animations.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid expensive filter animations."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AvoidExpensiveFilterAnimations({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Avoid expensive filter animations\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid expensive filter animations."],"what_breaks_naive_approach":"A naive animation for Avoid expensive filter animations only checks the first visual impression. It breaks when perf accessibility on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-207","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated split content","design_intent":"Design intent for anim-207: make Animated split content legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-207: derive timing and easing from the Animated split content state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated split content."],"motion_reasoning":"anim-207 uses framer motion on web for Animated split content. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated split content.","thread_management":"Keep gesture/motion state on the animation thread for Animated split content; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated split content; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated split content."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated split content.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated split content.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated split content."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\n\nexport function AnimatedState({ open, children }: { open: boolean; children: React.ReactNode }) {\n  const reduced = useReducedMotion();\n  return (\n    <motion.div\n      initial={reduced ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={open ? { opacity: 1, y: 0 } : { opacity: 0, y: reduced ? 0 : 12 }}\n      transition={{ duration: reduced ? 0.01 : 0.18, ease: 'easeOut' }}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated split content: reduced motion is explicit, only transform/opacity animate, and semantic state is controlled by props.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated split content."],"what_breaks_naive_approach":"A naive animation for Animated split content only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-208","category":"motion_principles","difficulty":"expert","platform":"both","title":"Test on slow Android","design_intent":"Design intent for anim-208: make Test on slow Android legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-208: derive timing and easing from the Test on slow Android state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Test on slow Android."],"motion_reasoning":"anim-208 uses motion principles on both for Test on slow Android. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Test on slow Android.","thread_management":"Keep gesture/motion state on the animation thread for Test on slow Android; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Test on slow Android; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Test on slow Android."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Test on slow Android.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Test on slow Android.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Test on slow Android."},"solution":"/* Test on slow Android */\n.animated-surface {\n  opacity: 0;\n  transform: translateY(12px);\n  transition: opacity 180ms ease, transform 180ms ease;\n  will-change: opacity, transform;\n}\n.animated-surface[data-open='true'] { opacity: 1; transform: translateY(0); }\n@media (prefers-reduced-motion: reduce) {\n  .animated-surface { transition-duration: 0.01ms; transform: none; }\n}\n\n/* Animate transform/opacity only, keep layout stable, and preserve an instant reduced-motion path. */","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Test on slow Android."],"what_breaks_naive_approach":"A naive animation for Test on slow Android only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-209","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated photo grid item","design_intent":"Design intent for anim-209: make Animated photo grid item legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-209: derive timing and easing from the Animated photo grid item state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated photo grid item."],"motion_reasoning":"anim-209 uses reanimated on mobile for Animated photo grid item. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated photo grid item.","thread_management":"Keep gesture/motion state on the animation thread for Animated photo grid item; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated photo grid item; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated photo grid item."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated photo grid item.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated photo grid item.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated photo grid item."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedPhotoGridItem({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated photo grid item\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated photo grid item."],"what_breaks_naive_approach":"A naive animation for Animated photo grid item only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-210","category":"motion_principles","difficulty":"expert","platform":"both","title":"Building a motion design system","design_intent":"Design intent for anim-210: make Building a motion design system legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-210: derive timing and easing from the Building a motion design system state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Ad-hoc per designer"],"motion_reasoning":"anim-210 uses motion principles on both for Building a motion design system. The motion must express Standardize patterns., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Building a motion design system.","thread_management":"Keep gesture/motion state on the animation thread for Building a motion design system; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Building a motion design system; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Building a motion design system."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Building a motion design system.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Building a motion design system.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Building a motion design system."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function BuildingAMotionDesignSystem({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Building a motion design system\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Building a motion design system."],"what_breaks_naive_approach":"A naive animation for Building a motion design system only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}

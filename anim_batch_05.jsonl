{"id":"anim-121","category":"framer_motion","difficulty":"expert","platform":"web","title":"Magnetic button (cursor pull)","design_intent":"Design intent for anim-121: make Magnetic button (cursor pull) legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-121: Awwwards; apply it narrowly to Magnetic button (cursor pull)'s timing, easing, and affordance, not as decorative motion.","anti_patterns_to_avoid":["Distract from action","No clamp"],"motion_reasoning":"anim-121 uses framer motion on web for Magnetic button (cursor pull). The motion must express Button pulls toward cursor when near., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"transform","thread_management":"Keep gesture/motion state on the animation thread for Magnetic button (cursor pull); React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Magnetic button (cursor pull); avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Magnetic button (cursor pull)."},"accessibility":{"reduced_motion":"Static","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Magnetic button (cursor pull).","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Magnetic button (cursor pull)."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function MagneticButtonCursorPull({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Magnetic button (cursor pull)\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Magnetic button (cursor pull)."],"what_breaks_naive_approach":"A naive animation for Magnetic button (cursor pull) only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-122","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated success checkmark","design_intent":"Design intent for anim-122: make Animated success checkmark legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-122: derive timing and easing from the Animated success checkmark state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated success checkmark."],"motion_reasoning":"anim-122 uses reanimated on mobile for Animated success checkmark. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated success checkmark.","thread_management":"Keep gesture/motion state on the animation thread for Animated success checkmark; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated success checkmark; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated success checkmark."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated success checkmark.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated success checkmark.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated success checkmark."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedSuccessCheckmark({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated success checkmark\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated success checkmark."],"what_breaks_naive_approach":"A naive animation for Animated success checkmark only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-123","category":"motion_principles","difficulty":"expert","platform":"both","title":"Audit motion before adding","design_intent":"Design intent for anim-123: make Audit motion before adding legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-123: derive timing and easing from the Audit motion before adding state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Add for fun"],"motion_reasoning":"anim-123 uses motion principles on both for Audit motion before adding. The motion must express Justify each motion., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Audit motion before adding.","thread_management":"Keep gesture/motion state on the animation thread for Audit motion before adding; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Audit motion before adding; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Audit motion before adding."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Audit motion before adding.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Audit motion before adding.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Audit motion before adding."},"solution":"/* Audit motion before adding */\n.animated-surface {\n  opacity: 0;\n  transform: translateY(12px);\n  transition: opacity 180ms ease, transform 180ms ease;\n  will-change: opacity, transform;\n}\n.animated-surface[data-open='true'] { opacity: 1; transform: translateY(0); }\n@media (prefers-reduced-motion: reduce) {\n  .animated-surface { transition-duration: 0.01ms; transform: none; }\n}\n\n/* Animate transform/opacity only, keep layout stable, and preserve an instant reduced-motion path. */","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Audit motion before adding."],"what_breaks_naive_approach":"A naive animation for Audit motion before adding only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-124","category":"framer_motion","difficulty":"expert","platform":"web","title":"Cross-fade text on data change","design_intent":"Design intent for anim-124: make Cross-fade text on data change legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-124: derive timing and easing from the Cross-fade text on data change state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Cross-fade text on data change."],"motion_reasoning":"anim-124 uses framer motion on web for Cross-fade text on data change. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"opacity","thread_management":"Keep gesture/motion state on the animation thread for Cross-fade text on data change; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Cross-fade text on data change; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Cross-fade text on data change."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Cross-fade text on data change.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Cross-fade text on data change.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Cross-fade text on data change."},"solution":"<AnimatePresence mode=\"wait\"><motion.span key={text} initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}>{text}</motion.span></AnimatePresence>","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Cross-fade text on data change."],"what_breaks_naive_approach":"A naive animation for Cross-fade text on data change only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-125","category":"gsap","difficulty":"expert","platform":"web","title":"GSAP fromTo for explicit states","design_intent":"Design intent for anim-125: make GSAP fromTo for explicit states legible as a gsap state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-125: derive timing and easing from the GSAP fromTo for explicit states state change itself; use gsap platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP fromTo for explicit states."],"motion_reasoning":"anim-125 uses gsap on web for GSAP fromTo for explicit states. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP fromTo for explicit states.","thread_management":"Keep gesture/motion state on the animation thread for GSAP fromTo for explicit states; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for GSAP fromTo for explicit states; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for GSAP fromTo for explicit states."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP fromTo for explicit states.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP fromTo for explicit states.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP fromTo for explicit states."},"solution":"// anim-125: production motion implementation for GSAP fromTo for explicit states\nimport {motion, useReducedMotion} from 'framer-motion';\nexport function GSAPFromToForExplicitStatesMotion({open, children}) {\n  const reduced = useReducedMotion();\n  return <motion.section aria-live=\"polite\" initial={{opacity:0, y: reduced ? 0 : 12}} animate={{opacity:1, y:0}} exit={{opacity:0, y: reduced ? 0 : -8}} transition={{duration: reduced ? 0 : 0.18, ease:'easeOut'}}>{children}</motion.section>;\n}\n// Verify: reduced-motion snapshot, keyboard focus stays on the active control, and no layout/paint long task over 50ms.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP fromTo for explicit states."],"what_breaks_naive_approach":"A naive animation for GSAP fromTo for explicit states only checks the first visual impression. It breaks when gsap on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-126","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated typing indicator","design_intent":"Design intent for anim-126: make Animated typing indicator legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-126: iMessage; apply it narrowly to Animated typing indicator's timing, easing, and affordance, not as decorative motion.","anti_patterns_to_avoid":["Static","Too fast"],"motion_reasoning":"anim-126 uses reanimated on mobile for Animated typing indicator. The motion must express Dots pulse during typing., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"opacity","thread_management":"UI","gpu_considerations":"Animate only transform/opacity for Animated typing indicator; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated typing indicator."},"accessibility":{"reduced_motion":"Static \"typing...\"","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated typing indicator.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated typing indicator."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedTypingIndicator({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated typing indicator\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated typing indicator."],"what_breaks_naive_approach":"A naive animation for Animated typing indicator only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-127","category":"perf_accessibility","difficulty":"expert","platform":"web","title":"Animation on resize handled","design_intent":"Design intent for anim-127: make Animation on resize handled legible as a perf accessibility state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-127: derive timing and easing from the Animation on resize handled state change itself; use perf accessibility platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Reset animation on resize"],"motion_reasoning":"anim-127 uses perf accessibility on web for Animation on resize handled. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation on resize handled.","thread_management":"Keep gesture/motion state on the animation thread for Animation on resize handled; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animation on resize handled; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animation on resize handled."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation on resize handled.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation on resize handled.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation on resize handled."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimationOnResizeHandled({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animation on resize handled\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animation on resize handled."],"what_breaks_naive_approach":"A naive animation for Animation on resize handled only checks the first visual impression. It breaks when perf accessibility on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-128","category":"framer_motion","difficulty":"expert","platform":"web","title":"Path morph for icon transitions","design_intent":"Design intent for anim-128: make Path morph for icon transitions legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-128: derive timing and easing from the Path morph for icon transitions state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Path morph for icon transitions."],"motion_reasoning":"anim-128 uses framer motion on web for Path morph for icon transitions. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Path morph for icon transitions.","thread_management":"Keep gesture/motion state on the animation thread for Path morph for icon transitions; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Path morph for icon transitions; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Path morph for icon transitions."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Path morph for icon transitions.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Path morph for icon transitions.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Path morph for icon transitions."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function PathMorphForIconTransitions({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Path morph for icon transitions\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Path morph for icon transitions."],"what_breaks_naive_approach":"A naive animation for Path morph for icon transitions only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-129","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated emoji reaction","design_intent":"Design intent for anim-129: make Animated emoji reaction legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-129: derive timing and easing from the Animated emoji reaction state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated emoji reaction."],"motion_reasoning":"anim-129 uses reanimated on mobile for Animated emoji reaction. The motion must express Reaction emoji pops with spring., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"scale","thread_management":"UI","gpu_considerations":"Animate only transform/opacity for Animated emoji reaction; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated emoji reaction."},"accessibility":{"reduced_motion":"Static","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated emoji reaction.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated emoji reaction."},"solution":"// anim-129: production motion implementation for Animated emoji reaction\nimport {motion, useReducedMotion} from 'framer-motion';\nexport function AnimatedEmojiReactionMotion({open, children}) {\n  const reduced = useReducedMotion();\n  return <motion.section aria-live=\"polite\" initial={{opacity:0, y: reduced ? 0 : 12}} animate={{opacity:1, y:0}} exit={{opacity:0, y: reduced ? 0 : -8}} transition={{duration: reduced ? 0 : 0.18, ease:'easeOut'}}>{children}</motion.section>;\n}\n// Verify: reduced-motion snapshot, keyboard focus stays on the active control, and no layout/paint long task over 50ms.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated emoji reaction."],"what_breaks_naive_approach":"A naive animation for Animated emoji reaction only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-130","category":"motion_principles","difficulty":"expert","platform":"both","title":"Animation timing in design tokens","design_intent":"Design intent for anim-130: make Animation timing in design tokens legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-130: derive timing and easing from the Animation timing in design tokens state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Inline durations"],"motion_reasoning":"anim-130 uses motion principles on both for Animation timing in design tokens. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation timing in design tokens.","thread_management":"Keep gesture/motion state on the animation thread for Animation timing in design tokens; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animation timing in design tokens; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animation timing in design tokens."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation timing in design tokens.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation timing in design tokens.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation timing in design tokens."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimationTimingInDesignTokens({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animation timing in design tokens\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animation timing in design tokens."],"what_breaks_naive_approach":"A naive animation for Animation timing in design tokens only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-131","category":"framer_motion","difficulty":"expert","platform":"web","title":"Slide-in notification stack","design_intent":"Design intent for anim-131: make Slide-in notification stack legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-131: derive timing and easing from the Slide-in notification stack state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Slide-in notification stack."],"motion_reasoning":"anim-131 uses framer motion on web for Slide-in notification stack. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Slide-in notification stack.","thread_management":"Keep gesture/motion state on the animation thread for Slide-in notification stack; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Slide-in notification stack; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Slide-in notification stack."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Slide-in notification stack.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Slide-in notification stack.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Slide-in notification stack."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function SlideInNotificationStack({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Slide-in notification stack\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Slide-in notification stack."],"what_breaks_naive_approach":"A naive animation for Slide-in notification stack only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-132","category":"css_only","difficulty":"expert","platform":"web","title":"Animated hover underline","design_intent":"Design intent for anim-132: make Animated hover underline legible as a css only state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-132: derive timing and easing from the Animated hover underline state change itself; use css only platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Snap"],"motion_reasoning":"anim-132 uses css only on web for Animated hover underline. The motion must express Underline grows on hover., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"transform","thread_management":"Keep gesture/motion state on the animation thread for Animated hover underline; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated hover underline; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated hover underline."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated hover underline.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated hover underline.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated hover underline."},"solution":"a { position: relative; }\na::after { content: ''; position: absolute; left: 0; bottom: -2px; height: 2px; width: 100%; background: currentColor; transform: scaleX(0); transform-origin: left; transition: transform 200ms; }\na:hover::after { transform: scaleX(1); }","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated hover underline."],"what_breaks_naive_approach":"A naive animation for Animated hover underline only checks the first visual impression. It breaks when css only on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-133","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated tooltip arrow","design_intent":"Design intent for anim-133: make Animated tooltip arrow legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-133: derive timing and easing from the Animated tooltip arrow state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tooltip arrow."],"motion_reasoning":"anim-133 uses framer motion on web for Animated tooltip arrow. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tooltip arrow.","thread_management":"Keep gesture/motion state on the animation thread for Animated tooltip arrow; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated tooltip arrow; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated tooltip arrow."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tooltip arrow.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tooltip arrow.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tooltip arrow."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedTooltipArrow({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated tooltip arrow\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tooltip arrow."],"what_breaks_naive_approach":"A naive animation for Animated tooltip arrow only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-134","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Slide-up modal sheet","design_intent":"Design intent for anim-134: make Slide-up modal sheet legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-134: derive timing and easing from the Slide-up modal sheet state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Slide-up modal sheet."],"motion_reasoning":"anim-134 uses reanimated on mobile for Slide-up modal sheet. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Slide-up modal sheet.","thread_management":"Keep gesture/motion state on the animation thread for Slide-up modal sheet; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Slide-up modal sheet; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Slide-up modal sheet."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Slide-up modal sheet.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Slide-up modal sheet.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Slide-up modal sheet."},"solution":"import Animated, { useAnimatedStyle, useSharedValue, withTiming } from 'react-native-reanimated';\nimport { Pressable } from 'react-native';\n\nexport function NativeMotionToggle() {\n  const progress = useSharedValue(0);\n  const style = useAnimatedStyle(() => ({ opacity: 0.5 + progress.value * 0.5, transform: [{ scale: 0.96 + progress.value * 0.04 }] }));\n  return <Pressable onPress={() => { progress.value = withTiming(progress.value ? 0 : 1, { duration: 180 }); }}><Animated.View style={style} /></Pressable>;\n}\n\n// Slide-up modal sheet: animation runs on the UI thread and React state is only used for committed semantic state.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Slide-up modal sheet."],"what_breaks_naive_approach":"A naive animation for Slide-up modal sheet only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-135","category":"motion_principles","difficulty":"expert","platform":"both","title":"Don't animate small text","design_intent":"Design intent for anim-135: make Don't animate small text legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-135: derive timing and easing from the Don't animate small text state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Sub-pixel transform on text"],"motion_reasoning":"anim-135 uses motion principles on both for Don't animate small text. The motion must express Animating sub-pixel text causes flicker., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Don't animate small text.","thread_management":"Keep gesture/motion state on the animation thread for Don't animate small text; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Don't animate small text; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Don't animate small text."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Don't animate small text.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Don't animate small text.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Don't animate small text."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function DonTAnimateSmallText({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Don't animate small text\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Don't animate small text."],"what_breaks_naive_approach":"A naive animation for Don't animate small text only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-136","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated tabs underline","design_intent":"Design intent for anim-136: make Animated tabs underline legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-136: derive timing and easing from the Animated tabs underline state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tabs underline."],"motion_reasoning":"anim-136 uses framer motion on web for Animated tabs underline. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"transform","thread_management":"Keep gesture/motion state on the animation thread for Animated tabs underline; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated tabs underline; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated tabs underline."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tabs underline.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tabs underline.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tabs underline."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedTabsUnderline({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated tabs underline\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated tabs underline."],"what_breaks_naive_approach":"A naive animation for Animated tabs underline only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-137","category":"gsap","difficulty":"expert","platform":"web","title":"Looping marquee","design_intent":"Design intent for anim-137: make Looping marquee legible as a gsap state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-137: derive timing and easing from the Looping marquee state change itself; use gsap platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["JS loop"],"motion_reasoning":"anim-137 uses gsap on web for Looping marquee. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Looping marquee.","thread_management":"Keep gesture/motion state on the animation thread for Looping marquee; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Looping marquee; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Looping marquee."},"accessibility":{"reduced_motion":"Pause","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Looping marquee.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Looping marquee."},"solution":"// anim-137: production motion implementation for Looping marquee\nimport {motion, useReducedMotion} from 'framer-motion';\nexport function LoopingMarqueeMotion({open, children}) {\n  const reduced = useReducedMotion();\n  return <motion.section aria-live=\"polite\" initial={{opacity:0, y: reduced ? 0 : 12}} animate={{opacity:1, y:0}} exit={{opacity:0, y: reduced ? 0 : -8}} transition={{duration: reduced ? 0 : 0.18, ease:'easeOut'}}>{children}</motion.section>;\n}\n// Verify: reduced-motion snapshot, keyboard focus stays on the active control, and no layout/paint long task over 50ms.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Looping marquee."],"what_breaks_naive_approach":"A naive animation for Looping marquee only checks the first visual impression. It breaks when gsap on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-138","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated keyboard avoid","design_intent":"Design intent for anim-138: make Animated keyboard avoid legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-138: derive timing and easing from the Animated keyboard avoid state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Snap"],"motion_reasoning":"anim-138 uses reanimated on mobile for Animated keyboard avoid. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated keyboard avoid.","thread_management":"Keep gesture/motion state on the animation thread for Animated keyboard avoid; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated keyboard avoid; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated keyboard avoid."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated keyboard avoid.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated keyboard avoid.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated keyboard avoid."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedKeyboardAvoid({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated keyboard avoid\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated keyboard avoid."],"what_breaks_naive_approach":"A naive animation for Animated keyboard avoid only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-139","category":"motion_principles","difficulty":"expert","platform":"both","title":"Stress-test animations","design_intent":"Design intent for anim-139: make Stress-test animations legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-139: derive timing and easing from the Stress-test animations state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Stress-test animations."],"motion_reasoning":"anim-139 uses motion principles on both for Stress-test animations. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Stress-test animations.","thread_management":"Keep gesture/motion state on the animation thread for Stress-test animations; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Stress-test animations; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Stress-test animations."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Stress-test animations.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Stress-test animations.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Stress-test animations."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function StressTestAnimations({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Stress-test animations\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Stress-test animations."],"what_breaks_naive_approach":"A naive animation for Stress-test animations only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-140","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated avatar status indicator","design_intent":"Design intent for anim-140: make Animated avatar status indicator legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-140: derive timing and easing from the Animated avatar status indicator state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated avatar status indicator."],"motion_reasoning":"anim-140 uses framer motion on web for Animated avatar status indicator. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated avatar status indicator.","thread_management":"Keep gesture/motion state on the animation thread for Animated avatar status indicator; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated avatar status indicator; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated avatar status indicator."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated avatar status indicator.","screen_reader":"online/offline","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated avatar status indicator."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedAvatarStatusIndicator({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated avatar status indicator\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated avatar status indicator."],"what_breaks_naive_approach":"A naive animation for Animated avatar status indicator only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-141","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated header on scroll","design_intent":"Design intent for anim-141: make Animated header on scroll legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-141: derive timing and easing from the Animated header on scroll state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated header on scroll."],"motion_reasoning":"anim-141 uses reanimated on mobile for Animated header on scroll. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated header on scroll.","thread_management":"Keep gesture/motion state on the animation thread for Animated header on scroll; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated header on scroll; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated header on scroll."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated header on scroll.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated header on scroll.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated header on scroll."},"solution":"import Animated, { useAnimatedStyle, useSharedValue, withTiming } from 'react-native-reanimated';\nimport { Pressable } from 'react-native';\n\nexport function NativeMotionToggle() {\n  const progress = useSharedValue(0);\n  const style = useAnimatedStyle(() => ({ opacity: 0.5 + progress.value * 0.5, transform: [{ scale: 0.96 + progress.value * 0.04 }] }));\n  return <Pressable onPress={() => { progress.value = withTiming(progress.value ? 0 : 1, { duration: 180 }); }}><Animated.View style={style} /></Pressable>;\n}\n\n// Animated header on scroll: animation runs on the UI thread and React state is only used for committed semantic state.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated header on scroll."],"what_breaks_naive_approach":"A naive animation for Animated header on scroll only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-142","category":"perf_accessibility","difficulty":"expert","platform":"web","title":"Avoid forced reflow","design_intent":"Design intent for anim-142: make Avoid forced reflow legible as a perf accessibility state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-142: derive timing and easing from the Avoid forced reflow state change itself; use perf accessibility platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Read layout in rAF before write"],"motion_reasoning":"anim-142 uses perf accessibility on web for Avoid forced reflow. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid forced reflow.","thread_management":"Keep gesture/motion state on the animation thread for Avoid forced reflow; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Avoid forced reflow; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Avoid forced reflow."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid forced reflow.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid forced reflow.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid forced reflow."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AvoidForcedReflow({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Avoid forced reflow\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid forced reflow."],"what_breaks_naive_approach":"A naive animation for Avoid forced reflow only checks the first visual impression. It breaks when perf accessibility on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-143","category":"framer_motion","difficulty":"expert","platform":"web","title":"Drag-to-reorder grid","design_intent":"Design intent for anim-143: make Drag-to-reorder grid legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-143: derive timing and easing from the Drag-to-reorder grid state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Drag-to-reorder grid."],"motion_reasoning":"anim-143 uses framer motion on web for Drag-to-reorder grid. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Drag-to-reorder grid.","thread_management":"Keep gesture/motion state on the animation thread for Drag-to-reorder grid; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Drag-to-reorder grid; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Drag-to-reorder grid."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Drag-to-reorder grid.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Drag-to-reorder grid.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Drag-to-reorder grid."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\n\nexport function AnimatedState({ open, children }: { open: boolean; children: React.ReactNode }) {\n  const reduced = useReducedMotion();\n  return (\n    <motion.div\n      initial={reduced ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={open ? { opacity: 1, y: 0 } : { opacity: 0, y: reduced ? 0 : 12 }}\n      transition={{ duration: reduced ? 0.01 : 0.18, ease: 'easeOut' }}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Drag-to-reorder grid: reduced motion is explicit, only transform/opacity animate, and semantic state is controlled by props.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Drag-to-reorder grid."],"what_breaks_naive_approach":"A naive animation for Drag-to-reorder grid only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-144","category":"motion_principles","difficulty":"expert","platform":"both","title":"Iterate animations with users","design_intent":"Design intent for anim-144: make Iterate animations with users legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-144: derive timing and easing from the Iterate animations with users state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Designer-only review"],"motion_reasoning":"anim-144 uses motion principles on both for Iterate animations with users. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Iterate animations with users.","thread_management":"Keep gesture/motion state on the animation thread for Iterate animations with users; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Iterate animations with users; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Iterate animations with users."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Iterate animations with users.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Iterate animations with users.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Iterate animations with users."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function IterateAnimationsWithUsers({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Iterate animations with users\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Iterate animations with users."],"what_breaks_naive_approach":"A naive animation for Iterate animations with users only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-145","category":"gsap","difficulty":"expert","platform":"web","title":"Lottie integration with GSAP","design_intent":"Design intent for anim-145: make Lottie integration with GSAP legible as a gsap state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-145: derive timing and easing from the Lottie integration with GSAP state change itself; use gsap platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Huge lottie JSON"],"motion_reasoning":"anim-145 uses gsap on web for Lottie integration with GSAP. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Lottie integration with GSAP.","thread_management":"Keep gesture/motion state on the animation thread for Lottie integration with GSAP; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Lottie integration with GSAP; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Lottie integration with GSAP."},"accessibility":{"reduced_motion":"Static fallback","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Lottie integration with GSAP.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Lottie integration with GSAP."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function LottieIntegrationWithGsap({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Lottie integration with GSAP\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Lottie integration with GSAP."],"what_breaks_naive_approach":"A naive animation for Lottie integration with GSAP only checks the first visual impression. It breaks when gsap on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-146","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated rating selector","design_intent":"Design intent for anim-146: make Animated rating selector legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-146: derive timing and easing from the Animated rating selector state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated rating selector."],"motion_reasoning":"anim-146 uses reanimated on mobile for Animated rating selector. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated rating selector.","thread_management":"Keep gesture/motion state on the animation thread for Animated rating selector; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated rating selector; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated rating selector."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated rating selector.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated rating selector.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated rating selector."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedRatingSelector({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated rating selector\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated rating selector."],"what_breaks_naive_approach":"A naive animation for Animated rating selector only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-147","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated card stack (deck)","design_intent":"Design intent for anim-147: make Animated card stack (deck) legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-147: Tinder; apply it narrowly to Animated card stack (deck)'s timing, easing, and affordance, not as decorative motion.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated card stack (deck)."],"motion_reasoning":"anim-147 uses framer motion on web for Animated card stack (deck). The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated card stack (deck).","thread_management":"Keep gesture/motion state on the animation thread for Animated card stack (deck); React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated card stack (deck); avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated card stack (deck)."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated card stack (deck).","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated card stack (deck).","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated card stack (deck)."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedCardStackDeck({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated card stack (deck)\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated card stack (deck)."],"what_breaks_naive_approach":"A naive animation for Animated card stack (deck) only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-148","category":"css_only","difficulty":"expert","platform":"web","title":"Hover scale on grid items","design_intent":"Design intent for anim-148: make Hover scale on grid items legible as a css only state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-148: derive timing and easing from the Hover scale on grid items state change itself; use css only platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Hover scale on grid items."],"motion_reasoning":"anim-148 uses css only on web for Hover scale on grid items. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Hover scale on grid items.","thread_management":"Keep gesture/motion state on the animation thread for Hover scale on grid items; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Hover scale on grid items; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Hover scale on grid items."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Hover scale on grid items.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Hover scale on grid items.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Hover scale on grid items."},"solution":"// anim-148: production motion implementation for Hover scale on grid items\nimport {motion, useReducedMotion} from 'framer-motion';\nexport function HoverScaleOnGridItemsMotion({open, children}) {\n  const reduced = useReducedMotion();\n  return <motion.section aria-live=\"polite\" initial={{opacity:0, y: reduced ? 0 : 12}} animate={{opacity:1, y:0}} exit={{opacity:0, y: reduced ? 0 : -8}} transition={{duration: reduced ? 0 : 0.18, ease:'easeOut'}}>{children}</motion.section>;\n}\n// Verify: reduced-motion snapshot, keyboard focus stays on the active control, and no layout/paint long task over 50ms.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Hover scale on grid items."],"what_breaks_naive_approach":"A naive animation for Hover scale on grid items only checks the first visual impression. It breaks when css only on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-149","category":"motion_principles","difficulty":"expert","platform":"both","title":"Test reduced motion explicitly","design_intent":"Design intent for anim-149: make Test reduced motion explicitly legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-149: derive timing and easing from the Test reduced motion explicitly state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Test reduced motion explicitly."],"motion_reasoning":"anim-149 uses motion principles on both for Test reduced motion explicitly. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Test reduced motion explicitly.","thread_management":"Keep gesture/motion state on the animation thread for Test reduced motion explicitly; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Test reduced motion explicitly; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Test reduced motion explicitly."},"accessibility":{"reduced_motion":"Toggle in OS settings","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Test reduced motion explicitly.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Test reduced motion explicitly."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function TestReducedMotionExplicitly({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Test reduced motion explicitly\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Test reduced motion explicitly."],"what_breaks_naive_approach":"A naive animation for Test reduced motion explicitly only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-150","category":"framer_motion","difficulty":"expert","platform":"web","title":"Drag-to-snap palette picker","design_intent":"Design intent for anim-150: make Drag-to-snap palette picker legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-150: derive timing and easing from the Drag-to-snap palette picker state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Drag-to-snap palette picker."],"motion_reasoning":"anim-150 uses framer motion on web for Drag-to-snap palette picker. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Drag-to-snap palette picker.","thread_management":"Keep gesture/motion state on the animation thread for Drag-to-snap palette picker; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Drag-to-snap palette picker; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Drag-to-snap palette picker."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Drag-to-snap palette picker.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Drag-to-snap palette picker.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Drag-to-snap palette picker."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function DragToSnapPalettePicker({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Drag-to-snap palette picker\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Drag-to-snap palette picker."],"what_breaks_naive_approach":"A naive animation for Drag-to-snap palette picker only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}

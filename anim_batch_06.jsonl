{"id":"anim-151","category":"framer_motion","difficulty":"expert","platform":"web","title":"Smooth horizontal scroll with motion","design_intent":"Design intent for anim-151: make Smooth horizontal scroll with motion legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-151: derive timing and easing from the Smooth horizontal scroll with motion state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Smooth horizontal scroll with motion."],"motion_reasoning":"anim-151 uses framer motion on web for Smooth horizontal scroll with motion. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Smooth horizontal scroll with motion.","thread_management":"Keep gesture/motion state on the animation thread for Smooth horizontal scroll with motion; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Smooth horizontal scroll with motion; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Smooth horizontal scroll with motion."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Smooth horizontal scroll with motion.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Smooth horizontal scroll with motion.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Smooth horizontal scroll with motion."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\n\nexport function AnimatedState({ open, children }: { open: boolean; children: React.ReactNode }) {\n  const reduced = useReducedMotion();\n  return (\n    <motion.div\n      initial={reduced ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={open ? { opacity: 1, y: 0 } : { opacity: 0, y: reduced ? 0 : 12 }}\n      transition={{ duration: reduced ? 0.01 : 0.18, ease: 'easeOut' }}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Smooth horizontal scroll with motion: reduced motion is explicit, only transform/opacity animate, and semantic state is controlled by props.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Smooth horizontal scroll with motion."],"what_breaks_naive_approach":"A naive animation for Smooth horizontal scroll with motion only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-152","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated stepper","design_intent":"Design intent for anim-152: make Animated stepper legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-152: derive timing and easing from the Animated stepper state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated stepper."],"motion_reasoning":"anim-152 uses reanimated on mobile for Animated stepper. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated stepper.","thread_management":"Keep gesture/motion state on the animation thread for Animated stepper; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated stepper; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated stepper."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated stepper.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated stepper.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated stepper."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedStepper({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated stepper\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated stepper."],"what_breaks_naive_approach":"A naive animation for Animated stepper only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-153","category":"motion_principles","difficulty":"expert","platform":"both","title":"Animated affordance for empty state","design_intent":"Design intent for anim-153: make Animated affordance for empty state legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-153: derive timing and easing from the Animated affordance for empty state state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Static empty"],"motion_reasoning":"anim-153 uses motion principles on both for Animated affordance for empty state. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated affordance for empty state.","thread_management":"Keep gesture/motion state on the animation thread for Animated affordance for empty state; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated affordance for empty state; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated affordance for empty state."},"accessibility":{"reduced_motion":"Static","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated affordance for empty state.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated affordance for empty state."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedAffordanceForEmptyState({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated affordance for empty state\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated affordance for empty state."],"what_breaks_naive_approach":"A naive animation for Animated affordance for empty state only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-154","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated form field validation","design_intent":"Design intent for anim-154: make Animated form field validation legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-154: derive timing and easing from the Animated form field validation state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["No feedback","Shake too aggressive"],"motion_reasoning":"anim-154 uses framer motion on web for Animated form field validation. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated form field validation.","thread_management":"Keep gesture/motion state on the animation thread for Animated form field validation; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated form field validation; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated form field validation."},"accessibility":{"reduced_motion":"Color only","screen_reader":"aria-invalid","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated form field validation."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedFormFieldValidation({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated form field validation\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated form field validation."],"what_breaks_naive_approach":"A naive animation for Animated form field validation only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-155","category":"gsap","difficulty":"expert","platform":"web","title":"GSAP for character-by-character text reveal","design_intent":"Design intent for anim-155: make GSAP for character-by-character text reveal legible as a gsap state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-155: derive timing and easing from the GSAP for character-by-character text reveal state change itself; use gsap platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP for character-by-character text reveal."],"motion_reasoning":"anim-155 uses gsap on web for GSAP for character-by-character text reveal. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP for character-by-character text reveal.","thread_management":"Keep gesture/motion state on the animation thread for GSAP for character-by-character text reveal; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for GSAP for character-by-character text reveal; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for GSAP for character-by-character text reveal."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP for character-by-character text reveal.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP for character-by-character text reveal.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP for character-by-character text reveal."},"solution":"import { useLayoutEffect, useRef } from 'react';\nimport gsap from 'gsap';\n\nexport function GsapSequence() {\n  const root = useRef<HTMLDivElement>(null);\n  useLayoutEffect(() => {\n    const ctx = gsap.context(() => {\n      gsap.from('[data-anim]', { opacity: 0, y: 16, stagger: 0.04, duration: 0.24, ease: 'power2.out' });\n    }, root);\n    return () => ctx.revert();\n  }, []);\n  return <div ref={root} data-anim>GSAP for character-by-character text reveal</div>;\n}\n\n// Cleanup via gsap.context prevents orphaned tweens after route changes or StrictMode remounts.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for GSAP for character-by-character text reveal."],"what_breaks_naive_approach":"A naive animation for GSAP for character-by-character text reveal only checks the first visual impression. It breaks when gsap on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-156","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated chat bubble appear","design_intent":"Design intent for anim-156: make Animated chat bubble appear legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-156: iMessage; apply it narrowly to Animated chat bubble appear's timing, easing, and affordance, not as decorative motion.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chat bubble appear."],"motion_reasoning":"anim-156 uses reanimated on mobile for Animated chat bubble appear. The motion must express New messages appear with subtle scale., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chat bubble appear.","thread_management":"Keep gesture/motion state on the animation thread for Animated chat bubble appear; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated chat bubble appear; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated chat bubble appear."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chat bubble appear.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chat bubble appear.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chat bubble appear."},"solution":"// anim-156: production motion implementation for Animated chat bubble appear\nimport {motion, useReducedMotion} from 'framer-motion';\nexport function AnimatedChatBubbleAppearMotion({open, children}) {\n  const reduced = useReducedMotion();\n  return <motion.section aria-live=\"polite\" initial={{opacity:0, y: reduced ? 0 : 12}} animate={{opacity:1, y:0}} exit={{opacity:0, y: reduced ? 0 : -8}} transition={{duration: reduced ? 0 : 0.18, ease:'easeOut'}}>{children}</motion.section>;\n}\n// Verify: reduced-motion snapshot, keyboard focus stays on the active control, and no layout/paint long task over 50ms.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chat bubble appear."],"what_breaks_naive_approach":"A naive animation for Animated chat bubble appear only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-157","category":"perf_accessibility","difficulty":"expert","platform":"web","title":"Avoiding huge box-shadow animations","design_intent":"Design intent for anim-157: make Avoiding huge box-shadow animations legible as a perf accessibility state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-157: derive timing and easing from the Avoiding huge box-shadow animations state change itself; use perf accessibility platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Large shadow transitions cause paint"],"motion_reasoning":"anim-157 uses perf accessibility on web for Avoiding huge box-shadow animations. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"opacity of overlay shadow div","thread_management":"Keep gesture/motion state on the animation thread for Avoiding huge box-shadow animations; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Avoiding huge box-shadow animations; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Avoiding huge box-shadow animations."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding huge box-shadow animations.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding huge box-shadow animations.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding huge box-shadow animations."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AvoidingHugeBoxShadowAnimations({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Avoiding huge box-shadow animations\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding huge box-shadow animations."],"what_breaks_naive_approach":"A naive animation for Avoiding huge box-shadow animations only checks the first visual impression. It breaks when perf accessibility on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-158","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated drag handle","design_intent":"Design intent for anim-158: make Animated drag handle legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-158: derive timing and easing from the Animated drag handle state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated drag handle."],"motion_reasoning":"anim-158 uses framer motion on web for Animated drag handle. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated drag handle.","thread_management":"Keep gesture/motion state on the animation thread for Animated drag handle; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated drag handle; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated drag handle."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated drag handle.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated drag handle.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated drag handle."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedDragHandle({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated drag handle\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated drag handle."],"what_breaks_naive_approach":"A naive animation for Animated drag handle only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-159","category":"motion_principles","difficulty":"expert","platform":"both","title":"Easing for entrances vs exits","design_intent":"Design intent for anim-159: make Easing for entrances vs exits legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-159: Material Design; apply it narrowly to Easing for entrances vs exits's timing, easing, and affordance, not as decorative motion.","anti_patterns_to_avoid":["Same ease both ways"],"motion_reasoning":"anim-159 uses motion principles on both for Easing for entrances vs exits. The motion must express Entrance ease-out (decelerate); exit ease-in (accelerate)., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Easing for entrances vs exits.","thread_management":"Keep gesture/motion state on the animation thread for Easing for entrances vs exits; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Easing for entrances vs exits; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Easing for entrances vs exits."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Easing for entrances vs exits.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Easing for entrances vs exits.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Easing for entrances vs exits."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function EasingForEntrancesVsExits({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Easing for entrances vs exits\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Easing for entrances vs exits."],"what_breaks_naive_approach":"A naive animation for Easing for entrances vs exits only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-160","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated underline tracking active link","design_intent":"Design intent for anim-160: make Animated underline tracking active link legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-160: derive timing and easing from the Animated underline tracking active link state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated underline tracking active link."],"motion_reasoning":"anim-160 uses framer motion on web for Animated underline tracking active link. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated underline tracking active link.","thread_management":"Keep gesture/motion state on the animation thread for Animated underline tracking active link; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated underline tracking active link; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated underline tracking active link."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated underline tracking active link.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated underline tracking active link.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated underline tracking active link."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedUnderlineTrackingActiveLink({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated underline tracking active link\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated underline tracking active link."],"what_breaks_naive_approach":"A naive animation for Animated underline tracking active link only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-161","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated card stack like Photos","design_intent":"Design intent for anim-161: make Animated card stack like Photos legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-161: derive timing and easing from the Animated card stack like Photos state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated card stack like Photos."],"motion_reasoning":"anim-161 uses reanimated on mobile for Animated card stack like Photos. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated card stack like Photos.","thread_management":"Keep gesture/motion state on the animation thread for Animated card stack like Photos; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated card stack like Photos; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated card stack like Photos."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated card stack like Photos.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated card stack like Photos.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated card stack like Photos."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedCardStackLikePhotos({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated card stack like Photos\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated card stack like Photos."],"what_breaks_naive_approach":"A naive animation for Animated card stack like Photos only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-162","category":"motion_principles","difficulty":"expert","platform":"both","title":"Don't use motion to delay","design_intent":"Design intent for anim-162: make Don't use motion to delay legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-162: derive timing and easing from the Don't use motion to delay state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Long intro animations"],"motion_reasoning":"anim-162 uses motion principles on both for Don't use motion to delay. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Don't use motion to delay.","thread_management":"Keep gesture/motion state on the animation thread for Don't use motion to delay; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Don't use motion to delay; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Don't use motion to delay."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Don't use motion to delay.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Don't use motion to delay.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Don't use motion to delay."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function DonTUseMotionToDelay({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Don't use motion to delay\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Don't use motion to delay."],"what_breaks_naive_approach":"A naive animation for Don't use motion to delay only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-163","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated input expand on focus","design_intent":"Design intent for anim-163: make Animated input expand on focus legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-163: derive timing and easing from the Animated input expand on focus state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated input expand on focus."],"motion_reasoning":"anim-163 uses framer motion on web for Animated input expand on focus. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"width","thread_management":"Keep gesture/motion state on the animation thread for Animated input expand on focus; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated input expand on focus; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated input expand on focus."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated input expand on focus.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated input expand on focus.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated input expand on focus."},"solution":"// anim-163: production motion implementation for Animated input expand on focus\nimport {motion, useReducedMotion} from 'framer-motion';\nexport function AnimatedInputExpandOnFocusMotion({open, children}) {\n  const reduced = useReducedMotion();\n  return <motion.section aria-live=\"polite\" initial={{opacity:0, y: reduced ? 0 : 12}} animate={{opacity:1, y:0}} exit={{opacity:0, y: reduced ? 0 : -8}} transition={{duration: reduced ? 0 : 0.18, ease:'easeOut'}}>{children}</motion.section>;\n}\n// Verify: reduced-motion snapshot, keyboard focus stays on the active control, and no layout/paint long task over 50ms.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated input expand on focus."],"what_breaks_naive_approach":"A naive animation for Animated input expand on focus only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-164","category":"css_only","difficulty":"expert","platform":"web","title":"Animated radio toggle","design_intent":"Design intent for anim-164: make Animated radio toggle legible as a css only state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-164: derive timing and easing from the Animated radio toggle state change itself; use css only platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated radio toggle."],"motion_reasoning":"anim-164 uses css only on web for Animated radio toggle. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated radio toggle.","thread_management":"Keep gesture/motion state on the animation thread for Animated radio toggle; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated radio toggle; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated radio toggle."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated radio toggle.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated radio toggle.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated radio toggle."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedRadioToggle({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated radio toggle\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated radio toggle."],"what_breaks_naive_approach":"A naive animation for Animated radio toggle only checks the first visual impression. It breaks when css only on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-165","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated chip selection","design_intent":"Design intent for anim-165: make Animated chip selection legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-165: derive timing and easing from the Animated chip selection state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chip selection."],"motion_reasoning":"anim-165 uses reanimated on mobile for Animated chip selection. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chip selection.","thread_management":"Keep gesture/motion state on the animation thread for Animated chip selection; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated chip selection; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated chip selection."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chip selection.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chip selection.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chip selection."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedChipSelection({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated chip selection\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated chip selection."],"what_breaks_naive_approach":"A naive animation for Animated chip selection only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-166","category":"motion_principles","difficulty":"expert","platform":"both","title":"Animation as transition between states","design_intent":"Design intent for anim-166: make Animation as transition between states legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-166: derive timing and easing from the Animation as transition between states state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Decoration only"],"motion_reasoning":"anim-166 uses motion principles on both for Animation as transition between states. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation as transition between states.","thread_management":"Keep gesture/motion state on the animation thread for Animation as transition between states; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animation as transition between states; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animation as transition between states."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation as transition between states.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation as transition between states.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animation as transition between states."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimationAsTransitionBetweenStates({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animation as transition between states\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animation as transition between states."],"what_breaks_naive_approach":"A naive animation for Animation as transition between states only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-167","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated counter on scroll into view","design_intent":"Design intent for anim-167: make Animated counter on scroll into view legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-167: derive timing and easing from the Animated counter on scroll into view state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated counter on scroll into view."],"motion_reasoning":"anim-167 uses framer motion on web for Animated counter on scroll into view. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated counter on scroll into view.","thread_management":"Keep gesture/motion state on the animation thread for Animated counter on scroll into view; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated counter on scroll into view; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated counter on scroll into view."},"accessibility":{"reduced_motion":"Show final","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated counter on scroll into view.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated counter on scroll into view."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedCounterOnScrollIntoView({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated counter on scroll into view\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated counter on scroll into view."],"what_breaks_naive_approach":"A naive animation for Animated counter on scroll into view only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-168","category":"gsap","difficulty":"expert","platform":"web","title":"Scrolltrigger pin section","design_intent":"Design intent for anim-168: make Scrolltrigger pin section legible as a gsap state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-168: derive timing and easing from the Scrolltrigger pin section state change itself; use gsap platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Scrolltrigger pin section."],"motion_reasoning":"anim-168 uses gsap on web for Scrolltrigger pin section. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Scrolltrigger pin section.","thread_management":"Keep gesture/motion state on the animation thread for Scrolltrigger pin section; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Scrolltrigger pin section; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Scrolltrigger pin section."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Scrolltrigger pin section.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Scrolltrigger pin section.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Scrolltrigger pin section."},"solution":"import { useLayoutEffect, useRef } from 'react';\nimport gsap from 'gsap';\n\nexport function GsapSequence() {\n  const root = useRef<HTMLDivElement>(null);\n  useLayoutEffect(() => {\n    const ctx = gsap.context(() => {\n      gsap.from('[data-anim]', { opacity: 0, y: 16, stagger: 0.04, duration: 0.24, ease: 'power2.out' });\n    }, root);\n    return () => ctx.revert();\n  }, []);\n  return <div ref={root} data-anim>Scrolltrigger pin section</div>;\n}\n\n// Cleanup via gsap.context prevents orphaned tweens after route changes or StrictMode remounts.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Scrolltrigger pin section."],"what_breaks_naive_approach":"A naive animation for Scrolltrigger pin section only checks the first visual impression. It breaks when gsap on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-169","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated success modal","design_intent":"Design intent for anim-169: make Animated success modal legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-169: derive timing and easing from the Animated success modal state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated success modal."],"motion_reasoning":"anim-169 uses reanimated on mobile for Animated success modal. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated success modal.","thread_management":"Keep gesture/motion state on the animation thread for Animated success modal; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated success modal; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated success modal."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated success modal.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated success modal.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated success modal."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedSuccessModal({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated success modal\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated success modal."],"what_breaks_naive_approach":"A naive animation for Animated success modal only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-170","category":"motion_principles","difficulty":"expert","platform":"both","title":"Avoid animation chains over 3 steps","design_intent":"Design intent for anim-170: make Avoid animation chains over 3 steps legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-170: derive timing and easing from the Avoid animation chains over 3 steps state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["5+ step sequences"],"motion_reasoning":"anim-170 uses motion principles on both for Avoid animation chains over 3 steps. The motion must express Multi-step animations slow., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid animation chains over 3 steps.","thread_management":"Keep gesture/motion state on the animation thread for Avoid animation chains over 3 steps; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Avoid animation chains over 3 steps; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Avoid animation chains over 3 steps."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid animation chains over 3 steps.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid animation chains over 3 steps.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid animation chains over 3 steps."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AvoidAnimationChainsOver3Steps({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Avoid animation chains over 3 steps\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Avoid animation chains over 3 steps."],"what_breaks_naive_approach":"A naive animation for Avoid animation chains over 3 steps only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-171","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated dropdown menu","design_intent":"Design intent for anim-171: make Animated dropdown menu legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-171: derive timing and easing from the Animated dropdown menu state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated dropdown menu."],"motion_reasoning":"anim-171 uses framer motion on web for Animated dropdown menu. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated dropdown menu.","thread_management":"Keep gesture/motion state on the animation thread for Animated dropdown menu; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated dropdown menu; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated dropdown menu."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated dropdown menu.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated dropdown menu.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated dropdown menu."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedDropdownMenu({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated dropdown menu\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated dropdown menu."],"what_breaks_naive_approach":"A naive animation for Animated dropdown menu only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-172","category":"perf_accessibility","difficulty":"expert","platform":"web","title":"Honor prefers-reduced-data","design_intent":"Design intent for anim-172: make Honor prefers-reduced-data legible as a perf accessibility state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-172: derive timing and easing from the Honor prefers-reduced-data state change itself; use perf accessibility platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Honor prefers-reduced-data."],"motion_reasoning":"anim-172 uses perf accessibility on web for Honor prefers-reduced-data. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Honor prefers-reduced-data.","thread_management":"Keep gesture/motion state on the animation thread for Honor prefers-reduced-data; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Honor prefers-reduced-data; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Honor prefers-reduced-data."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Honor prefers-reduced-data.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Honor prefers-reduced-data.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Honor prefers-reduced-data."},"solution":"// anim-172: production motion implementation for Honor prefers-reduced-data\nimport {motion, useReducedMotion} from 'framer-motion';\nexport function HonorPrefersReducedDataMotion({open, children}) {\n  const reduced = useReducedMotion();\n  return <motion.section aria-live=\"polite\" initial={{opacity:0, y: reduced ? 0 : 12}} animate={{opacity:1, y:0}} exit={{opacity:0, y: reduced ? 0 : -8}} transition={{duration: reduced ? 0 : 0.18, ease:'easeOut'}}>{children}</motion.section>;\n}\n// Verify: reduced-motion snapshot, keyboard focus stays on the active control, and no layout/paint long task over 50ms.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Honor prefers-reduced-data."],"what_breaks_naive_approach":"A naive animation for Honor prefers-reduced-data only checks the first visual impression. It breaks when perf accessibility on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-173","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated like heart pop","design_intent":"Design intent for anim-173: make Animated like heart pop legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-173: derive timing and easing from the Animated like heart pop state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated like heart pop."],"motion_reasoning":"anim-173 uses framer motion on web for Animated like heart pop. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated like heart pop.","thread_management":"Keep gesture/motion state on the animation thread for Animated like heart pop; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated like heart pop; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated like heart pop."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated like heart pop.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated like heart pop.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated like heart pop."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\n\nexport function AnimatedState({ open, children }: { open: boolean; children: React.ReactNode }) {\n  const reduced = useReducedMotion();\n  return (\n    <motion.div\n      initial={reduced ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={open ? { opacity: 1, y: 0 } : { opacity: 0, y: reduced ? 0 : 12 }}\n      transition={{ duration: reduced ? 0.01 : 0.18, ease: 'easeOut' }}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated like heart pop: reduced motion is explicit, only transform/opacity animate, and semantic state is controlled by props.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated like heart pop."],"what_breaks_naive_approach":"A naive animation for Animated like heart pop only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-174","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated bottom nav","design_intent":"Design intent for anim-174: make Animated bottom nav legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-174: derive timing and easing from the Animated bottom nav state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated bottom nav."],"motion_reasoning":"anim-174 uses reanimated on mobile for Animated bottom nav. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated bottom nav.","thread_management":"Keep gesture/motion state on the animation thread for Animated bottom nav; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated bottom nav; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated bottom nav."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated bottom nav.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated bottom nav.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated bottom nav."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedBottomNav({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated bottom nav\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated bottom nav."],"what_breaks_naive_approach":"A naive animation for Animated bottom nav only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-175","category":"motion_principles","difficulty":"expert","platform":"both","title":"Provide skip for animations","design_intent":"Design intent for anim-175: make Provide skip for animations legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-175: derive timing and easing from the Provide skip for animations state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Forced intro"],"motion_reasoning":"anim-175 uses motion principles on both for Provide skip for animations. The motion must express Users can opt out of intro., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Provide skip for animations.","thread_management":"Keep gesture/motion state on the animation thread for Provide skip for animations; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Provide skip for animations; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Provide skip for animations."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Provide skip for animations.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Provide skip for animations.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Provide skip for animations."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function ProvideSkipForAnimations({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Provide skip for animations\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Provide skip for animations."],"what_breaks_naive_approach":"A naive animation for Provide skip for animations only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-176","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated price update","design_intent":"Design intent for anim-176: make Animated price update legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-176: derive timing and easing from the Animated price update state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated price update."],"motion_reasoning":"anim-176 uses framer motion on web for Animated price update. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated price update.","thread_management":"Keep gesture/motion state on the animation thread for Animated price update; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated price update; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated price update."},"accessibility":{"reduced_motion":"Show final","screen_reader":"announce","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated price update."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\n\nexport function AnimatedState({ open, children }: { open: boolean; children: React.ReactNode }) {\n  const reduced = useReducedMotion();\n  return (\n    <motion.div\n      initial={reduced ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={open ? { opacity: 1, y: 0 } : { opacity: 0, y: reduced ? 0 : 12 }}\n      transition={{ duration: reduced ? 0.01 : 0.18, ease: 'easeOut' }}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated price update: reduced motion is explicit, only transform/opacity animate, and semantic state is controlled by props.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated price update."],"what_breaks_naive_approach":"A naive animation for Animated price update only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-177","category":"reanimated","difficulty":"expert","platform":"mobile","title":"Animated drawer open","design_intent":"Design intent for anim-177: make Animated drawer open legible as a reanimated state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-177: derive timing and easing from the Animated drawer open state change itself; use reanimated platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated drawer open."],"motion_reasoning":"anim-177 uses reanimated on mobile for Animated drawer open. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated drawer open.","thread_management":"Keep gesture/motion state on the animation thread for Animated drawer open; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated drawer open; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated drawer open."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated drawer open.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated drawer open.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated drawer open."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedDrawerOpen({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated drawer open\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated drawer open."],"what_breaks_naive_approach":"A naive animation for Animated drawer open only checks the first visual impression. It breaks when reanimated on mobile is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-178","category":"perf_accessibility","difficulty":"expert","platform":"web","title":"Avoiding paint storms","design_intent":"Design intent for anim-178: make Avoiding paint storms legible as a perf accessibility state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-178: derive timing and easing from the Avoiding paint storms state change itself; use perf accessibility platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Many paint changes"],"motion_reasoning":"anim-178 uses perf accessibility on web for Avoiding paint storms. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding paint storms.","thread_management":"Keep gesture/motion state on the animation thread for Avoiding paint storms; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Avoiding paint storms; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Avoiding paint storms."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding paint storms.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding paint storms.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding paint storms."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AvoidingPaintStorms({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Avoiding paint storms\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Avoiding paint storms."],"what_breaks_naive_approach":"A naive animation for Avoiding paint storms only checks the first visual impression. It breaks when perf accessibility on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-179","category":"framer_motion","difficulty":"expert","platform":"web","title":"Animated avatar group","design_intent":"Design intent for anim-179: make Animated avatar group legible as a framer motion state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-179: derive timing and easing from the Animated avatar group state change itself; use framer motion platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated avatar group."],"motion_reasoning":"anim-179 uses framer motion on web for Animated avatar group. The motion must express The design intent is to make the state change legible without adding decorative motion or layout instability., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated avatar group.","thread_management":"Keep gesture/motion state on the animation thread for Animated avatar group; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Animated avatar group; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Animated avatar group."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated avatar group.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated avatar group.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Animated avatar group."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function AnimatedAvatarGroup({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Animated avatar group\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Animated avatar group."],"what_breaks_naive_approach":"A naive animation for Animated avatar group only checks the first visual impression. It breaks when framer motion on web is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}
{"id":"anim-180","category":"motion_principles","difficulty":"expert","platform":"both","title":"Closing motion philosophy","design_intent":"Design intent for anim-180: make Closing motion philosophy legible as a motion principles state change, preserve focus and layout stability, and communicate the same meaning when reduced motion removes animation.","reference_inspiration":"Reference for anim-180: derive timing and easing from the Closing motion philosophy state change itself; use motion principles platform conventions only to calibrate duration, interruption, and reduced-motion behavior.","anti_patterns_to_avoid":["Showy motion"],"motion_reasoning":"anim-180 uses motion principles on both for Closing motion philosophy. The motion must express Motion done well is invisible., survive interruption, preserve focus order, honor reduced motion, and keep animated work on transform or opacity where possible.","performance_strategy":{"what_animates":"Respect prefers-reduced-motion, focus order, and interruption behavior for Closing motion philosophy.","thread_management":"Keep gesture/motion state on the animation thread for Closing motion philosophy; React state updates only at committed milestones.","gpu_considerations":"Animate only transform/opacity for Closing motion philosophy; avoid layout-affecting properties and validate no long paint blocks.","interrupt_handling":"Interrupt by cancelling the active timeline, preserving focus, and snapping to the nearest semantic state for Closing motion philosophy."},"accessibility":{"reduced_motion":"Respect prefers-reduced-motion, focus order, and interruption behavior for Closing motion philosophy.","screen_reader":"Respect prefers-reduced-motion, focus order, and interruption behavior for Closing motion philosophy.","vestibular_safety":"Respect prefers-reduced-motion, focus order, and interruption behavior for Closing motion philosophy."},"solution":"import { motion, useReducedMotion } from 'framer-motion';\nimport { useEffect, useRef } from 'react';\n\nexport function ClosingMotionPhilosophy({ active, children }: { active: boolean; children: React.ReactNode }) {\n  const reduce = useReducedMotion();\n  const ref = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!active) return;\n    const node = ref.current;\n    node?.setAttribute('data-motion-state', 'entered');\n    return () => node?.removeAttribute('data-motion-state');\n  }, [active]);\n\n  return (\n    <motion.div\n      ref={ref}\n      initial={reduce ? { opacity: 0 } : { opacity: 0, y: 12 }}\n      animate={active ? { opacity: 1, y: 0 } : { opacity: 0, y: reduce ? 0 : 12 }}\n      transition={{ duration: reduce ? 0.01 : 0.18, ease: 'easeOut' }}\n      aria-hidden={!active}\n    >\n      {children}\n    </motion.div>\n  );\n}\n\n// Closing motion philosophy\n// Standalone behavior: transform/opacity only, reduced-motion path, cleanup on exit, and semantic active state separated from animation timing.","interaction_details":["Respect prefers-reduced-motion, focus order, and interruption behavior for Closing motion philosophy."],"what_breaks_naive_approach":"A naive animation for Closing motion philosophy only checks the first visual impression. It breaks when motion principles on both is interrupted, focus changes mid-transition, reduced-motion users need an instant equivalent, or layout and paint work exceed the frame budget."}

{"id":"perf-121","category":"react_rendering","difficulty":"expert","title":"Component co-location for code splitting","performance_problem":{"symptom":"Heavy feature in main bundle","metric_impact":"Track render_count_per_action: React Profiler shows only components whose inputs changed re-render.","business_impact":"If Component co-location for code splitting regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["Bundle visualizer","Feature pulled by main"],"anti_patterns_to_avoid":["Adding memoization before measuring renders","Moving state without proving ownership or invalidation boundaries","Hiding work behind Suspense or debounce while INP remains slow"],"reasoning":"perf-121 treats Component co-location for code splitting as a react_rendering bottleneck. The fix is valid only when React Profiler, browser INP trace, render-count assertions, and interaction replay show render_count_per_action (count) meeting the row target: only components with changed inputs re-render; correctness output must remain unchanged and the rollback path must be explicit.","solution":"// perf-121: measured react_rendering optimization for Component co-location for code splitting\nconst fixture = await loadPerfFixture('perf-121', {users: 171, rows: 1500, network: '4g-slow'});\nconst before = await profile('render_count_per_action', () => runBaseline(fixture));\nconst optimized = await applyReactRenderingOptimization(fixture);\nconst after = await profile('render_count_per_action', () => optimized.run());\nassertBelow(after.render_count_per_action, 27);\nassertNoCorrectnessDiff(before.output, after.output);\nrecordPerfBudget('perf-121', {metric: 'render_count_per_action', before: before.render_count_per_action, after: after.render_count_per_action, owner: 'performance'});","before_metrics":"perf-121 baseline for Component co-location for code splitting: record render_count_per_action in count, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-121 optimized result for Component co-location for code splitting: render_count_per_action must meet target (only components with changed inputs re-render), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Component co-location for code splitting adds state-boundary and memoization discipline; keep the component model readable and remove the optimization if Profiler evidence disappears.","regression_prevention":"React Profiler fixture for Component co-location for code splitting asserts render_count_per_action; browser INP budget and render-count snapshots fail the pull request on regression.","what_breaks_naive_approach":"A naive fix for Component co-location for code splitting changes the nearest slow line without measuring render_count_per_action. It can move cost into another react_rendering boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-122","category":"database","difficulty":"expert","title":"Avoiding pg_dump in transaction","performance_problem":{"symptom":"Backups block writes","metric_impact":"Track primary_io_wait_ms: primary read/write latency unchanged while backup runs from a replica.","business_impact":"If Avoiding pg_dump in transaction regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["pg_dump default uses SERIALIZABLE","Long lock"],"anti_patterns_to_avoid":["pg_dump on primary in peak"],"reasoning":"perf-122 treats Avoiding pg_dump in transaction as a database bottleneck. The fix is valid only when EXPLAIN (ANALYZE, BUFFERS), pg_stat_statements, lock/connection wait telemetry, and a rollback rehearsal show primary_io_wait_ms (milliseconds) meeting the row target: primary IO wait unchanged while backup runs off-primary; correctness output must remain unchanged and the rollback path must be explicit.","solution":"// perf-122: measured database optimization for Avoiding pg_dump in transaction\nconst fixture = await loadPerfFixture('perf-122', {users: 172, rows: 2000, network: '4g-slow'});\nconst before = await profile('query_p95_ms', () => runBaseline(fixture));\nconst optimized = await applyDatabaseOptimization(fixture);\nconst after = await profile('query_p95_ms', () => optimized.run());\nassertBelow(after.query_p95_ms, 210);\nassertNoCorrectnessDiff(before.output, after.output);\nrecordPerfBudget('perf-122', {metric: 'query_p95_ms', before: before.query_p95_ms, after: after.query_p95_ms, owner: 'performance'});","before_metrics":"perf-122 baseline for Avoiding pg_dump in transaction: record primary_io_wait_ms in milliseconds, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-122 optimized result for Avoiding pg_dump in transaction: primary_io_wait_ms must meet target (primary IO wait unchanged while backup runs off-primary), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Avoiding pg_dump in transaction may add index/storage/operational complexity; keep the old path or rollback migration until EXPLAIN and production metrics prove the database budget holds.","regression_prevention":"CI stores an EXPLAIN/pg_stat snapshot for Avoiding pg_dump in transaction; fail if primary_io_wait_ms regresses, row scans/locks increase, or the rollback migration is missing.","what_breaks_naive_approach":"A naive fix for Avoiding pg_dump in transaction changes the nearest slow line without measuring primary_io_wait_ms. It can move cost into another database boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-123","category":"network","difficulty":"expert","title":"Reducing JS execution time","performance_problem":{"symptom":"3rd-party scripts dominate","metric_impact":"Track critical_waterfall_ms: critical request waterfall shortens without increasing error rate or cache staleness.","business_impact":"If Reducing JS execution time regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["DevTools Coverage","3rd-party top of list"],"anti_patterns_to_avoid":["Sync 3rd-party","No partytown"],"reasoning":"perf-123 treats Reducing JS execution time as a network bottleneck. The fix is valid only when network waterfall replay, cache/preflight counters, retry logs, and server timing headers show critical_waterfall_ms (milliseconds) meeting the row target: critical request waterfall shortens without cache correctness loss; correctness output must remain unchanged and the rollback path must be explicit.","solution":"// perf-123: measured network optimization for Reducing JS execution time\nconst fixture = await loadPerfFixture('perf-123', {users: 173, rows: 2500, network: '4g-slow'});\nconst before = await profile('critical_request_waterfall_ms', () => runBaseline(fixture));\nconst optimized = await applyNetworkOptimization(fixture);\nconst after = await profile('critical_request_waterfall_ms', () => optimized.run());\nassertBelow(after.critical_request_waterfall_ms, 719);\nassertNoCorrectnessDiff(before.output, after.output);\nrecordPerfBudget('perf-123', {metric: 'critical_request_waterfall_ms', before: before.critical_request_waterfall_ms, after: after.critical_request_waterfall_ms, owner: 'performance'});","before_metrics":"perf-123 baseline for Reducing JS execution time: record critical_waterfall_ms in milliseconds, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-123 optimized result for Reducing JS execution time: critical_waterfall_ms must meet target (critical request waterfall shortens without cache correctness loss), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Reducing JS execution time can trade latency for cache staleness or server concurrency; document freshness bounds and backpressure limits.","regression_prevention":"Waterfall replay for Reducing JS execution time records critical_waterfall_ms, cache hit ratio, preflight count, and bounded retry behavior.","what_breaks_naive_approach":"A naive fix for Reducing JS execution time changes the nearest slow line without measuring critical_waterfall_ms. It can move cost into another network boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-124","category":"core_web_vitals","difficulty":"expert","title":"Avoiding @import in CSS","performance_problem":{"symptom":"CSS @import chains","metric_impact":"Track p75_lcp_ms: p75 LCP improves on slow 4G without increasing CLS or INP.","business_impact":"If Avoiding @import in CSS regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["Waterfall: chained CSS","Each @import = new request"],"anti_patterns_to_avoid":["@import chains","Build-time inline missing"],"reasoning":"perf-124 treats Avoiding @import in CSS as a core_web_vitals bottleneck. The fix is valid only when Lighthouse trace, Chrome performance recording, and web-vitals RUM sample show p75_lcp_ms (milliseconds) meeting the row target: p75 LCP improves without worsening CLS or INP; correctness output must remain unchanged and the rollback path must be explicit.","solution":"import { visualizer } from 'rollup-plugin-visualizer';\nexport default { plugins: [visualizer({ gzipSize: true, brotliSize: true })] };\n\nconst budget = { gzipBytes: 180_000, brotliBytes: 140_000 };\nawait assertBundleBudget('dist/assets/app.js', budget);\n\n// Replace broad imports with direct ESM imports, split rare routes, and store the visualizer artifact for review.","before_metrics":"perf-124 baseline for Avoiding @import in CSS: record p75_lcp_ms in milliseconds, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-124 optimized result for Avoiding @import in CSS: p75_lcp_ms must meet target (p75 LCP improves without worsening CLS or INP), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Avoiding @import in CSS can trade one vital for another; ship only with LCP, CLS, and INP measured together on slow-device fixtures.","regression_prevention":"Lighthouse plus web-vitals RUM budget for Avoiding @import in CSS tracks p75_lcp_ms; test mobile/desktop traces and block regressions in adjacent vitals.","what_breaks_naive_approach":"A naive fix for Avoiding @import in CSS changes the nearest slow line without measuring p75_lcp_ms. It can move cost into another core_web_vitals boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-125","category":"memory_leaks","difficulty":"expert","title":"Avoiding leaks in Vue/Svelte","performance_problem":{"symptom":"Listeners not cleaned","metric_impact":"Track heap_growth_after_gc_mb: heap and listener counts return to baseline after repeated mount/unmount cycles.","business_impact":"If Avoiding leaks in Vue/Svelte regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["Heap","Listener count grows"],"anti_patterns_to_avoid":["No onUnmounted cleanup"],"reasoning":"perf-125 treats Avoiding leaks in Vue/Svelte as a memory_leaks bottleneck. The fix is valid only when heap snapshots after forced GC, listener/timer counts, and repeated lifecycle stress tests show heap_growth_after_gc_mb (megabytes) meeting the row target: heap growth returns to baseline after repeated lifecycle cycles; correctness output must remain unchanged and the rollback path must be explicit.","solution":"// perf-125: measured memory_leaks optimization for Avoiding leaks in Vue/Svelte\nconst fixture = await loadPerfFixture('perf-125', {users: 175, rows: 3500, network: '4g-slow'});\nconst before = await profile('heap_after_10min_mb', () => runBaseline(fixture));\nconst optimized = await applyMemoryLeaksOptimization(fixture);\nconst after = await profile('heap_after_10min_mb', () => optimized.run());\nassertBelow(after.heap_after_10min_mb, 312);\nassertNoCorrectnessDiff(before.output, after.output);\nrecordPerfBudget('perf-125', {metric: 'heap_after_10min_mb', before: before.heap_after_10min_mb, after: after.heap_after_10min_mb, owner: 'performance'});","before_metrics":"perf-125 baseline for Avoiding leaks in Vue/Svelte: record heap_growth_after_gc_mb in megabytes, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-125 optimized result for Avoiding leaks in Vue/Svelte: heap_growth_after_gc_mb must meet target (heap growth returns to baseline after repeated lifecycle cycles), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Avoiding leaks in Vue/Svelte adds cleanup and cancellation plumbing; preserve correctness by testing abort/error paths, not just heap reduction.","regression_prevention":"Lifecycle stress test for Avoiding leaks in Vue/Svelte compares heap snapshots, listener counts, and heap_growth_after_gc_mb after forced GC.","what_breaks_naive_approach":"A naive fix for Avoiding leaks in Vue/Svelte changes the nearest slow line without measuring heap_growth_after_gc_mb. It can move cost into another memory_leaks boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-126","category":"nodejs","difficulty":"expert","title":"Caching DNS","performance_problem":{"symptom":"DNS lookup per outbound","metric_impact":"Track event_loop_lag_p95_ms: p95 event-loop lag remains below 50ms under representative load.","business_impact":"If Caching DNS regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["Many DNS","node-cache-dns absent"],"anti_patterns_to_avoid":["Doing CPU or sync I/O on the request path","Adding workers without pool bounds or backpressure","Measuring throughput while ignoring event-loop lag"],"reasoning":"perf-126 treats Caching DNS as a nodejs bottleneck. The fix is valid only when event-loop delay histogram, CPU profile, load test, and queue-depth telemetry show event_loop_lag_p95_ms (milliseconds) meeting the row target: p95 event-loop lag below 50ms under load; correctness output must remain unchanged and the rollback path must be explicit.","solution":"import CacheableLookup from 'cacheable-lookup';\nimport {Agent} from 'undici';\nconst lookup = new CacheableLookup();\nconst agent = new Agent({connect: {lookup: lookup.lookup}});","before_metrics":"perf-126 baseline for Caching DNS: record event_loop_lag_p95_ms in milliseconds, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-126 optimized result for Caching DNS: event_loop_lag_p95_ms must meet target (p95 event-loop lag below 50ms under load), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Caching DNS may move cost into queues/workers; expose queue depth, timeouts, and fallback behavior so throughput does not hide user latency.","regression_prevention":"Load test for Caching DNS records event_loop_lag_p95_ms, CPU profile, queue depth, and rejection/backpressure behavior.","what_breaks_naive_approach":"A naive fix for Caching DNS changes the nearest slow line without measuring event_loop_lag_p95_ms. It can move cost into another nodejs boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-127","category":"react_rendering","difficulty":"expert","title":"Avoiding Provider hell","performance_problem":{"symptom":"15 nested providers","metric_impact":"Track render_count_per_action: React Profiler shows only components whose inputs changed re-render.","business_impact":"If Avoiding Provider hell regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["Many providers","Mostly per-feature"],"anti_patterns_to_avoid":["Provider per concern","No composition util"],"reasoning":"perf-127 treats Avoiding Provider hell as a react_rendering bottleneck. The fix is valid only when React Profiler, browser INP trace, render-count assertions, and interaction replay show render_count_per_action (count) meeting the row target: only components with changed inputs re-render; correctness output must remain unchanged and the rollback path must be explicit.","solution":"function compose(...providers) {\n  return ({children}) => providers.reduceRight((acc, P) => <P>{acc}</P>, children);\n}\nconst AppProviders = compose(ThemeProvider, AuthProvider, QueryProvider);","before_metrics":"perf-127 baseline for Avoiding Provider hell: record render_count_per_action in count, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-127 optimized result for Avoiding Provider hell: render_count_per_action must meet target (only components with changed inputs re-render), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Avoiding Provider hell adds state-boundary and memoization discipline; keep the component model readable and remove the optimization if Profiler evidence disappears.","regression_prevention":"React Profiler fixture for Avoiding Provider hell asserts render_count_per_action; browser INP budget and render-count snapshots fail the pull request on regression.","what_breaks_naive_approach":"A naive fix for Avoiding Provider hell changes the nearest slow line without measuring render_count_per_action. It can move cost into another react_rendering boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-128","category":"bundle_size","difficulty":"expert","title":"Removing dev-only code in prod","performance_problem":{"symptom":"Dev assertions in prod","metric_impact":"Track initial_js_kb_gzip: gzip initial JS and route chunk counts stay within bundle budget.","business_impact":"If Removing dev-only code in prod regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["NODE_ENV checks present","Build replaces?"],"anti_patterns_to_avoid":["No NODE_ENV define","No dead-code elim"],"reasoning":"perf-128 treats Removing dev-only code in prod as a bundle_size bottleneck. The fix is valid only when bundle analyzer output, gzip/brotli byte budgets, and route chunk inspection show initial_js_kb_gzip (kilobytes) meeting the row target: initial gzip JS stays under the route budget; correctness output must remain unchanged and the rollback path must be explicit.","solution":"define: {'process.env.NODE_ENV': JSON.stringify(mode)}\n// in code\nif (process.env.NODE_ENV !== 'production') console.warn('dev only');","before_metrics":"perf-128 baseline for Removing dev-only code in prod: record initial_js_kb_gzip in kilobytes, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-128 optimized result for Removing dev-only code in prod: initial_js_kb_gzip must meet target (initial gzip JS stays under the route budget), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Removing dev-only code in prod can increase dependency or chunk-management complexity; keep lockfile review and integration tests with the byte budget.","regression_prevention":"Bundle analyzer artifact and gzip/brotli budget for Removing dev-only code in prod fail CI when initial_js_kb_gzip grows or a duplicate dependency returns.","what_breaks_naive_approach":"A naive fix for Removing dev-only code in prod changes the nearest slow line without measuring initial_js_kb_gzip. It can move cost into another bundle_size boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-129","category":"database","difficulty":"expert","title":"Sequential vs random IO","performance_problem":{"symptom":"Random IO patterns slow","metric_impact":"Track db_query_p95_ms: EXPLAIN ANALYZE shows fewer rows scanned and p95 query latency inside the endpoint budget.","business_impact":"If Sequential vs random IO regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["pg_stat: high random reads","No clustering"],"anti_patterns_to_avoid":["Changing indexes or pools without EXPLAIN/pg_stat evidence","Optimizing the happy query while ignoring locks, replicas, and rollback","Letting ORM defaults hide query count or transaction scope"],"reasoning":"perf-129 treats Sequential vs random IO as a database bottleneck. The fix is valid only when EXPLAIN (ANALYZE, BUFFERS), pg_stat_statements, lock/connection wait telemetry, and a rollback rehearsal show db_query_p95_ms (milliseconds) meeting the row target: p95 query latency drops and EXPLAIN rows scanned decreases; correctness output must remain unchanged and the rollback path must be explicit.","solution":"// perf-129: measured database optimization for Sequential vs random IO\nconst fixture = await loadPerfFixture('perf-129', {users: 179, rows: 5500, network: '4g-slow'});\nconst before = await profile('query_p95_ms', () => runBaseline(fixture));\nconst optimized = await applyDatabaseOptimization(fixture);\nconst after = await profile('query_p95_ms', () => optimized.run());\nassertBelow(after.query_p95_ms, 234);\nassertNoCorrectnessDiff(before.output, after.output);\nrecordPerfBudget('perf-129', {metric: 'query_p95_ms', before: before.query_p95_ms, after: after.query_p95_ms, owner: 'performance'});","before_metrics":"perf-129 baseline for Sequential vs random IO: record db_query_p95_ms in milliseconds, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-129 optimized result for Sequential vs random IO: db_query_p95_ms must meet target (p95 query latency drops and EXPLAIN rows scanned decreases), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Sequential vs random IO may add index/storage/operational complexity; keep the old path or rollback migration until EXPLAIN and production metrics prove the database budget holds.","regression_prevention":"CI stores an EXPLAIN/pg_stat snapshot for Sequential vs random IO; fail if db_query_p95_ms regresses, row scans/locks increase, or the rollback migration is missing.","what_breaks_naive_approach":"A naive fix for Sequential vs random IO changes the nearest slow line without measuring db_query_p95_ms. It can move cost into another database boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-130","category":"network","difficulty":"expert","title":"Edge caching API responses","performance_problem":{"symptom":"API hit on every call","metric_impact":"Track critical_waterfall_ms: critical request waterfall shortens without increasing error rate or cache staleness.","business_impact":"If Edge caching API responses regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["TTFB high","No edge cache"],"anti_patterns_to_avoid":["No Cache-Control","Private when public OK"],"reasoning":"perf-130 treats Edge caching API responses as a network bottleneck. The fix is valid only when network waterfall replay, cache/preflight counters, retry logs, and server timing headers show critical_waterfall_ms (milliseconds) meeting the row target: critical request waterfall shortens without cache correctness loss; correctness output must remain unchanged and the rollback path must be explicit.","solution":"// perf-130: measured network optimization for Edge caching API responses\nconst fixture = await loadPerfFixture('perf-130', {users: 180, rows: 6000, network: '4g-slow'});\nconst before = await profile('critical_request_waterfall_ms', () => runBaseline(fixture));\nconst optimized = await applyNetworkOptimization(fixture);\nconst after = await profile('critical_request_waterfall_ms', () => optimized.run());\nassertBelow(after.critical_request_waterfall_ms, 744);\nassertNoCorrectnessDiff(before.output, after.output);\nrecordPerfBudget('perf-130', {metric: 'critical_request_waterfall_ms', before: before.critical_request_waterfall_ms, after: after.critical_request_waterfall_ms, owner: 'performance'});","before_metrics":"perf-130 baseline for Edge caching API responses: record critical_waterfall_ms in milliseconds, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-130 optimized result for Edge caching API responses: critical_waterfall_ms must meet target (critical request waterfall shortens without cache correctness loss), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Edge caching API responses can trade latency for cache staleness or server concurrency; document freshness bounds and backpressure limits.","regression_prevention":"Waterfall replay for Edge caching API responses records critical_waterfall_ms, cache hit ratio, preflight count, and bounded retry behavior.","what_breaks_naive_approach":"A naive fix for Edge caching API responses changes the nearest slow line without measuring critical_waterfall_ms. It can move cost into another network boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-131","category":"react_rendering","difficulty":"expert","title":"Identifying re-render causes","performance_problem":{"symptom":"Don't know why re-rendered","metric_impact":"Track render_count_per_action: React Profiler shows only components whose inputs changed re-render.","business_impact":"If Identifying re-render causes regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["Profiler 'why did this render?'","Hovering commit shows"],"anti_patterns_to_avoid":["Adding memoization before measuring renders","Moving state without proving ownership or invalidation boundaries","Hiding work behind Suspense or debounce while INP remains slow"],"reasoning":"perf-131 treats Identifying re-render causes as a react_rendering bottleneck. The fix is valid only when React Profiler, browser INP trace, render-count assertions, and interaction replay show render_count_per_action (count) meeting the row target: only components with changed inputs re-render; correctness output must remain unchanged and the rollback path must be explicit.","solution":"import whyDidYouRender from '@welldone-software/why-did-you-render';\nif (process.env.NODE_ENV === 'development') whyDidYouRender(React, {trackAllPureComponents: true});","before_metrics":"perf-131 baseline for Identifying re-render causes: record render_count_per_action in count, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-131 optimized result for Identifying re-render causes: render_count_per_action must meet target (only components with changed inputs re-render), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Identifying re-render causes adds state-boundary and memoization discipline; keep the component model readable and remove the optimization if Profiler evidence disappears.","regression_prevention":"React Profiler fixture for Identifying re-render causes asserts render_count_per_action; browser INP budget and render-count snapshots fail the pull request on regression.","what_breaks_naive_approach":"A naive fix for Identifying re-render causes changes the nearest slow line without measuring render_count_per_action. It can move cost into another react_rendering boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-132","category":"bundle_size","difficulty":"expert","title":"Avoiding duplicate React copies","performance_problem":{"symptom":"Two React copies","metric_impact":"Track initial_js_kb_gzip: gzip initial JS and route chunk counts stay within bundle budget.","business_impact":"If Avoiding duplicate React copies regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["pnpm why react","Multiple"],"anti_patterns_to_avoid":["Workspace deps misconfigured"],"reasoning":"perf-132 treats Avoiding duplicate React copies as a bundle_size bottleneck. The fix is valid only when bundle analyzer output, gzip/brotli byte budgets, and route chunk inspection show initial_js_kb_gzip (kilobytes) meeting the row target: initial gzip JS stays under the route budget; correctness output must remain unchanged and the rollback path must be explicit.","solution":"// perf-132: measured bundle_size optimization for Avoiding duplicate React copies\nconst fixture = await loadPerfFixture('perf-132', {users: 182, rows: 7000, network: '4g-slow'});\nconst before = await profile('main_bundle_gzip_kb', () => runBaseline(fixture));\nconst optimized = await applyBundleSizeOptimization(fixture);\nconst after = await profile('main_bundle_gzip_kb', () => optimized.run());\nassertBelow(after.main_bundle_gzip_kb, 471);\nassertNoCorrectnessDiff(before.output, after.output);\nrecordPerfBudget('perf-132', {metric: 'main_bundle_gzip_kb', before: before.main_bundle_gzip_kb, after: after.main_bundle_gzip_kb, owner: 'performance'});","before_metrics":"perf-132 baseline for Avoiding duplicate React copies: record initial_js_kb_gzip in kilobytes, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-132 optimized result for Avoiding duplicate React copies: initial_js_kb_gzip must meet target (initial gzip JS stays under the route budget), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Avoiding duplicate React copies can increase dependency or chunk-management complexity; keep lockfile review and integration tests with the byte budget.","regression_prevention":"Bundle analyzer artifact and gzip/brotli budget for Avoiding duplicate React copies fail CI when initial_js_kb_gzip grows or a duplicate dependency returns.","what_breaks_naive_approach":"A naive fix for Avoiding duplicate React copies changes the nearest slow line without measuring initial_js_kb_gzip. It can move cost into another bundle_size boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-133","category":"core_web_vitals","difficulty":"expert","title":"Reducing layout work","performance_problem":{"symptom":"Style recalc dominant","metric_impact":"Track p75_lcp_ms: p75 LCP improves on slow 4G without increasing CLS or INP.","business_impact":"If Reducing layout work regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["Performance > Recalculate Style","CSS selectors complex"],"anti_patterns_to_avoid":["Universal selectors","Deep descendant"],"reasoning":"perf-133 treats Reducing layout work as a core_web_vitals bottleneck. The fix is valid only when Lighthouse trace, Chrome performance recording, and web-vitals RUM sample show p75_lcp_ms (milliseconds) meeting the row target: p75 LCP improves without worsening CLS or INP; correctness output must remain unchanged and the rollback path must be explicit.","solution":"// perf-133: measured core_web_vitals optimization for Reducing layout work\nconst fixture = await loadPerfFixture('perf-133', {users: 183, rows: 7500, network: '4g-slow'});\nconst before = await profile('p75_lcp_ms', () => runBaseline(fixture));\nconst optimized = await applyCoreWebVitalsOptimization(fixture);\nconst after = await profile('p75_lcp_ms', () => optimized.run());\nassertBelow(after.p75_lcp_ms, 2418);\nassertNoCorrectnessDiff(before.output, after.output);\nrecordPerfBudget('perf-133', {metric: 'p75_lcp_ms', before: before.p75_lcp_ms, after: after.p75_lcp_ms, owner: 'performance'});","before_metrics":"perf-133 baseline for Reducing layout work: record p75_lcp_ms in milliseconds, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-133 optimized result for Reducing layout work: p75_lcp_ms must meet target (p75 LCP improves without worsening CLS or INP), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Reducing layout work can trade one vital for another; ship only with LCP, CLS, and INP measured together on slow-device fixtures.","regression_prevention":"Lighthouse plus web-vitals RUM budget for Reducing layout work tracks p75_lcp_ms; test mobile/desktop traces and block regressions in adjacent vitals.","what_breaks_naive_approach":"A naive fix for Reducing layout work changes the nearest slow line without measuring p75_lcp_ms. It can move cost into another core_web_vitals boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-134","category":"memory_leaks","difficulty":"expert","title":"Detecting native module leaks","performance_problem":{"symptom":"RSS grows; heap stable","metric_impact":"Track heap_growth_after_gc_mb: heap and listener counts return to baseline after repeated mount/unmount cycles.","business_impact":"If Detecting native module leaks regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["heap snapshot doesn't grow","process.memoryUsage RSS does","Native lib"],"anti_patterns_to_avoid":["Native module without cleanup"],"reasoning":"perf-134 treats Detecting native module leaks as a memory_leaks bottleneck. The fix is valid only when heap snapshots after forced GC, listener/timer counts, and repeated lifecycle stress tests show heap_growth_after_gc_mb (megabytes) meeting the row target: heap growth returns to baseline after repeated lifecycle cycles; correctness output must remain unchanged and the rollback path must be explicit.","solution":"// perf-134: measured memory_leaks optimization for Detecting native module leaks\nconst fixture = await loadPerfFixture('perf-134', {users: 184, rows: 8000, network: '4g-slow'});\nconst before = await profile('heap_after_10min_mb', () => runBaseline(fixture));\nconst optimized = await applyMemoryLeaksOptimization(fixture);\nconst after = await profile('heap_after_10min_mb', () => optimized.run());\nassertBelow(after.heap_after_10min_mb, 305);\nassertNoCorrectnessDiff(before.output, after.output);\nrecordPerfBudget('perf-134', {metric: 'heap_after_10min_mb', before: before.heap_after_10min_mb, after: after.heap_after_10min_mb, owner: 'performance'});","before_metrics":"perf-134 baseline for Detecting native module leaks: record heap_growth_after_gc_mb in megabytes, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-134 optimized result for Detecting native module leaks: heap_growth_after_gc_mb must meet target (heap growth returns to baseline after repeated lifecycle cycles), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Detecting native module leaks adds cleanup and cancellation plumbing; preserve correctness by testing abort/error paths, not just heap reduction.","regression_prevention":"Lifecycle stress test for Detecting native module leaks compares heap snapshots, listener counts, and heap_growth_after_gc_mb after forced GC.","what_breaks_naive_approach":"A naive fix for Detecting native module leaks changes the nearest slow line without measuring heap_growth_after_gc_mb. It can move cost into another memory_leaks boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-135","category":"nodejs","difficulty":"expert","title":"Streaming JSON parse","performance_problem":{"symptom":"Big JSON OOMs","metric_impact":"Track event_loop_lag_p95_ms: p95 event-loop lag remains below 50ms under representative load.","business_impact":"If Streaming JSON parse regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["JSON.parse on huge string","Heap spike"],"anti_patterns_to_avoid":["JSON.parse on huge","No streaming"],"reasoning":"perf-135 treats Streaming JSON parse as a nodejs bottleneck. The fix is valid only when event-loop delay histogram, CPU profile, load test, and queue-depth telemetry show event_loop_lag_p95_ms (milliseconds) meeting the row target: p95 event-loop lag below 50ms under load; correctness output must remain unchanged and the rollback path must be explicit.","solution":"// perf-135: measured nodejs optimization for Streaming JSON parse\nconst fixture = await loadPerfFixture('perf-135', {users: 185, rows: 8500, network: '4g-slow'});\nconst before = await profile('event_loop_delay_p99_ms', () => runBaseline(fixture));\nconst optimized = await applyNodejsOptimization(fixture);\nconst after = await profile('event_loop_delay_p99_ms', () => optimized.run());\nassertBelow(after.event_loop_delay_p99_ms, 73);\nassertNoCorrectnessDiff(before.output, after.output);\nrecordPerfBudget('perf-135', {metric: 'event_loop_delay_p99_ms', before: before.event_loop_delay_p99_ms, after: after.event_loop_delay_p99_ms, owner: 'performance'});","before_metrics":"perf-135 baseline for Streaming JSON parse: record event_loop_lag_p95_ms in milliseconds, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-135 optimized result for Streaming JSON parse: event_loop_lag_p95_ms must meet target (p95 event-loop lag below 50ms under load), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Streaming JSON parse may move cost into queues/workers; expose queue depth, timeouts, and fallback behavior so throughput does not hide user latency.","regression_prevention":"Load test for Streaming JSON parse records event_loop_lag_p95_ms, CPU profile, queue depth, and rejection/backpressure behavior.","what_breaks_naive_approach":"A naive fix for Streaming JSON parse changes the nearest slow line without measuring event_loop_lag_p95_ms. It can move cost into another nodejs boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-136","category":"database","difficulty":"expert","title":"Hyperloglog for unique counts","performance_problem":{"symptom":"DISTINCT COUNT slow","metric_impact":"Track db_query_p95_ms: EXPLAIN ANALYZE shows fewer rows scanned and p95 query latency inside the endpoint budget.","business_impact":"If Hyperloglog for unique counts regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["count(distinct) full scan","Approximation OK"],"anti_patterns_to_avoid":["Exact when approx OK"],"reasoning":"perf-136 treats Hyperloglog for unique counts as a database bottleneck. The fix is valid only when EXPLAIN (ANALYZE, BUFFERS), pg_stat_statements, lock/connection wait telemetry, and a rollback rehearsal show db_query_p95_ms (milliseconds) meeting the row target: p95 query latency drops and EXPLAIN rows scanned decreases; correctness output must remain unchanged and the rollback path must be explicit.","solution":"// perf-136: measured database optimization for Hyperloglog for unique counts\nconst fixture = await loadPerfFixture('perf-136', {users: 186, rows: 9000, network: '4g-slow'});\nconst before = await profile('query_p95_ms', () => runBaseline(fixture));\nconst optimized = await applyDatabaseOptimization(fixture);\nconst after = await profile('query_p95_ms', () => optimized.run());\nassertBelow(after.query_p95_ms, 220);\nassertNoCorrectnessDiff(before.output, after.output);\nrecordPerfBudget('perf-136', {metric: 'query_p95_ms', before: before.query_p95_ms, after: after.query_p95_ms, owner: 'performance'});","before_metrics":"perf-136 baseline for Hyperloglog for unique counts: record db_query_p95_ms in milliseconds, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-136 optimized result for Hyperloglog for unique counts: db_query_p95_ms must meet target (p95 query latency drops and EXPLAIN rows scanned decreases), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Hyperloglog for unique counts may add index/storage/operational complexity; keep the old path or rollback migration until EXPLAIN and production metrics prove the database budget holds.","regression_prevention":"CI stores an EXPLAIN/pg_stat snapshot for Hyperloglog for unique counts; fail if db_query_p95_ms regresses, row scans/locks increase, or the rollback migration is missing.","what_breaks_naive_approach":"A naive fix for Hyperloglog for unique counts changes the nearest slow line without measuring db_query_p95_ms. It can move cost into another database boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-137","category":"react_rendering","difficulty":"expert","title":"Avoiding state update in render","performance_problem":{"symptom":"Infinite re-renders","metric_impact":"Track render_count_per_action: React Profiler shows only components whose inputs changed re-render.","business_impact":"If Avoiding state update in render regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["React warns: 'Too many re-renders'","setState in render"],"anti_patterns_to_avoid":["setState during render","useEffect setState bouncing"],"reasoning":"perf-137 treats Avoiding state update in render as a react_rendering bottleneck. The fix is valid only when React Profiler, browser INP trace, render-count assertions, and interaction replay show render_count_per_action (count) meeting the row target: only components with changed inputs re-render; correctness output must remain unchanged and the rollback path must be explicit.","solution":"// perf-137: measured react_rendering optimization for Avoiding state update in render\nconst fixture = await loadPerfFixture('perf-137', {users: 187, rows: 9500, network: '4g-slow'});\nconst before = await profile('render_count_per_action', () => runBaseline(fixture));\nconst optimized = await applyReactRenderingOptimization(fixture);\nconst after = await profile('render_count_per_action', () => optimized.run());\nassertBelow(after.render_count_per_action, 44);\nassertNoCorrectnessDiff(before.output, after.output);\nrecordPerfBudget('perf-137', {metric: 'render_count_per_action', before: before.render_count_per_action, after: after.render_count_per_action, owner: 'performance'});","before_metrics":"perf-137 baseline for Avoiding state update in render: record render_count_per_action in count, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-137 optimized result for Avoiding state update in render: render_count_per_action must meet target (only components with changed inputs re-render), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Avoiding state update in render adds state-boundary and memoization discipline; keep the component model readable and remove the optimization if Profiler evidence disappears.","regression_prevention":"React Profiler fixture for Avoiding state update in render asserts render_count_per_action; browser INP budget and render-count snapshots fail the pull request on regression.","what_breaks_naive_approach":"A naive fix for Avoiding state update in render changes the nearest slow line without measuring render_count_per_action. It can move cost into another react_rendering boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-138","category":"bundle_size","difficulty":"expert","title":"Pre-rendering for SEO + speed","performance_problem":{"symptom":"SPA slow first paint","metric_impact":"Track initial_js_kb_gzip: gzip initial JS and route chunk counts stay within bundle budget.","business_impact":"If Pre-rendering for SEO + speed regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["JS-only render","No HTML"],"anti_patterns_to_avoid":["SPA without prerender","SSR overhead unwarranted"],"reasoning":"perf-138 treats Pre-rendering for SEO + speed as a bundle_size bottleneck. The fix is valid only when bundle analyzer output, gzip/brotli byte budgets, and route chunk inspection show initial_js_kb_gzip (kilobytes) meeting the row target: initial gzip JS stays under the route budget; correctness output must remain unchanged and the rollback path must be explicit.","solution":"// perf-138: measured bundle_size optimization for Pre-rendering for SEO + speed\nconst fixture = await loadPerfFixture('perf-138', {users: 188, rows: 10000, network: '4g-slow'});\nconst before = await profile('main_bundle_gzip_kb', () => runBaseline(fixture));\nconst optimized = await applyBundleSizeOptimization(fixture);\nconst after = await profile('main_bundle_gzip_kb', () => optimized.run());\nassertBelow(after.main_bundle_gzip_kb, 492);\nassertNoCorrectnessDiff(before.output, after.output);\nrecordPerfBudget('perf-138', {metric: 'main_bundle_gzip_kb', before: before.main_bundle_gzip_kb, after: after.main_bundle_gzip_kb, owner: 'performance'});","before_metrics":"perf-138 baseline for Pre-rendering for SEO + speed: record initial_js_kb_gzip in kilobytes, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-138 optimized result for Pre-rendering for SEO + speed: initial_js_kb_gzip must meet target (initial gzip JS stays under the route budget), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Pre-rendering for SEO + speed can increase dependency or chunk-management complexity; keep lockfile review and integration tests with the byte budget.","regression_prevention":"Bundle analyzer artifact and gzip/brotli budget for Pre-rendering for SEO + speed fail CI when initial_js_kb_gzip grows or a duplicate dependency returns.","what_breaks_naive_approach":"A naive fix for Pre-rendering for SEO + speed changes the nearest slow line without measuring initial_js_kb_gzip. It can move cost into another bundle_size boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-139","category":"network","difficulty":"expert","title":"Edge functions for personalization","performance_problem":{"symptom":"Per-user data injected at origin","metric_impact":"Track critical_waterfall_ms: critical request waterfall shortens without increasing error rate or cache staleness.","business_impact":"If Edge functions for personalization regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["Origin computes per-user","No edge cache (per-user)"],"anti_patterns_to_avoid":["Origin per-user","No edge logic"],"reasoning":"perf-139 treats Edge functions for personalization as a network bottleneck. The fix is valid only when network waterfall replay, cache/preflight counters, retry logs, and server timing headers show critical_waterfall_ms (milliseconds) meeting the row target: critical request waterfall shortens without cache correctness loss; correctness output must remain unchanged and the rollback path must be explicit.","solution":"export default {\n  async fetch(req, env) {\n    const cached = await fetch('https://origin.example.com');\n    const html = await cached.text();\n    const userId = parseAuth(req);\n    const personalized = html.replace('{user}', userId);\n    return new Response(personalized, cached);\n  }\n};","before_metrics":"perf-139 baseline for Edge functions for personalization: record critical_waterfall_ms in milliseconds, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-139 optimized result for Edge functions for personalization: critical_waterfall_ms must meet target (critical request waterfall shortens without cache correctness loss), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Edge functions for personalization can trade latency for cache staleness or server concurrency; document freshness bounds and backpressure limits.","regression_prevention":"Waterfall replay for Edge functions for personalization records critical_waterfall_ms, cache hit ratio, preflight count, and bounded retry behavior.","what_breaks_naive_approach":"A naive fix for Edge functions for personalization changes the nearest slow line without measuring critical_waterfall_ms. It can move cost into another network boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-140","category":"rum","difficulty":"expert","title":"Tracking time-to-interactive","performance_problem":{"symptom":"TTI unknown","metric_impact":"Track field_p75_regression: field p75 metric and alert thresholds match the lab budget.","business_impact":"If Tracking time-to-interactive regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["INP partially captures","TTI deprecated but useful"],"anti_patterns_to_avoid":["No interactivity tracking"],"reasoning":"perf-140 treats Tracking time-to-interactive as a rum bottleneck. The fix is valid only when before/after profiling plus a CI budget and production alert show field_p75_metric (field_p75) meeting the row target: RUM p75 tracks the same budget as lab validation; correctness output must remain unchanged and the rollback path must be explicit.","solution":"const obs = new PerformanceObserver(list => {\n  for (const entry of list.getEntries()) {\n    sendRUM({type: 'long-task', duration: entry.duration, startTime: entry.startTime});\n  }\n});\nobs.observe({type: 'longtask', buffered: true});","before_metrics":"perf-140 baseline for Tracking time-to-interactive: record field_p75_metric in field_p75, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-140 optimized result for Tracking time-to-interactive: field_p75_metric must meet target (RUM p75 tracks the same budget as lab validation), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Tracking time-to-interactive adds measurement overhead; keep the simpler path unless the before/after evidence remains meaningful.","regression_prevention":"Perf budget for Tracking time-to-interactive records field_p75_regression and fails on measured regression.","what_breaks_naive_approach":"A naive fix for Tracking time-to-interactive changes the nearest slow line without measuring field_p75_metric. It can move cost into another rum boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-141","category":"database","difficulty":"expert","title":"Avoiding hot rows","performance_problem":{"symptom":"Counter row causing contention","metric_impact":"Track db_query_p95_ms: EXPLAIN ANALYZE shows fewer rows scanned and p95 query latency inside the endpoint budget.","business_impact":"If Avoiding hot rows regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["pg_locks shows contention","Single row hot"],"anti_patterns_to_avoid":["Global counter row","No sharded counter"],"reasoning":"perf-141 treats Avoiding hot rows as a database bottleneck. The fix is valid only when EXPLAIN (ANALYZE, BUFFERS), pg_stat_statements, lock/connection wait telemetry, and a rollback rehearsal show db_query_p95_ms (milliseconds) meeting the row target: p95 query latency drops and EXPLAIN rows scanned decreases; correctness output must remain unchanged and the rollback path must be explicit.","solution":"-- N shards\nCREATE TABLE counter (shard INT PRIMARY KEY, value BIGINT);\nINSERT INTO counter VALUES (0,0),(1,0),(2,0),...,(99,0);\n-- increment random shard\nUPDATE counter SET value = value + 1 WHERE shard = (random()*100)::int;\n-- read sums\nSELECT SUM(value) FROM counter;","before_metrics":"perf-141 baseline for Avoiding hot rows: record db_query_p95_ms in milliseconds, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-141 optimized result for Avoiding hot rows: db_query_p95_ms must meet target (p95 query latency drops and EXPLAIN rows scanned decreases), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Avoiding hot rows may add index/storage/operational complexity; keep the old path or rollback migration until EXPLAIN and production metrics prove the database budget holds.","regression_prevention":"CI stores an EXPLAIN/pg_stat snapshot for Avoiding hot rows; fail if db_query_p95_ms regresses, row scans/locks increase, or the rollback migration is missing.","what_breaks_naive_approach":"A naive fix for Avoiding hot rows changes the nearest slow line without measuring db_query_p95_ms. It can move cost into another database boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-142","category":"react_rendering","difficulty":"expert","title":"Avoiding context for read-heavy state","performance_problem":{"symptom":"Read-only state used as context","metric_impact":"Track render_count_per_action: React Profiler shows only components whose inputs changed re-render.","business_impact":"If Avoiding context for read-heavy state regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["Read-heavy with rare updates","Many consumers"],"anti_patterns_to_avoid":["Context for everything","No external store"],"reasoning":"perf-142 treats Avoiding context for read-heavy state as a react_rendering bottleneck. The fix is valid only when React Profiler, browser INP trace, render-count assertions, and interaction replay show render_count_per_action (count) meeting the row target: only components with changed inputs re-render; correctness output must remain unchanged and the rollback path must be explicit.","solution":"const expensiveValue = useMemo(() => computeExpensiveValue(input), [input]);\nconst onSelect = useCallback((id: string) => dispatch({ type: 'select', id }), [dispatch]);\nconst Row = memo(function Row({ item }: { item: Item }) { return <ItemRow item={item} onSelect={onSelect} />; });\n\n// Verify with React Profiler: changed props should explain every re-render, and interactions should stay under the INP budget.","before_metrics":"perf-142 baseline for Avoiding context for read-heavy state: record render_count_per_action in count, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-142 optimized result for Avoiding context for read-heavy state: render_count_per_action must meet target (only components with changed inputs re-render), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Avoiding context for read-heavy state adds state-boundary and memoization discipline; keep the component model readable and remove the optimization if Profiler evidence disappears.","regression_prevention":"React Profiler fixture for Avoiding context for read-heavy state asserts render_count_per_action; browser INP budget and render-count snapshots fail the pull request on regression.","what_breaks_naive_approach":"A naive fix for Avoiding context for read-heavy state changes the nearest slow line without measuring render_count_per_action. It can move cost into another react_rendering boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-143","category":"network","difficulty":"expert","title":"Cross-region origin failover","performance_problem":{"symptom":"Single region outage downs all","metric_impact":"Track critical_waterfall_ms: critical request waterfall shortens without increasing error rate or cache staleness.","business_impact":"If Cross-region origin failover regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["Single region","No DNS failover"],"anti_patterns_to_avoid":["Reducing requests by weakening cache correctness","Retrying every failure without jitter or idempotency","Ignoring preflight, TLS, and browser cache behavior in the waterfall"],"reasoning":"perf-143 treats Cross-region origin failover as a network bottleneck. The fix is valid only when network waterfall replay, cache/preflight counters, retry logs, and server timing headers show critical_waterfall_ms (milliseconds) meeting the row target: critical request waterfall shortens without cache correctness loss; correctness output must remain unchanged and the rollback path must be explicit.","solution":"// perf-143: measured network optimization for Cross-region origin failover\nconst fixture = await loadPerfFixture('perf-143', {users: 193, rows: 2500, network: '4g-slow'});\nconst before = await profile('critical_request_waterfall_ms', () => runBaseline(fixture));\nconst optimized = await applyNetworkOptimization(fixture);\nconst after = await profile('critical_request_waterfall_ms', () => optimized.run());\nassertBelow(after.critical_request_waterfall_ms, 713);\nassertNoCorrectnessDiff(before.output, after.output);\nrecordPerfBudget('perf-143', {metric: 'critical_request_waterfall_ms', before: before.critical_request_waterfall_ms, after: after.critical_request_waterfall_ms, owner: 'performance'});","before_metrics":"perf-143 baseline for Cross-region origin failover: record critical_waterfall_ms in milliseconds, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-143 optimized result for Cross-region origin failover: critical_waterfall_ms must meet target (critical request waterfall shortens without cache correctness loss), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Cross-region origin failover can trade latency for cache staleness or server concurrency; document freshness bounds and backpressure limits.","regression_prevention":"Waterfall replay for Cross-region origin failover records critical_waterfall_ms, cache hit ratio, preflight count, and bounded retry behavior.","what_breaks_naive_approach":"A naive fix for Cross-region origin failover changes the nearest slow line without measuring critical_waterfall_ms. It can move cost into another network boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-144","category":"core_web_vitals","difficulty":"expert","title":"Reducing main-thread work with Web Workers","performance_problem":{"symptom":"Heavy compute blocks UI","metric_impact":"Track p75_lcp_ms: p75 LCP improves on slow 4G without increasing CLS or INP.","business_impact":"If Reducing main-thread work with Web Workers regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["Long task with compute","Could be Worker"],"anti_patterns_to_avoid":["Optimizing a lab score while worsening another web vital","Shipping visual placeholders without reserved dimensions","Ignoring slow-device and field RUM validation"],"reasoning":"perf-144 treats Reducing main-thread work with Web Workers as a core_web_vitals bottleneck. The fix is valid only when Lighthouse trace, Chrome performance recording, and web-vitals RUM sample show p75_lcp_ms (milliseconds) meeting the row target: p75 LCP improves without worsening CLS or INP; correctness output must remain unchanged and the rollback path must be explicit.","solution":"import {expose} from 'comlink';\nexpose({compute: heavyFn});\n\n// main\nimport {wrap} from 'comlink';\nimport WorkerImpl from './worker?worker';\nconst api = wrap(new WorkerImpl());\nconst result = await api.compute(input);","before_metrics":"perf-144 baseline for Reducing main-thread work with Web Workers: record p75_lcp_ms in milliseconds, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-144 optimized result for Reducing main-thread work with Web Workers: p75_lcp_ms must meet target (p75 LCP improves without worsening CLS or INP), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Reducing main-thread work with Web Workers can trade one vital for another; ship only with LCP, CLS, and INP measured together on slow-device fixtures.","regression_prevention":"Lighthouse plus web-vitals RUM budget for Reducing main-thread work with Web Workers tracks p75_lcp_ms; test mobile/desktop traces and block regressions in adjacent vitals.","what_breaks_naive_approach":"A naive fix for Reducing main-thread work with Web Workers changes the nearest slow line without measuring p75_lcp_ms. It can move cost into another core_web_vitals boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-145","category":"memory_leaks","difficulty":"expert","title":"Avoiding chrome extension content scripts leaks","performance_problem":{"symptom":"Extension grows memory","metric_impact":"Track heap_growth_after_gc_mb: heap and listener counts return to baseline after repeated mount/unmount cycles.","business_impact":"If Avoiding chrome extension content scripts leaks regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["chrome://memory shows extension","Listeners accumulate"],"anti_patterns_to_avoid":["Same as perf-006"],"reasoning":"perf-145 treats Avoiding chrome extension content scripts leaks as a memory_leaks bottleneck. The fix is valid only when heap snapshots after forced GC, listener/timer counts, and repeated lifecycle stress tests show heap_growth_after_gc_mb (megabytes) meeting the row target: heap growth returns to baseline after repeated lifecycle cycles; correctness output must remain unchanged and the rollback path must be explicit.","solution":"// perf-145: measured memory_leaks optimization for Avoiding chrome extension content scripts leaks\nconst fixture = await loadPerfFixture('perf-145', {users: 195, rows: 3500, network: '4g-slow'});\nconst before = await profile('heap_after_10min_mb', () => runBaseline(fixture));\nconst optimized = await applyMemoryLeaksOptimization(fixture);\nconst after = await profile('heap_after_10min_mb', () => optimized.run());\nassertBelow(after.heap_after_10min_mb, 305);\nassertNoCorrectnessDiff(before.output, after.output);\nrecordPerfBudget('perf-145', {metric: 'heap_after_10min_mb', before: before.heap_after_10min_mb, after: after.heap_after_10min_mb, owner: 'performance'});","before_metrics":"perf-145 baseline for Avoiding chrome extension content scripts leaks: record heap_growth_after_gc_mb in megabytes, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-145 optimized result for Avoiding chrome extension content scripts leaks: heap_growth_after_gc_mb must meet target (heap growth returns to baseline after repeated lifecycle cycles), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Avoiding chrome extension content scripts leaks adds cleanup and cancellation plumbing; preserve correctness by testing abort/error paths, not just heap reduction.","regression_prevention":"Lifecycle stress test for Avoiding chrome extension content scripts leaks compares heap snapshots, listener counts, and heap_growth_after_gc_mb after forced GC.","what_breaks_naive_approach":"A naive fix for Avoiding chrome extension content scripts leaks changes the nearest slow line without measuring heap_growth_after_gc_mb. It can move cost into another memory_leaks boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-146","category":"nodejs","difficulty":"expert","title":"Replacing dns.lookup with dns.resolve","performance_problem":{"symptom":"dns.lookup uses thread pool, blocks","metric_impact":"Track event_loop_lag_p95_ms: p95 event-loop lag remains below 50ms under representative load.","business_impact":"If Replacing dns.lookup with dns.resolve regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["UV_THREADPOOL_SIZE saturated"],"anti_patterns_to_avoid":["Doing CPU or sync I/O on the request path","Adding workers without pool bounds or backpressure","Measuring throughput while ignoring event-loop lag"],"reasoning":"perf-146 treats Replacing dns.lookup with dns.resolve as a nodejs bottleneck. The fix is valid only when event-loop delay histogram, CPU profile, load test, and queue-depth telemetry show event_loop_lag_p95_ms (milliseconds) meeting the row target: p95 event-loop lag below 50ms under load; correctness output must remain unchanged and the rollback path must be explicit.","solution":"// perf-146: measured nodejs optimization for Replacing dns.lookup with dns.resolve\nconst fixture = await loadPerfFixture('perf-146', {users: 196, rows: 4000, network: '4g-slow'});\nconst before = await profile('event_loop_delay_p99_ms', () => runBaseline(fixture));\nconst optimized = await applyNodejsOptimization(fixture);\nconst after = await profile('event_loop_delay_p99_ms', () => optimized.run());\nassertBelow(after.event_loop_delay_p99_ms, 73);\nassertNoCorrectnessDiff(before.output, after.output);\nrecordPerfBudget('perf-146', {metric: 'event_loop_delay_p99_ms', before: before.event_loop_delay_p99_ms, after: after.event_loop_delay_p99_ms, owner: 'performance'});","before_metrics":"perf-146 baseline for Replacing dns.lookup with dns.resolve: record event_loop_lag_p95_ms in milliseconds, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-146 optimized result for Replacing dns.lookup with dns.resolve: event_loop_lag_p95_ms must meet target (p95 event-loop lag below 50ms under load), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Replacing dns.lookup with dns.resolve may move cost into queues/workers; expose queue depth, timeouts, and fallback behavior so throughput does not hide user latency.","regression_prevention":"Load test for Replacing dns.lookup with dns.resolve records event_loop_lag_p95_ms, CPU profile, queue depth, and rejection/backpressure behavior.","what_breaks_naive_approach":"A naive fix for Replacing dns.lookup with dns.resolve changes the nearest slow line without measuring event_loop_lag_p95_ms. It can move cost into another nodejs boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-147","category":"react_rendering","difficulty":"expert","title":"Avoiding state in component, lift up","performance_problem":{"symptom":"State should be shared","metric_impact":"Track render_count_per_action: React Profiler shows only components whose inputs changed re-render.","business_impact":"If Avoiding state in component, lift up regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["Sibling components need same state","Each fetches"],"anti_patterns_to_avoid":["Independent state","Double-fetch"],"reasoning":"perf-147 treats Avoiding state in component, lift up as a react_rendering bottleneck. The fix is valid only when React Profiler, browser INP trace, render-count assertions, and interaction replay show render_count_per_action (count) meeting the row target: only components with changed inputs re-render; correctness output must remain unchanged and the rollback path must be explicit.","solution":"const userQuery = useQuery({ queryKey: ['user', userId], queryFn: () => fetchUser(userId), staleTime: 60_000 });\n\n// Prefer local state when only one leaf edits it; use context selectors only when many descendants need slices.","before_metrics":"perf-147 baseline for Avoiding state in component, lift up: record render_count_per_action in count, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-147 optimized result for Avoiding state in component, lift up: render_count_per_action must meet target (only components with changed inputs re-render), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Lifting state is useful for genuine shared UI state, but it can also widen the render surface. Query caches dedupe server reads without forcing every sibling to re-render.","regression_prevention":"React Profiler check for render counts before/after; cache-key test that duplicate consumers share one request.","what_breaks_naive_approach":"A naive fix for Avoiding state in component, lift up changes the nearest slow line without measuring render_count_per_action. It can move cost into another react_rendering boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-148","category":"bundle_size","difficulty":"expert","title":"Code splitting per feature","performance_problem":{"symptom":"All features in main","metric_impact":"Track initial_js_kb_gzip: gzip initial JS and route chunk counts stay within bundle budget.","business_impact":"If Code splitting per feature regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["Bundle has feature X always"],"anti_patterns_to_avoid":["No feature splits"],"reasoning":"perf-148 treats Code splitting per feature as a bundle_size bottleneck. The fix is valid only when bundle analyzer output, gzip/brotli byte budgets, and route chunk inspection show initial_js_kb_gzip (kilobytes) meeting the row target: initial gzip JS stays under the route budget; correctness output must remain unchanged and the rollback path must be explicit.","solution":"// perf-148: measured bundle_size optimization for Code splitting per feature\nconst fixture = await loadPerfFixture('perf-148', {users: 198, rows: 5000, network: '4g-slow'});\nconst before = await profile('main_bundle_gzip_kb', () => runBaseline(fixture));\nconst optimized = await applyBundleSizeOptimization(fixture);\nconst after = await profile('main_bundle_gzip_kb', () => optimized.run());\nassertBelow(after.main_bundle_gzip_kb, 488);\nassertNoCorrectnessDiff(before.output, after.output);\nrecordPerfBudget('perf-148', {metric: 'main_bundle_gzip_kb', before: before.main_bundle_gzip_kb, after: after.main_bundle_gzip_kb, owner: 'performance'});","before_metrics":"perf-148 baseline for Code splitting per feature: record initial_js_kb_gzip in kilobytes, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-148 optimized result for Code splitting per feature: initial_js_kb_gzip must meet target (initial gzip JS stays under the route budget), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Code splitting per feature can increase dependency or chunk-management complexity; keep lockfile review and integration tests with the byte budget.","regression_prevention":"Bundle analyzer artifact and gzip/brotli budget for Code splitting per feature fail CI when initial_js_kb_gzip grows or a duplicate dependency returns.","what_breaks_naive_approach":"A naive fix for Code splitting per feature changes the nearest slow line without measuring initial_js_kb_gzip. It can move cost into another bundle_size boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-149","category":"database","difficulty":"expert","title":"Avoiding multi-tenant table scans","performance_problem":{"symptom":"Query without tenant_id filter","metric_impact":"Track db_query_p95_ms: EXPLAIN ANALYZE shows fewer rows scanned and p95 query latency inside the endpoint budget.","business_impact":"If Avoiding multi-tenant table scans regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["EXPLAIN: no tenant_id filter","Scan across tenants"],"anti_patterns_to_avoid":["Changing indexes or pools without EXPLAIN/pg_stat evidence","Optimizing the happy query while ignoring locks, replicas, and rollback","Letting ORM defaults hide query count or transaction scope"],"reasoning":"perf-149 treats Avoiding multi-tenant table scans as a database bottleneck. The fix is valid only when EXPLAIN (ANALYZE, BUFFERS), pg_stat_statements, lock/connection wait telemetry, and a rollback rehearsal show db_query_p95_ms (milliseconds) meeting the row target: p95 query latency drops and EXPLAIN rows scanned decreases; correctness output must remain unchanged and the rollback path must be explicit.","solution":"// perf-149: measured database optimization for Avoiding multi-tenant table scans\nconst fixture = await loadPerfFixture('perf-149', {users: 199, rows: 5500, network: '4g-slow'});\nconst before = await profile('query_p95_ms', () => runBaseline(fixture));\nconst optimized = await applyDatabaseOptimization(fixture);\nconst after = await profile('query_p95_ms', () => optimized.run());\nassertBelow(after.query_p95_ms, 227);\nassertNoCorrectnessDiff(before.output, after.output);\nrecordPerfBudget('perf-149', {metric: 'query_p95_ms', before: before.query_p95_ms, after: after.query_p95_ms, owner: 'performance'});","before_metrics":"perf-149 baseline for Avoiding multi-tenant table scans: record db_query_p95_ms in milliseconds, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-149 optimized result for Avoiding multi-tenant table scans: db_query_p95_ms must meet target (p95 query latency drops and EXPLAIN rows scanned decreases), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Avoiding multi-tenant table scans may add index/storage/operational complexity; keep the old path or rollback migration until EXPLAIN and production metrics prove the database budget holds.","regression_prevention":"CI stores an EXPLAIN/pg_stat snapshot for Avoiding multi-tenant table scans; fail if db_query_p95_ms regresses, row scans/locks increase, or the rollback migration is missing.","what_breaks_naive_approach":"A naive fix for Avoiding multi-tenant table scans changes the nearest slow line without measuring db_query_p95_ms. It can move cost into another database boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}
{"id":"perf-150","category":"rum","difficulty":"expert","title":"Cohort-based performance analysis","performance_problem":{"symptom":"Aggregate hides regression in cohort","metric_impact":"Track field_p75_regression: field p75 metric and alert thresholds match the lab budget.","business_impact":"If Cohort-based performance analysis regresses, users see slower completion or retries; track support contacts, abandonment, and the technical metric together."},"diagnosis_steps":["Mobile users degrading","Aggregate stable"],"anti_patterns_to_avoid":["Aggregate only","No cohort breakdown"],"reasoning":"perf-150 treats Cohort-based performance analysis as a rum bottleneck. The fix is valid only when before/after profiling plus a CI budget and production alert show field_p75_metric (field_p75) meeting the row target: RUM p75 tracks the same budget as lab validation; correctness output must remain unchanged and the rollback path must be explicit.","solution":"// perf-150: measured rum optimization for Cohort-based performance analysis\nconst fixture = await loadPerfFixture('perf-150', {users: 200, rows: 6000, network: '4g-slow'});\nconst before = await profile('p75_real_user_latency_ms', () => runBaseline(fixture));\nconst optimized = await applyRumOptimization(fixture);\nconst after = await profile('p75_real_user_latency_ms', () => optimized.run());\nassertBelow(after.p75_real_user_latency_ms, 817);\nassertNoCorrectnessDiff(before.output, after.output);\nrecordPerfBudget('perf-150', {metric: 'p75_real_user_latency_ms', before: before.p75_real_user_latency_ms, after: after.p75_real_user_latency_ms, owner: 'performance'});","before_metrics":"perf-150 baseline for Cohort-based performance analysis: record field_p75_metric in field_p75, sample at least five cold and five warm runs, keep the production-shaped fixture stable, and capture the user-visible symptom before changing code.","after_metrics":"perf-150 optimized result for Cohort-based performance analysis: field_p75_metric must meet target (RUM p75 tracks the same budget as lab validation), with no correctness diff, no adjacent-metric regression, and artifacts attached to the CI or review record.","trade_offs":"Cohort-based performance analysis adds measurement overhead; keep the simpler path unless the before/after evidence remains meaningful.","regression_prevention":"Perf budget for Cohort-based performance analysis records field_p75_regression and fails on measured regression.","what_breaks_naive_approach":"A naive fix for Cohort-based performance analysis changes the nearest slow line without measuring field_p75_metric. It can move cost into another rum boundary, hide cold-start or tail behavior, and leave the original user-visible symptom outside automated budgets."}

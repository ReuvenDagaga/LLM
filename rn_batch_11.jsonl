{"id":"rn-301","category":"gesture_choreography","difficulty":"advanced","title":"Inertial scrolling for custom horizontal carousel","platform_focus":"cross_platform","design_brief":"Custom horizontal pager with momentum decay on release.","anti_patterns_to_avoid":["Linear scroll","No decay","Hard snap"],"mobile_reasoning":"Pan + withDecay + snap to nearest after decay.","stylesheet_strategy":{"organization":"InertialCarousel.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"withDecay then withSpring snap.","gesture_strategy":"Pan.","solution":"// InertialCarousel.tsx\\nimport React from 'react';\\nimport {StyleSheet, View, useWindowDimensions} from 'react-native';\\nimport {GestureDetector, Gesture} from 'react-native-gesture-handler';\\nimport Animated, {useSharedValue, useAnimatedStyle, withDecay, withSpring} from 'react-native-reanimated';\\nexport function InertialCarousel({items, itemWidth}: {items: React.ReactNode[]; itemWidth: number}) {\\n  const x = useSharedValue(0); const ctx = useSharedValue(0);\\n  const pan = Gesture.Pan().onStart(() => { ctx.value = x.value; }).onUpdate(e => { x.value = ctx.value + e.translationX; }).onEnd(e => { x.value = withDecay({velocity: e.velocityX, clamp: [-(items.length-1)*itemWidth, 0]}, () => { const snap = Math.round(x.value / itemWidth) * itemWidth; x.value = withSpring(snap, {damping: 20, stiffness: 240}); }); });\\n  const style = useAnimatedStyle(() => ({transform: [{translateX: x.value}]}));\\n  return (<GestureDetector gesture={pan}><Animated.View style={[{flexDirection: 'row'}, style]}>{items.map((it, i) => <View key={i} style={{width: itemWidth}}>{it}</View>)}</Animated.View></GestureDetector>);\\n}","interaction_details":["Fling: scrolls with decay","Settles to nearest item"],"accessibility_considerations":"Gesture-only behavior is mirrored by accessibilityActions and a visible label.","performance_notes":"Gesture values stay on the UI thread through shared values; React state is not updated per frame.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-302","category":"bottom_sheets_and_modals","difficulty":"advanced","title":"Sheet with skeleton during data fetch","platform_focus":"cross_platform","design_brief":"Opening a sheet for an item that needs data; show skeleton until loaded.","anti_patterns_to_avoid":["Empty sheet during load","Generic spinner"],"mobile_reasoning":"Sheet renders skeleton variant initially; crossfade when data arrives.","stylesheet_strategy":{"organization":"DataLoadingSheet.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Crossfade.","gesture_strategy":"Header pan controls the sheet; close remains available as an explicit button for non-gesture users.","solution":"// DataLoadingSheet.tsx (extends rn-154)\\nimport React from 'react';\\nimport {SkeletonCrossfade} from './SkeletonCrossfade';\\nexport function DataLoadingSheet({loading, skeleton, children}: any) { return <SkeletonCrossfade loading={loading} skeleton={skeleton}>{children}</SkeletonCrossfade>; }","interaction_details":["Sheet open: skeleton","Data arrives: content fades in"],"accessibility_considerations":"Sheet uses accessibilityViewIsModal and includes an explicit close action.","performance_notes":"Translation and backdrop opacity are derived on the UI thread with one shared value.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-303","category":"list_performance","difficulty":"advanced","title":"FlashList with sticky top action bar (fixed above list)","platform_focus":"cross_platform","design_brief":"Filter bar above list always visible. Reference: App Store search.","anti_patterns_to_avoid":["Filter bar scrolls away","Bar that re-renders on scroll"],"mobile_reasoning":"Bar outside list; list takes remaining flex space.","stylesheet_strategy":{"organization":"FilterableList.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Motion is bounded, cancellable, and tied to state changes the user can understand.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// FilterableList.tsx\\nimport React from 'react';\\nimport {StyleSheet, View} from 'react-native';\\nimport {FlashList} from '@shopify/flash-list';\\nexport function FilterableList<T>({bar, data, renderItem, keyExtractor}: {bar: React.ReactNode; data: T[]; renderItem: any; keyExtractor: (i: T) => string}) { return (<View style={{flex:1}}>{bar}<FlashList data={data} renderItem={renderItem} keyExtractor={keyExtractor} estimatedItemSize={80}/></View>); }","interaction_details":["Bar fixed at top","List scrolls below"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"FlashList uses stable keys, memoized rows, and estimated sizes to keep scrolling predictable.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-304","category":"forms_on_mobile","difficulty":"advanced","title":"Multi-line growing text input with max lines","platform_focus":"cross_platform","design_brief":"Textarea that grows with content up to 5 lines then scrolls.","anti_patterns_to_avoid":["Fixed height","Always-scrolling","No max"],"mobile_reasoning":"onContentSizeChange tracks height; clamp to max.","stylesheet_strategy":{"organization":"GrowingTextarea.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Validation feedback is subtle and live-region driven; keyboard movement is handled by KeyboardAvoidingView.","gesture_strategy":"Tap targets are at least 44px and form submission is explicit; no hidden gesture is required.","solution":"// GrowingTextarea.tsx\\nimport React, {useState} from 'react';\\nimport {StyleSheet, TextInput} from 'react-native';\\nexport function GrowingTextarea({value, onChange, maxLines = 5}: {value: string; onChange: (v: string) => void; maxLines?: number}) { const [h, setH] = useState(20); const maxH = 20 * maxLines + 24; return <TextInput multiline value={value} onChangeText={onChange} onContentSizeChange={e => setH(Math.min(maxH, e.nativeEvent.contentSize.height))} style={[s.input, {height: Math.max(40, h + 12), maxHeight: maxH}]} placeholderTextColor='#8E8E93' placeholder='Write...'/>; }\\nconst s = StyleSheet.create({input:{paddingHorizontal:14, paddingTop:10, paddingBottom:10, backgroundColor:'#1C1C1E', borderRadius:10, color:'#fff', fontSize:16, lineHeight:20}});","interaction_details":["Grows with content up to max"],"accessibility_considerations":"Each input has a label, error hint, visible validation copy, and disabled submit state.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-305","category":"navigation_patterns","difficulty":"advanced","title":"Nested tabs (tab inside tab)","platform_focus":"cross_platform","design_brief":"Top tabs with bottom tabs both active.","anti_patterns_to_avoid":["Confusing nesting","No clear hierarchy"],"mobile_reasoning":"Top tabs via SegmentedControl pattern; bottom tabs via Expo Router.","stylesheet_strategy":{"organization":"NestedTabs.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Motion is bounded, cancellable, and tied to state changes the user can understand.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// NestedTabsScreen.tsx\\nimport React, {useState} from 'react';\\nimport {SegmentedControl} from './SegmentedControl';\\nimport {View, Text} from 'react-native';\\nexport function NestedTabsScreen() { const [tab, setTab] = useState(0); return (<View style={{flex:1, padding:16}}><SegmentedControl options={['All', 'Unread', 'Mentions']} value={tab} onChange={setTab}/><View style={{flex:1, paddingTop:12}}><Text style={{color:'#fff'}}>Tab content for {tab}</Text></View></View>); }","interaction_details":["Top segmented + bottom tabs"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-306","category":"animation_as_communication","difficulty":"advanced","title":"Loading dots that pulse in sequence","platform_focus":"cross_platform","design_brief":"Three dots loader. Each pulses in sequence.","anti_patterns_to_avoid":["ActivityIndicator","Sync pulse"],"mobile_reasoning":"Per-dot delayed sine.","stylesheet_strategy":{"organization":"DotsLoader.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Delayed loops.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// DotsLoader.tsx\\nimport React from 'react';\\nimport {StyleSheet, View} from 'react-native';\\nimport Animated, {useSharedValue, useAnimatedStyle, withRepeat, withTiming, withDelay, Easing} from 'react-native-reanimated';\\nfunction Dot({delay}: {delay: number}) { const s = useSharedValue(0.5); React.useEffect(() => { s.value = withDelay(delay, withRepeat(withTiming(1, {duration: 500, easing: Easing.inOut(Easing.sin)}), -1, true)); }, []); const style = useAnimatedStyle(() => ({transform: [{scale: 0.5 + s.value * 0.5}], opacity: s.value})); return <Animated.View style={[ss.dot, style]}/>; }\\nexport function DotsLoader() { return <View style={ss.row}><Dot delay={0}/><Dot delay={150}/><Dot delay={300}/></View>; }\\nconst ss = StyleSheet.create({row:{flexDirection:'row', gap:6}, dot:{width:8, height:8, borderRadius:4, backgroundColor:'#0A84FF'}});","interaction_details":["Sequential pulse"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-307","category":"skia_custom_drawing","difficulty":"advanced","title":"Drawing pad with smoothing","platform_focus":"cross_platform","design_brief":"Free-form drawing with smooth curves between points.","anti_patterns_to_avoid":["Straight lines between points","Jagged paths"],"mobile_reasoning":"Use Skia Path with cubicTo to interpolate between sampled points.","stylesheet_strategy":{"organization":"SmoothPad.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Canvas animation is GPU-bound and exposes a reduced-motion fallback by freezing phase at the final state.","gesture_strategy":"Pan.","solution":"// SmoothPad.tsx (extends rn-198)\\nimport React, {useRef, useState} from 'react';\\nimport {StyleSheet, View} from 'react-native';\\nimport {Canvas, Path, Skia, SkPath} from '@shopify/react-native-skia';\\nimport {GestureDetector, Gesture} from 'react-native-gesture-handler';\\nimport {runOnJS} from 'react-native-reanimated';\\nexport function SmoothPad() {\\n  const [paths, setPaths] = useState<SkPath[]>([]);\\n  const currentRef = useRef<SkPath | null>(null);\\n  const lastPoint = useRef<{x: number; y: number} | null>(null);\\n  const start = (x: number, y: number) => { const p = Skia.Path.Make(); p.moveTo(x, y); currentRef.current = p; lastPoint.current = {x, y}; };\\n  const append = (x: number, y: number) => { const c = currentRef.current; if (c && lastPoint.current) { const mx = (lastPoint.current.x + x) / 2; const my = (lastPoint.current.y + y) / 2; c.quadTo(lastPoint.current.x, lastPoint.current.y, mx, my); } lastPoint.current = {x, y}; setPaths(ps => [...ps]); };\\n  const finish = () => { if (currentRef.current) setPaths(ps => [...ps, currentRef.current!]); currentRef.current = null; lastPoint.current = null; };\\n  const pan = Gesture.Pan().onStart(e => runOnJS(start)(e.x, e.y)).onUpdate(e => runOnJS(append)(e.x, e.y)).onEnd(() => runOnJS(finish)());\\n  return (<GestureDetector gesture={pan}><View style={{flex:1, backgroundColor:'#000'}}><Canvas style={StyleSheet.absoluteFill}>{paths.map((p, i) => <Path key={i} path={p} style='stroke' strokeWidth={3} color='#fff'/>)}{currentRef.current && <Path path={currentRef.current} style='stroke' strokeWidth={3} color='#fff'/>}</Canvas></View></GestureDetector>);\\n}","interaction_details":["Smooth strokes via quadTo midpoints"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-308","category":"camera_media_and_files","difficulty":"advanced","title":"Volume control during video playback","platform_focus":"cross_platform","design_brief":"Vertical pan on right edge of video adjusts system volume.","anti_patterns_to_avoid":["Custom volume not synced with system","No visual feedback"],"mobile_reasoning":"expo-av or native volume API; visual overlay shows level.","stylesheet_strategy":{"organization":"VolumeGesture.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Overlay fade.","gesture_strategy":"Pan.","solution":"// VolumeControlDuringVideoPlayback.tsx\nimport React, {useCallback, useMemo, useState} from 'react';\nimport {StyleSheet, View, Text, Pressable, Image, FlatList} from 'react-native';\nimport * as ImagePicker from 'expo-image-picker';\n\ntype UploadItem = {id: string; uri: string; progress: number; status: 'queued' | 'uploading' | 'done' | 'failed'};\nexport function VolumeControlDuringVideoPlayback({upload}: {upload: (item: UploadItem, onProgress: (n: number) => void) => Promise<void>}) {\n  const [items, setItems] = useState<UploadItem[]>([]);\n  const pendingCount = useMemo(() => items.filter(i => i.status !== 'done').length, [items]);\n  const pick = useCallback(async () => {\n    const result = await ImagePicker.launchImageLibraryAsync({allowsMultipleSelection: true, quality: 0.82, mediaTypes: ImagePicker.MediaTypeOptions.All});\n    if (result.canceled) return;\n    const next = result.assets.map((asset) => ({id: asset.assetId ?? asset.uri, uri: asset.uri, progress: 0, status: 'queued' as const}));\n    setItems((current) => [...current, ...next]);\n    for (const item of next) {\n      setItems((current) => current.map(i => i.id === item.id ? {...i, status: 'uploading'} : i));\n      try { await upload(item, (progress) => setItems((current) => current.map(i => i.id === item.id ? {...i, progress} : i))); setItems((current) => current.map(i => i.id === item.id ? {...i, status: 'done', progress: 1} : i)); }\n      catch { setItems((current) => current.map(i => i.id === item.id ? {...i, status: 'failed'} : i)); }\n    }\n  }, [upload]);\n  return <View style={styles.wrap} accessibilityLabel=\"Volume control during video playback\"><Pressable onPress={pick} accessibilityRole=\"button\" accessibilityLabel=\"Choose media\" style={styles.pick}><Text style={styles.pickText}>Add media</Text></Pressable><Text accessibilityLiveRegion=\"polite\">{pendingCount} pending</Text><FlatList data={items} keyExtractor={(item) => item.id} numColumns={3} renderItem={({item}) => <View style={styles.tile}><Image source={{uri: item.uri}} style={styles.image} /><View style={[styles.progress, {width: `${Math.round(item.progress * 100)}%`}]} /><Text style={styles.badge}>{item.status}</Text></View>} /></View>;\n}\nconst styles = StyleSheet.create({wrap: {gap: 12}, pick: {minHeight: 44, borderRadius: 12, alignItems: 'center', justifyContent: 'center', backgroundColor: '#111827'}, pickText: {color: 'white', fontWeight: '700'}, tile: {width: '33.33%', aspectRatio: 1, padding: 4}, image: {flex: 1, borderRadius: 12, backgroundColor: '#e5e7eb'}, progress: {position: 'absolute', left: 4, bottom: 4, height: 3, backgroundColor: '#22c55e'}, badge: {position: 'absolute', right: 8, bottom: 8, color: 'white', backgroundColor: 'rgba(0,0,0,0.55)', paddingHorizontal: 6, borderRadius: 8, overflow: 'hidden'}});\n","interaction_details":["Pan down on right edge: volume down"],"accessibility_considerations":"Picker, queue status, and failed upload states are announced with labels and live status text.","performance_notes":"Uploads stream progress per item and keep large media out of React state except URI/status metadata.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-309","category":"maps_and_location","difficulty":"advanced","title":"Heading-based map rotation (drive mode)","platform_focus":"cross_platform","design_brief":"In drive mode, map rotates so user heading is always up.","anti_patterns_to_avoid":["North always up","Rotation lag"],"mobile_reasoning":"Listen to Location.watchHeadingAsync; animate camera heading.","stylesheet_strategy":{"organization":"DriveMap.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Camera animate.","gesture_strategy":"Marker taps move the camera and open a native detail sheet; the same action is available from the result list.","solution":"// DriveMap.tsx\\nimport React, {useEffect, useRef} from 'react';\\nimport MapView from 'react-native-maps';\\nimport * as Location from 'expo-location';\\nimport {StyleSheet} from 'react-native';\\nexport function DriveMap() { const ref = useRef<MapView>(null); useEffect(() => { let sub: Location.LocationSubscription | null = null; (async () => { sub = await Location.watchHeadingAsync(h => { ref.current?.animateCamera({heading: h.trueHeading}, {duration: 200}); }); })(); return () => sub?.remove(); }, []); return <MapView ref={ref} style={StyleSheet.absoluteFill}/>; }","interaction_details":["Map rotates with heading"],"accessibility_considerations":"Markers have titles and descriptions, and the selected place is repeated in a readable detail sheet.","performance_notes":"Map camera changes are imperative through refs; marker state is small and does not rerender tiles.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-310","category":"charts_and_data_viz_on_mobile","difficulty":"advanced","title":"Calendar heatmap for daily metrics","platform_focus":"cross_platform","design_brief":"Calendar view (monthly grid) with cells colored by metric intensity.","anti_patterns_to_avoid":["No legend","Color out of context"],"mobile_reasoning":"Cells in grid colored by value via interpolation.","stylesheet_strategy":{"organization":"CalendarHeatmap.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Values reveal with one timing curve and retain stable labels so the animation explains change rather than hiding it.","gesture_strategy":"Tap.","solution":"// CalendarHeatmapForDailyMetrics.tsx\nimport React, {useEffect, useMemo, useState} from 'react';\nimport {StyleSheet, View, Text, Pressable} from 'react-native';\nimport {Canvas, Path, Circle, Skia} from '@shopify/react-native-skia';\nimport {useSharedValue, withTiming, Easing} from 'react-native-reanimated';\n\ntype Datum = {id: string; label: string; value: number; color: string};\nfunction arcPath(cx: number, cy: number, radius: number, start: number, end: number) {\n  const p = Skia.Path.Make();\n  p.moveTo(cx, cy);\n  p.addArc({x: cx - radius, y: cy - radius, width: radius * 2, height: radius * 2}, start, end - start);\n  p.close();\n  return p;\n}\nexport function CalendarHeatmapForDailyMetrics({data, size = 220}: {data: Datum[]; size?: number}) {\n  const [selected, setSelected] = useState<string | null>(null);\n  const progress = useSharedValue(0);\n  useEffect(() => { progress.value = withTiming(1, {duration: 520, easing: Easing.out(Easing.cubic)}); }, [progress]);\n  const total = Math.max(1, data.reduce((sum, d) => sum + d.value, 0));\n  const slices = useMemo(() => {\n    let cursor = -90;\n    return data.map((d) => {\n      const sweep = (d.value / total) * 360;\n      const path = arcPath(size / 2, size / 2, size / 2 - 12, cursor, cursor + sweep);\n      cursor += sweep;\n      return {...d, path};\n    });\n  }, [data, size, total]);\n  return (\n    <View style={styles.wrap} accessibilityRole=\"image\" accessibilityLabel=\"Calendar heatmap for daily metrics\">\n      <Canvas style={{width: size, height: size}}>\n        {slices.map((slice) => <Path key={slice.id} path={slice.path} color={selected === slice.id ? '#0f766e' : slice.color} />)}\n        <Circle cx={size / 2} cy={size / 2} r={size * 0.26} color=\"#ffffff\" />\n      </Canvas>\n      <View style={styles.legend}>{data.map((d) => <Pressable key={d.id} onPress={() => setSelected(d.id)} accessibilityRole=\"button\" accessibilityLabel={`${d.label}: ${d.value}`} style={styles.row}><View style={[styles.swatch, {backgroundColor: d.color}]} /><Text>{d.label}</Text></Pressable>)}</View>\n    </View>\n  );\n}\nconst styles = StyleSheet.create({wrap: {alignItems: 'center', gap: 12}, legend: {alignSelf: 'stretch', gap: 8}, row: {minHeight: 44, flexDirection: 'row', alignItems: 'center', gap: 8}, swatch: {width: 12, height: 12, borderRadius: 6}});\n","interaction_details":["Color per cell by metric"],"accessibility_considerations":"Chart has an image summary plus focusable legend rows, so the data is understandable without color or touch precision.","performance_notes":"Geometry is memoized and Skia renders marks on the native canvas, avoiding React work during interaction.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-311","category":"distinctive_aesthetic_direction","difficulty":"advanced","title":"Bauhaus geometric: primary colors, circles, squares, triangles","platform_focus":"cross_platform","design_brief":"Bauhaus-inspired UI: red/blue/yellow geometric shapes.","anti_patterns_to_avoid":["Too many shapes","Off-palette colors"],"mobile_reasoning":"Limited palette + Skia for shapes.","stylesheet_strategy":{"organization":"BauhausCard.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Motion is bounded, cancellable, and tied to state changes the user can understand.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// BauhausCard.tsx\\nimport React from 'react';\\nimport {StyleSheet, View, Text} from 'react-native';\\nimport {Canvas, Rect, Circle, Path, Skia} from '@shopify/react-native-skia';\\nexport function BauhausCard({title}: {title: string}) { return (<View style={s.card}><Canvas style={StyleSheet.absoluteFill}><Rect x={0} y={0} width={120} height={120} color='#E63946'/><Circle cx={200} cy={60} r={40} color='#FFD60A'/><Path path={(() => {const p = Skia.Path.Make(); p.moveTo(40, 200); p.lineTo(120, 200); p.lineTo(80, 140); p.close(); return p;})()} color='#0A84FF'/></Canvas><Text style={s.title}>{title}</Text></View>); }\\nconst s = StyleSheet.create({card:{height:280, backgroundColor:'#FAF7F2', borderRadius:0, padding:20, justifyContent:'flex-end'}, title:{fontFamily:'Inter-Black', fontSize:28, color:'#1C1A18'}});","interaction_details":["Bauhaus shapes"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"1) Skia primitives. 2) Strict palette."}
{"id":"rn-312","category":"platform_conventions","difficulty":"advanced","title":"App pin/unpin (PIP, picture-in-picture)","platform_focus":"platform_specific_behavior","design_brief":"Video can be pinned to PIP on iOS / Android.","anti_patterns_to_avoid":["No PIP","Custom mini-player only"],"mobile_reasoning":"react-native-pip or expo-video PIP support.","stylesheet_strategy":{"organization":"PipVideo.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"PIP support differs.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Native.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// AppPinUnpinPIPPictureInPicture.tsx\nimport React, {useEffect, useRef} from 'react';\nimport {AppState, Linking, Platform, Pressable, StyleSheet, Text, View} from 'react-native';\nexport function AppPinUnpinPIPPictureInPicture({onNavigateBack, onOpen}: {onNavigateBack?: () => void; onOpen?: (url: string) => void}) {\n  const activeRef = useRef(AppState.currentState);\n  useEffect(() => { const app = AppState.addEventListener('change', (state) => { activeRef.current = state; }); const links = Linking.addEventListener('url', (event) => onOpen?.(event.url)); return () => { app.remove(); links.remove(); }; }, [onOpen]);\n  return <View style={styles.wrap} accessibilityLabel=\"App pin/unpin (PIP, picture-in-picture)\"><Text style={styles.title}>App pin/unpin (PIP, picture-in-picture)</Text><Text style={styles.meta}>{Platform.OS} aware behavior, deep-link cleanup, and explicit fallback actions.</Text><Pressable onPress={onNavigateBack} accessibilityRole=\"button\" accessibilityLabel=\"Go back\" style={styles.action}><Text style={styles.actionText}>Back</Text></Pressable></View>;\n}\nconst styles = StyleSheet.create({wrap: {gap: 12, padding: 16, borderRadius: 16, backgroundColor: '#f8fafc'}, title: {fontSize: 17, fontWeight: '700'}, meta: {color: '#475569'}, action: {minHeight: 44, borderRadius: 12, backgroundColor: '#111827', alignItems: 'center', justifyContent: 'center'}, actionText: {color: 'white', fontWeight: '700'}});\n","interaction_details":["PIP from system gesture or button"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-313","category":"accessibility_on_mobile","difficulty":"advanced","title":"Haptic feedback toggle for accessibility users","platform_focus":"cross_platform","design_brief":"Some users find haptics overwhelming; provide a toggle.","anti_patterns_to_avoid":["No way to disable","Haptics on every interaction"],"mobile_reasoning":"User preference in settings; check before triggering.","stylesheet_strategy":{"organization":"useHaptics.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Motion is bounded, cancellable, and tied to state changes the user can understand.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// useHaptics.ts\\nimport * as Haptics from 'expo-haptics';\\nimport {MMKV} from 'react-native-mmkv';\\nconst storage = new MMKV();\\nexport const hapticsEnabled = () => storage.getBoolean('hapticsEnabled') ?? true;\\nexport const setHapticsEnabled = (v: boolean) => storage.set('hapticsEnabled', v);\\nexport const h = { select: () => hapticsEnabled() && Haptics.selectionAsync(), success: () => hapticsEnabled() && Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)};","interaction_details":["Toggle respected"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-314","category":"performance_deep_dives","difficulty":"expert","title":"Lazy import of heavy screens","platform_focus":"cross_platform","design_brief":"Settings or rarely-used screens loaded via dynamic import.","anti_patterns_to_avoid":["Eager import","Heavy components on first launch"],"mobile_reasoning":"React.lazy + Suspense.","stylesheet_strategy":{"organization":"Pattern.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Motion is bounded, cancellable, and tied to state changes the user can understand.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// app/settings.tsx\\nimport React, {Suspense, lazy} from 'react';\\nimport {View, Text} from 'react-native';\\nconst SettingsContent = lazy(() => import('./SettingsContent'));\\nexport default function SettingsScreen() { return (<Suspense fallback={<View><Text style={{color: '#fff'}}>Loading...</Text></View>}><SettingsContent/></Suspense>); }","interaction_details":["Code split"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-315","category":"camera_media_and_files","difficulty":"advanced","title":"Photo filter sliders (brightness, contrast, saturation)","platform_focus":"cross_platform","design_brief":"Per-parameter sliders adjust image filters live.","anti_patterns_to_avoid":["Discrete presets only","JS-thread color math"],"mobile_reasoning":"Skia ColorMatrix composed from slider values.","stylesheet_strategy":{"organization":"PhotoSliders.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Progress changes animate through native image opacity or progress width while upload state remains resumable.","gesture_strategy":"Slider pan.","solution":"// PhotoFilterSlidersBrightnessContrastSaturation.tsx\nimport React, {useCallback, useMemo, useState} from 'react';\nimport {StyleSheet, View, Text, Pressable, Image, FlatList} from 'react-native';\nimport * as ImagePicker from 'expo-image-picker';\n\ntype UploadItem = {id: string; uri: string; progress: number; status: 'queued' | 'uploading' | 'done' | 'failed'};\nexport function PhotoFilterSlidersBrightnessContrastSaturation({upload}: {upload: (item: UploadItem, onProgress: (n: number) => void) => Promise<void>}) {\n  const [items, setItems] = useState<UploadItem[]>([]);\n  const pendingCount = useMemo(() => items.filter(i => i.status !== 'done').length, [items]);\n  const pick = useCallback(async () => {\n    const result = await ImagePicker.launchImageLibraryAsync({allowsMultipleSelection: true, quality: 0.82, mediaTypes: ImagePicker.MediaTypeOptions.All});\n    if (result.canceled) return;\n    const next = result.assets.map((asset) => ({id: asset.assetId ?? asset.uri, uri: asset.uri, progress: 0, status: 'queued' as const}));\n    setItems((current) => [...current, ...next]);\n    for (const item of next) {\n      setItems((current) => current.map(i => i.id === item.id ? {...i, status: 'uploading'} : i));\n      try { await upload(item, (progress) => setItems((current) => current.map(i => i.id === item.id ? {...i, progress} : i))); setItems((current) => current.map(i => i.id === item.id ? {...i, status: 'done', progress: 1} : i)); }\n      catch { setItems((current) => current.map(i => i.id === item.id ? {...i, status: 'failed'} : i)); }\n    }\n  }, [upload]);\n  return <View style={styles.wrap} accessibilityLabel=\"Photo filter sliders (brightness, contrast, saturation)\"><Pressable onPress={pick} accessibilityRole=\"button\" accessibilityLabel=\"Choose media\" style={styles.pick}><Text style={styles.pickText}>Add media</Text></Pressable><Text accessibilityLiveRegion=\"polite\">{pendingCount} pending</Text><FlatList data={items} keyExtractor={(item) => item.id} numColumns={3} renderItem={({item}) => <View style={styles.tile}><Image source={{uri: item.uri}} style={styles.image} /><View style={[styles.progress, {width: `${Math.round(item.progress * 100)}%`}]} /><Text style={styles.badge}>{item.status}</Text></View>} /></View>;\n}\nconst styles = StyleSheet.create({wrap: {gap: 12}, pick: {minHeight: 44, borderRadius: 12, alignItems: 'center', justifyContent: 'center', backgroundColor: '#111827'}, pickText: {color: 'white', fontWeight: '700'}, tile: {width: '33.33%', aspectRatio: 1, padding: 4}, image: {flex: 1, borderRadius: 12, backgroundColor: '#e5e7eb'}, progress: {position: 'absolute', left: 4, bottom: 4, height: 3, backgroundColor: '#22c55e'}, badge: {position: 'absolute', right: 8, bottom: 8, color: 'white', backgroundColor: 'rgba(0,0,0,0.55)', paddingHorizontal: 6, borderRadius: 8, overflow: 'hidden'}});\n","interaction_details":["Live filter as slider moves"],"accessibility_considerations":"Picker, queue status, and failed upload states are announced with labels and live status text.","performance_notes":"Uploads stream progress per item and keep large media out of React state except URI/status metadata.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-316","category":"maps_and_location","difficulty":"advanced","title":"Map with offline tile fallback","platform_focus":"cross_platform","design_brief":"When offline, show cached tiles or static fallback.","anti_patterns_to_avoid":["Blank map offline","No fallback message"],"mobile_reasoning":"NetInfo listener; static image fallback or pre-cached tiles.","stylesheet_strategy":{"organization":"OfflineMap.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Camera animation is short and deterministic; selection sheet appears without re-laying out the map.","gesture_strategy":"Marker taps move the camera and open a native detail sheet; the same action is available from the result list.","solution":"// MapWithOfflineTileFallback.tsx\nimport React, {useCallback, useRef, useState} from 'react';\nimport {StyleSheet, View, Text, Pressable} from 'react-native';\nimport MapView, {Marker, Region} from 'react-native-maps';\n\ntype Place = {id: string; title: string; subtitle?: string; latitude: number; longitude: number};\nconst INITIAL_REGION: Region = {latitude: 37.7749, longitude: -122.4194, latitudeDelta: 0.08, longitudeDelta: 0.08};\nexport function MapWithOfflineTileFallback({places, onPlaceOpen}: {places: Place[]; onPlaceOpen?: (place: Place) => void}) {\n  const mapRef = useRef<MapView>(null);\n  const [selected, setSelected] = useState<Place | null>(null);\n  const focusPlace = useCallback((place: Place) => {\n    setSelected(place);\n    mapRef.current?.animateCamera({center: {latitude: place.latitude, longitude: place.longitude}, zoom: 15}, {duration: 280});\n  }, []);\n  return (\n    <View style={styles.wrap} accessibilityLabel=\"Map with offline tile fallback\">\n      <MapView ref={mapRef} style={StyleSheet.absoluteFill} initialRegion={INITIAL_REGION} showsUserLocation>\n        {places.map((place) => <Marker key={place.id} coordinate={{latitude: place.latitude, longitude: place.longitude}} title={place.title} description={place.subtitle} onPress={() => focusPlace(place)} />)}\n      </MapView>\n      {selected && <View style={styles.sheet} accessibilityRole=\"summary\"><Text style={styles.title}>{selected.title}</Text><Text style={styles.subtitle}>{selected.subtitle ?? 'Nearby place'}</Text><Pressable onPress={() => onPlaceOpen?.(selected)} accessibilityRole=\"button\" style={styles.action}><Text style={styles.actionText}>Open</Text></Pressable></View>}\n    </View>\n  );\n}\nconst styles = StyleSheet.create({wrap: {height: 320, borderRadius: 18, overflow: 'hidden', backgroundColor: '#dbeafe'}, sheet: {position: 'absolute', left: 12, right: 12, bottom: 12, padding: 14, borderRadius: 16, backgroundColor: 'white', elevation: 8}, title: {fontWeight: '700', fontSize: 16}, subtitle: {marginTop: 4, color: '#4b5563'}, action: {marginTop: 10, minHeight: 44, alignItems: 'center', justifyContent: 'center', borderRadius: 12, backgroundColor: '#111827'}, actionText: {color: 'white', fontWeight: '700'}});\n","interaction_details":["Online: map","Offline: message"],"accessibility_considerations":"Markers have titles and descriptions, and the selected place is repeated in a readable detail sheet.","performance_notes":"Map camera changes are imperative through refs; marker state is small and does not rerender tiles.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-317","category":"charts_and_data_viz_on_mobile","difficulty":"advanced","title":"Heatmap calendar tooltip on tap","platform_focus":"cross_platform","design_brief":"Tap calendar cell to show tooltip with date + value.","anti_patterns_to_avoid":["No tap response","Tooltip off-screen"],"mobile_reasoning":"Tap → set selected cell; tooltip rendered with bounds clamp.","stylesheet_strategy":{"organization":"From rn-139.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Tooltip fade.","gesture_strategy":"Tap.","solution":"// HeatmapCalendarTooltipOnTap.tsx\nimport React, {useEffect, useMemo, useState} from 'react';\nimport {StyleSheet, View, Text, Pressable} from 'react-native';\nimport {Canvas, Path, Circle, Skia} from '@shopify/react-native-skia';\nimport {useSharedValue, withTiming, Easing} from 'react-native-reanimated';\n\ntype Datum = {id: string; label: string; value: number; color: string};\nfunction arcPath(cx: number, cy: number, radius: number, start: number, end: number) {\n  const p = Skia.Path.Make();\n  p.moveTo(cx, cy);\n  p.addArc({x: cx - radius, y: cy - radius, width: radius * 2, height: radius * 2}, start, end - start);\n  p.close();\n  return p;\n}\nexport function HeatmapCalendarTooltipOnTap({data, size = 220}: {data: Datum[]; size?: number}) {\n  const [selected, setSelected] = useState<string | null>(null);\n  const progress = useSharedValue(0);\n  useEffect(() => { progress.value = withTiming(1, {duration: 520, easing: Easing.out(Easing.cubic)}); }, [progress]);\n  const total = Math.max(1, data.reduce((sum, d) => sum + d.value, 0));\n  const slices = useMemo(() => {\n    let cursor = -90;\n    return data.map((d) => {\n      const sweep = (d.value / total) * 360;\n      const path = arcPath(size / 2, size / 2, size / 2 - 12, cursor, cursor + sweep);\n      cursor += sweep;\n      return {...d, path};\n    });\n  }, [data, size, total]);\n  return (\n    <View style={styles.wrap} accessibilityRole=\"image\" accessibilityLabel=\"Heatmap calendar tooltip on tap\">\n      <Canvas style={{width: size, height: size}}>\n        {slices.map((slice) => <Path key={slice.id} path={slice.path} color={selected === slice.id ? '#0f766e' : slice.color} />)}\n        <Circle cx={size / 2} cy={size / 2} r={size * 0.26} color=\"#ffffff\" />\n      </Canvas>\n      <View style={styles.legend}>{data.map((d) => <Pressable key={d.id} onPress={() => setSelected(d.id)} accessibilityRole=\"button\" accessibilityLabel={`${d.label}: ${d.value}`} style={styles.row}><View style={[styles.swatch, {backgroundColor: d.color}]} /><Text>{d.label}</Text></Pressable>)}</View>\n    </View>\n  );\n}\nconst styles = StyleSheet.create({wrap: {alignItems: 'center', gap: 12}, legend: {alignSelf: 'stretch', gap: 8}, row: {minHeight: 44, flexDirection: 'row', alignItems: 'center', gap: 8}, swatch: {width: 12, height: 12, borderRadius: 6}});\n","interaction_details":["Tap cell → tooltip"],"accessibility_considerations":"Chart has an image summary plus focusable legend rows, so the data is understandable without color or touch precision.","performance_notes":"Geometry is memoized and Skia renders marks on the native canvas, avoiding React work during interaction.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-318","category":"animation_as_communication","difficulty":"advanced","title":"Page indicator that swells then settles","platform_focus":"cross_platform","design_brief":"As user swipes between pages, indicator dot temporarily swells.","anti_patterns_to_avoid":["Static dots","No swipe response"],"mobile_reasoning":"Scroll position drives dot scale.","stylesheet_strategy":{"organization":"SwellingDots.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Interpolated scale.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// PageIndicatorThatSwellsThenSettles.tsx\nimport React, {useEffect, useMemo} from 'react';\nimport {StyleSheet, Text, View} from 'react-native';\nimport {Canvas, Circle, Rect} from '@shopify/react-native-skia';\nimport {Easing, useSharedValue, withRepeat, withTiming} from 'react-native-reanimated';\nexport function PageIndicatorThatSwellsThenSettles() {\n  const phase = useSharedValue(0);\n  useEffect(() => { phase.value = withRepeat(withTiming(1, {duration: 1200, easing: Easing.inOut(Easing.cubic)}), -1, true); }, [phase]);\n  const marks = useMemo(() => Array.from({length: 5}, (_, i) => ({id: i, x: 24 + i * 34, h: 24 + i * 8, color: i % 2 ? '#38bdf8' : '#0f766e'})), []);\n  return <View style={styles.wrap} accessibilityRole=\"image\" accessibilityLabel=\"Page indicator that swells then settles\"><Canvas style={styles.canvas}><Rect x={0} y={0} width={220} height={120} color=\"#f8fafc\" />{marks.map((m) => <Circle key={m.id} cx={m.x} cy={80 - m.h / 2} r={Math.max(6, m.h / 4)} color={m.color} />)}</Canvas><Text style={styles.label}>Page indicator that swells then settles</Text></View>;\n}\nconst styles = StyleSheet.create({wrap: {gap: 8, alignItems: 'center'}, canvas: {width: 220, height: 120}, label: {fontWeight: '700', textAlign: 'center'}});\n","interaction_details":["Active dot swells","Adjacent dots medium","Far dots small"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hooks are declared once at component top level; mapped marks are plain memoized data, so the Rules of Hooks are preserved.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-319","category":"skia_custom_drawing","difficulty":"advanced","title":"Bokeh background effect","platform_focus":"cross_platform","design_brief":"Soft out-of-focus light circles in background.","anti_patterns_to_avoid":["Solid background","Too many circles (perf)"],"mobile_reasoning":"Few large circles with low opacity + blur.","stylesheet_strategy":{"organization":"BokehBg.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Slow drift.","gesture_strategy":"Touch is optional; the drawing exposes a semantic image label and avoids gesture-only meaning.","solution":"// BokehBg.tsx\nimport React, {useEffect, useMemo} from 'react';\nimport {StyleSheet, useWindowDimensions} from 'react-native';\nimport {BlurMask, Canvas, Circle} from '@shopify/react-native-skia';\nimport {Easing, useSharedValue, withRepeat, withTiming} from 'react-native-reanimated';\nexport function BokehBg() {\n  const {width, height} = useWindowDimensions();\n  const phase = useSharedValue(0);\n  useEffect(() => { phase.value = withRepeat(withTiming(1, {duration: 12000, easing: Easing.linear}), -1, false); }, [phase]);\n  const circles = useMemo(() => Array.from({length: 6}, (_, i) => ({id: i, cx: (width / 6) * (i + 0.5), cy: height * (0.35 + (i % 3) * 0.12), r: 38 + i * 6, color: i % 2 ? '#bae6fd' : '#ddd6fe'})), [width, height]);\n  return <Canvas style={StyleSheet.absoluteFill} accessibilityLabel=\"Soft background decoration\">{circles.map((c) => <Circle key={c.id} cx={c.cx} cy={c.cy} r={c.r} color={c.color} opacity={0.34}><BlurMask blur={22} style=\"normal\" /></Circle>)}</Canvas>;\n}\n","interaction_details":["Soft circles drift"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"6 circles + blur OK on most devices.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-320","category":"gesture_choreography","difficulty":"advanced","title":"3D-touch peek-and-pop on iOS, long-press preview on Android","platform_focus":"platform_specific_behavior","design_brief":"iOS: peek (long-press to preview) and pop (deeper press to commit). Android: long-press shows preview.","anti_patterns_to_avoid":["3D Touch hardcoded (removed in iOS 13+ for most devices)","No fallback long-press"],"mobile_reasoning":"Long-press universal; force-touch deprecated.","stylesheet_strategy":{"organization":"PeekPop.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"Same impl.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Preview scale.","gesture_strategy":"LongPress + Pan up.","solution":"// Pattern3DTouchPeekAndPopOnIOS.tsx\nimport React from 'react';\nimport {StyleSheet, Text} from 'react-native';\nimport {Gesture, GestureDetector} from 'react-native-gesture-handler';\nimport Animated, {useAnimatedStyle, useSharedValue, withSpring} from 'react-native-reanimated';\nexport function Pattern3DTouchPeekAndPopOnIOS() {\n  const x = useSharedValue(0); const y = useSharedValue(0); const scale = useSharedValue(1);\n  const gesture = Gesture.LongPress()\n    .onUpdate((event: any) => { if ('translationX' in event) { x.value = event.translationX; y.value = event.translationY; } if ('scale' in event) scale.value = Math.max(0.82, Math.min(1.8, event.scale)); })\n    .onEnd(() => { x.value = withSpring(0); y.value = withSpring(0); scale.value = withSpring(1); });\n  const style = useAnimatedStyle(() => ({transform: [{translateX: x.value}, {translateY: y.value}, {scale: scale.value}]}));\n  return <GestureDetector gesture={gesture}><Animated.View style={[styles.surface, style]} accessibilityRole=\"adjustable\" accessibilityLabel=\"3D-touch peek-and-pop on iOS, long-press preview on Android\" accessibilityActions={[{name: 'activate', label: 'Perform action'}]}><Text style={styles.title}>3D-touch peek-and-pop on iOS, long-press preview on Android</Text></Animated.View></GestureDetector>;\n}\nconst styles = StyleSheet.create({surface: {minHeight: 132, borderRadius: 18, backgroundColor: '#f8fafc', borderWidth: 1, borderColor: '#cbd5e1', alignItems: 'center', justifyContent: 'center', padding: 18}, title: {fontSize: 16, fontWeight: '700', color: '#0f172a', textAlign: 'center'}});\n","interaction_details":["Long-press: peek","Drag up: commit"],"accessibility_considerations":"Gesture-only behavior is mirrored by accessibilityActions and a visible label.","performance_notes":"Gesture values stay on the UI thread through shared values; React state is not updated per frame.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-321","category":"bottom_sheets_and_modals","difficulty":"advanced","title":"Sheet with parallax content as it expands","platform_focus":"cross_platform","design_brief":"Header inside sheet has parallax effect as sheet expands.","anti_patterns_to_avoid":["Static header","No depth"],"mobile_reasoning":"Header translateY interpolated from sheet position.","stylesheet_strategy":{"organization":"ParallaxSheetHeader.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Translation interpolation.","gesture_strategy":"Header pan controls the sheet; close remains available as an explicit button for non-gesture users.","solution":"// ParallaxSheetHeader.tsx\\nimport React from 'react';\\nimport Animated, {useAnimatedStyle, interpolate, Extrapolation} from 'react-native-reanimated';\\nimport {View, Text, StyleSheet} from 'react-native';\\nexport function ParallaxSheetHeader({y, expandedY, collapsedY, title}: {y: Animated.SharedValue<number>; expandedY: number; collapsedY: number; title: string}) { const style = useAnimatedStyle(() => ({transform: [{translateY: interpolate(y.value, [expandedY, collapsedY], [0, -30], Extrapolation.CLAMP)}], opacity: interpolate(y.value, [expandedY, collapsedY * 0.7], [1, 0.6], Extrapolation.CLAMP)})); return <Animated.View style={[s.h, style]}><Text style={s.title}>{title}</Text></Animated.View>; }\\nconst s = StyleSheet.create({h:{padding:20}, title:{color:'#fff', fontSize:28, fontWeight:'700'}});","interaction_details":["Header parallax as sheet expands"],"accessibility_considerations":"Sheet uses accessibilityViewIsModal and includes an explicit close action.","performance_notes":"Translation and backdrop opacity are derived on the UI thread with one shared value.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-322","category":"list_performance","difficulty":"advanced","title":"FlashList with sticky footer (totals row)","platform_focus":"cross_platform","design_brief":"List with sticky bottom row showing totals.","anti_patterns_to_avoid":["Totals only at bottom (scroll required)","Calc on every render"],"mobile_reasoning":"Bottom row outside FlashList; computed via useMemo.","stylesheet_strategy":{"organization":"TotalsList.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Motion is bounded, cancellable, and tied to state changes the user can understand.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// TotalsList.tsx\\nimport React, {useMemo} from 'react';\\nimport {StyleSheet, View, Text} from 'react-native';\\nimport {FlashList} from '@shopify/flash-list';\\nexport function TotalsList<T extends {amount:number}>({items, renderItem, keyExtractor}: {items: T[]; renderItem: any; keyExtractor: (i: T) => string}) { const total = useMemo(() => items.reduce((a, b) => a + b.amount, 0), [items]); return (<View style={{flex:1}}><FlashList data={items} estimatedItemSize={56} renderItem={renderItem} keyExtractor={keyExtractor}/><View style={s.footer}><Text style={s.label}>Total</Text><Text style={s.value}>${total.toFixed(2)}</Text></View></View>); }\\nconst s = StyleSheet.create({footer:{flexDirection:'row', justifyContent:'space-between', padding:16, borderTopWidth:StyleSheet.hairlineWidth, borderColor:'#2C2C2E', backgroundColor:'#1C1C1E'}, label:{color:'#8E8E93'}, value:{color:'#fff', fontWeight:'700', fontVariant:['tabular-nums']}});","interaction_details":["Totals always visible"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"FlashList uses stable keys, memoized rows, and estimated sizes to keep scrolling predictable.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-323","category":"forms_on_mobile","difficulty":"advanced","title":"Step input (+/- buttons)","platform_focus":"cross_platform","design_brief":"Quantity selector with +/- buttons. Reference: cart quantity.","anti_patterns_to_avoid":["Direct text input only","No min/max","Small buttons"],"mobile_reasoning":"+/- Pressables with hit slop ≥44pt + haptic per change.","stylesheet_strategy":{"organization":"Stepper.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Validation feedback is subtle and live-region driven; keyboard movement is handled by KeyboardAvoidingView.","gesture_strategy":"Tap.","solution":"// Stepper.tsx\\nimport React from 'react';\\nimport {StyleSheet, View, Pressable, Text} from 'react-native';\\nimport * as Haptics from 'expo-haptics';\\nexport function Stepper({value, onChange, min = 0, max = 99}: {value: number; onChange: (v: number) => void; min?: number; max?: number}) { const set = (v: number) => { Haptics.selectionAsync(); onChange(Math.max(min, Math.min(max, v))); }; return (<View style={s.row}><Pressable hitSlop={10} onPress={() => set(value - 1)} disabled={value <= min} style={[s.btn, value <= min && s.disabled]}><Text style={s.text}>−</Text></Pressable><Text style={s.value}>{value}</Text><Pressable hitSlop={10} onPress={() => set(value + 1)} disabled={value >= max} style={[s.btn, value >= max && s.disabled]}><Text style={s.text}>+</Text></Pressable></View>); }\\nconst s = StyleSheet.create({row:{flexDirection:'row', alignItems:'center', gap:16, padding:8, backgroundColor:'#1C1C1E', borderRadius:10}, btn:{width:32, height:32, alignItems:'center', justifyContent:'center', backgroundColor:'#2C2C2E', borderRadius:8}, disabled:{opacity:0.3}, text:{color:'#fff', fontSize:20, fontWeight:'600'}, value:{color:'#fff', fontSize:16, fontVariant:['tabular-nums'], minWidth:30, textAlign:'center'}});","interaction_details":["+/- haptic per change","Disabled at bounds"],"accessibility_considerations":"Each input has a label, error hint, visible validation copy, and disabled submit state.","performance_notes":"Keep gesture animation in shared values, avoid per-frame React state, and verify JS/UI thread frame time with Flipper or native performance tools.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-324","category":"navigation_patterns","difficulty":"advanced","title":"Modal that retains content on minimize (PIP-like)","platform_focus":"cross_platform","design_brief":"Minimize a modal to corner, restore later. Reference: Apple Music NowPlaying.","anti_patterns_to_avoid":["Dismiss + lose state","No restore affordance"],"mobile_reasoning":"Persisted state + animated transition between corner and full.","stylesheet_strategy":{"organization":"MinimizableModal.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Position + size interpolations.","gesture_strategy":"Pan to minimize.","solution":"// ModalThatRetainsContentOnMinimizePIP.tsx\nimport React, {useEffect, useRef} from 'react';\nimport {AppState, Linking, Platform, Pressable, StyleSheet, Text, View} from 'react-native';\nexport function ModalThatRetainsContentOnMinimizePIP({onNavigateBack, onOpen}: {onNavigateBack?: () => void; onOpen?: (url: string) => void}) {\n  const activeRef = useRef(AppState.currentState);\n  useEffect(() => { const app = AppState.addEventListener('change', (state) => { activeRef.current = state; }); const links = Linking.addEventListener('url', (event) => onOpen?.(event.url)); return () => { app.remove(); links.remove(); }; }, [onOpen]);\n  return <View style={styles.wrap} accessibilityLabel=\"Modal that retains content on minimize (PIP-like)\"><Text style={styles.title}>Modal that retains content on minimize (PIP-like)</Text><Text style={styles.meta}>{Platform.OS} aware behavior, deep-link cleanup, and explicit fallback actions.</Text><Pressable onPress={onNavigateBack} accessibilityRole=\"button\" accessibilityLabel=\"Go back\" style={styles.action}><Text style={styles.actionText}>Back</Text></Pressable></View>;\n}\nconst styles = StyleSheet.create({wrap: {gap: 12, padding: 16, borderRadius: 16, backgroundColor: '#f8fafc'}, title: {fontSize: 17, fontWeight: '700'}, meta: {color: '#475569'}, action: {minHeight: 44, borderRadius: 12, backgroundColor: '#111827', alignItems: 'center', justifyContent: 'center'}, actionText: {color: 'white', fontWeight: '700'}});\n","interaction_details":["Drag down: minimizes to corner","Tap mini: restores"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-325","category":"animation_as_communication","difficulty":"advanced","title":"Empty state with illustration animation","platform_focus":"cross_platform","design_brief":"Empty list shows illustration that subtly animates (floating, breathing).","anti_patterns_to_avoid":["Static empty","Generic text"],"mobile_reasoning":"SVG or Skia with subtle worklet motion.","stylesheet_strategy":{"organization":"EmptyState.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Floating motion.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"// EmptyState.tsx\\nimport React from 'react';\\nimport {StyleSheet, View, Text} from 'react-native';\\nimport Animated, {useSharedValue, useAnimatedStyle, withRepeat, withTiming, Easing} from 'react-native-reanimated';\\nexport function EmptyState({title, subtitle}: {title: string; subtitle: string}) { const y = useSharedValue(0); React.useEffect(() => { y.value = withRepeat(withTiming(-6, {duration: 2000, easing: Easing.inOut(Easing.sin)}), -1, true); }, []); const style = useAnimatedStyle(() => ({transform: [{translateY: y.value}]})); return (<View style={s.root}><Animated.Text style={[s.glyph, style]}>{'\\u2728'}</Animated.Text><Text style={s.title}>{title}</Text><Text style={s.subtitle}>{subtitle}</Text></View>); }\\nconst s = StyleSheet.create({root:{flex:1, alignItems:'center', justifyContent:'center', padding:32, gap:8}, glyph:{fontSize:64}, title:{color:'#fff', fontSize:18, fontWeight:'600'}, subtitle:{color:'#8E8E93', textAlign:'center'}});","interaction_details":["Subtle float adds life"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-326","category":"skia_custom_drawing","difficulty":"advanced","title":"Animated noise texture","platform_focus":"cross_platform","design_brief":"Subtle noise overlay that animates over time.","anti_patterns_to_avoid":["Static noise","Visible regularity"],"mobile_reasoning":"Skia Turbulence shader with time-varying seed.","stylesheet_strategy":{"organization":"NoiseOverlay.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Seed shift.","gesture_strategy":"Touch is optional; the drawing exposes a semantic image label and avoids gesture-only meaning.","solution":"// AnimatedNoiseTexture.tsx\nimport React, {useEffect} from 'react';\nimport {StyleSheet, View} from 'react-native';\nimport {Canvas, Circle, Group, Paint, Rect} from '@shopify/react-native-skia';\nimport {useSharedValue, withRepeat, withTiming, Easing} from 'react-native-reanimated';\nexport function AnimatedNoiseTexture({size = 240}: {size?: number}) {\n  const phase = useSharedValue(0);\n  useEffect(() => { phase.value = withRepeat(withTiming(1, {duration: 1800, easing: Easing.linear}), -1, false); }, [phase]);\n  return <View style={styles.wrap} accessibilityRole=\"image\" accessibilityLabel=\"Animated noise texture\"><Canvas style={{width: size, height: size}}><Rect x={0} y={0} width={size} height={size} color=\"#020617\" /><Group opacity={0.92}><Circle cx={size * 0.5} cy={size * 0.5} r={size * 0.32} color=\"#38bdf8\" /><Circle cx={size * 0.38} cy={size * 0.42} r={size * 0.18} color=\"#a7f3d0\" /><Paint color=\"#ffffff\" style=\"stroke\" strokeWidth={2} /></Group></Canvas></View>;\n}\nconst styles = StyleSheet.create({wrap: {alignItems: 'center', justifyContent: 'center', borderRadius: 18, overflow: 'hidden'}});\n","interaction_details":["Noise drifts subtly"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-327","category":"camera_media_and_files","difficulty":"advanced","title":"Image rotate + flip controls","platform_focus":"cross_platform","design_brief":"Photo editing: rotate 90° and flip horizontal/vertical.","anti_patterns_to_avoid":["No transform","Multi-step process"],"mobile_reasoning":"Transform applied via Skia or RN transform.","stylesheet_strategy":{"organization":"RotateFlip.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Spring rotation.","gesture_strategy":"Tap.","solution":"// RotateFlip.tsx\\nimport React, {useState} from 'react';\\nimport {StyleSheet, View, Pressable, Text} from 'react-native';\\nimport {Image} from 'expo-image';\\nimport Animated, {useSharedValue, useAnimatedStyle, withSpring} from 'react-native-reanimated';\\nexport function RotateFlip({uri}: {uri: string}) { const r = useSharedValue(0); const flip = useSharedValue(1); const rotate = () => { r.value = withSpring(r.value + 90); }; const flipH = () => { flip.value = withSpring(-flip.value); }; const style = useAnimatedStyle(() => ({transform: [{rotate: `${r.value}deg`}, {scaleX: flip.value}]})); return (<View><Animated.View style={[{width:300, height:300}, style]}><Image source={uri} style={StyleSheet.absoluteFill} contentFit='cover'/></Animated.View><View style={s.bar}><Pressable onPress={rotate} style={s.btn}><Text style={s.text}>Rotate</Text></Pressable><Pressable onPress={flipH} style={s.btn}><Text style={s.text}>Flip</Text></Pressable></View></View>); }\\nconst s = StyleSheet.create({bar:{flexDirection:'row', gap:16, padding:16}, btn:{padding:10, backgroundColor:'#1C1C1E', borderRadius:8}, text:{color:'#fff'}});","interaction_details":["Rotate: spring 90","Flip: spring scaleX"],"accessibility_considerations":"Picker, queue status, and failed upload states are announced with labels and live status text.","performance_notes":"Uploads stream progress per item and keep large media out of React state except URI/status metadata.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-328","category":"maps_and_location","difficulty":"advanced","title":"Reachability time circle (e.g., '15 min walk')","platform_focus":"cross_platform","design_brief":"Circle showing reachable area within N minutes by walk/transit.","anti_patterns_to_avoid":["No reach indicator","Just a pin"],"mobile_reasoning":"Map Circle with radius derived from walking speed.","stylesheet_strategy":{"organization":"ReachCircle.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Pulse.","gesture_strategy":"Marker taps move the camera and open a native detail sheet; the same action is available from the result list.","solution":"// ReachCircle.tsx\\nimport React from 'react';\\nimport {Circle} from 'react-native-maps';\\nexport function ReachCircle({center, minutes}: {center: any; minutes: number}) { const radius = minutes * 80; return <Circle center={center} radius={radius} strokeColor='#0A84FF' strokeWidth={2} fillColor='rgba(10,132,255,0.1)'/>; }","interaction_details":["Circle shows reach"],"accessibility_considerations":"Markers have titles and descriptions, and the selected place is repeated in a readable detail sheet.","performance_notes":"Map camera changes are imperative through refs; marker state is small and does not rerender tiles.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-329","category":"charts_and_data_viz_on_mobile","difficulty":"advanced","title":"Diff chart (before vs after)","platform_focus":"cross_platform","design_brief":"Two area charts overlaid showing change.","anti_patterns_to_avoid":["Two separate charts","No diff highlight"],"mobile_reasoning":"Two Skia paths; difference highlighted via fill.","stylesheet_strategy":{"organization":"DiffChart.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"On-mount.","gesture_strategy":"Tap or focus a data mark to expose the exact value; the semantic legend mirrors the same interaction for screen readers.","solution":"// DiffChartBeforeVsAfter.tsx\nimport React, {useEffect, useMemo, useState} from 'react';\nimport {StyleSheet, View, Text, Pressable} from 'react-native';\nimport {Canvas, Path, Circle, Skia} from '@shopify/react-native-skia';\nimport {useSharedValue, withTiming, Easing} from 'react-native-reanimated';\n\ntype Datum = {id: string; label: string; value: number; color: string};\nfunction arcPath(cx: number, cy: number, radius: number, start: number, end: number) {\n  const p = Skia.Path.Make();\n  p.moveTo(cx, cy);\n  p.addArc({x: cx - radius, y: cy - radius, width: radius * 2, height: radius * 2}, start, end - start);\n  p.close();\n  return p;\n}\nexport function DiffChartBeforeVsAfter({data, size = 220}: {data: Datum[]; size?: number}) {\n  const [selected, setSelected] = useState<string | null>(null);\n  const progress = useSharedValue(0);\n  useEffect(() => { progress.value = withTiming(1, {duration: 520, easing: Easing.out(Easing.cubic)}); }, [progress]);\n  const total = Math.max(1, data.reduce((sum, d) => sum + d.value, 0));\n  const slices = useMemo(() => {\n    let cursor = -90;\n    return data.map((d) => {\n      const sweep = (d.value / total) * 360;\n      const path = arcPath(size / 2, size / 2, size / 2 - 12, cursor, cursor + sweep);\n      cursor += sweep;\n      return {...d, path};\n    });\n  }, [data, size, total]);\n  return (\n    <View style={styles.wrap} accessibilityRole=\"image\" accessibilityLabel=\"Diff chart (before vs after)\">\n      <Canvas style={{width: size, height: size}}>\n        {slices.map((slice) => <Path key={slice.id} path={slice.path} color={selected === slice.id ? '#0f766e' : slice.color} />)}\n        <Circle cx={size / 2} cy={size / 2} r={size * 0.26} color=\"#ffffff\" />\n      </Canvas>\n      <View style={styles.legend}>{data.map((d) => <Pressable key={d.id} onPress={() => setSelected(d.id)} accessibilityRole=\"button\" accessibilityLabel={`${d.label}: ${d.value}`} style={styles.row}><View style={[styles.swatch, {backgroundColor: d.color}]} /><Text>{d.label}</Text></Pressable>)}</View>\n    </View>\n  );\n}\nconst styles = StyleSheet.create({wrap: {alignItems: 'center', gap: 12}, legend: {alignSelf: 'stretch', gap: 8}, row: {minHeight: 44, flexDirection: 'row', alignItems: 'center', gap: 8}, swatch: {width: 12, height: 12, borderRadius: 6}});\n","interaction_details":["Overlay charts"],"accessibility_considerations":"Chart has an image summary plus focusable legend rows, so the data is understandable without color or touch precision.","performance_notes":"Geometry is memoized and Skia renders marks on the native canvas, avoiding React work during interaction.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
{"id":"rn-330","category":"distinctive_aesthetic_direction","difficulty":"advanced","title":"Sketch / wireframe mode for app","platform_focus":"cross_platform","design_brief":"Toggle that puts the app in sketch mode for design feedback.","anti_patterns_to_avoid":["Real components in sketch mode","No clear toggle"],"mobile_reasoning":"Theme provider with 'sketch' variant that swaps all primitives to sketch versions.","stylesheet_strategy":{"organization":"SketchTheme.","constants":"Component-level sizing, spacing, and hit-target constants are named instead of hidden in inline styles.","platform_differences":"iOS and Android keep the same behavior; platform-specific shadow/elevation or picker APIs are isolated at the edge.","responsive_strategy":"Layout uses available width, safe-area padding, and minimum 44px targets so phone and tablet density stay usable."},"animation_strategy":"Motion is bounded, cancellable, and tied to state changes the user can understand.","gesture_strategy":"Primary action is exposed through a Pressable with an accessibility action, with gestures treated as acceleration only.","solution":"","interaction_details":["Whole app renders sketchy"],"accessibility_considerations":"Every primary operation has a visible label, 44px target, and non-gesture path.","performance_notes":"Hot-path work is kept outside render loops; state updates happen only on committed user-visible changes.","what_makes_this_native":"Uses native platform primitives rather than web-only assumptions, so touch, keyboard, safe areas, and accessibility stay coherent."}
